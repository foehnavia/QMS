# MIS-QMS

Реляционная база **отклонений** производства (импланты/абатменты и инструмент).
Единый источник **архитектуры** (`docs/`) и **кода** (`src/`) — модель оформления
**Repo-central** (ратифицирована 2026-08-05).

## Слои и где что живёт

| Слой | Где | Правило |
|---|---|---|
| Архитектура (канон модели, спеки, справочники) | этот репо, `docs/` | единый источник; в волт не дублируется |
| Код приложения | этот репо, `src/` | — |
| PM-слой (решения, задачи, обсуждения) | волт `50_MIS-QMS/` | указатель на репо — `code-registry` |
| **Данные** (реальные отклонения) | **локально, вне репо и git** | автономны; в git не кладётся целый реальный документ/протокол — отдельные значения не sensitive (см. `.gitignore`) |

## Инвариант данных

Отдельные значения (номер детали `מק"ט`, номер WO `מנה`/`פק"ע`, отдельное заключение на
иврите) сами по себе **не sensitive** — вне собранного документа ценности не несут и
сохраняются штатно (рабочая БД, тесты, примеры). В git и волт не кладётся только **целый
реальный документ/протокол с привязкой к компании** (собранный `אישור חריגה`, протокол
целиком). Работающая база самостоятельна: не коннектится ни с Claude, ни с волтом, ни с git;
парсинг детерминированный (регулярки), без LLM в рантайме.

## Доступ

- **Claude Code (VS Code)** — нативно, весь репо.
- **Дизайн-голова (Cowork)** — через подключённую папку («Add folder»).

## Сборка и поставка (S7, наряд `0040`)

Однофайловый `.exe` собирается из корня репо **одной командой**:

    .venv\Scripts\python.exe -m PyInstaller --noconfirm MIS-QMS.spec

На выходе `dist/MIS-QMS.exe`. Артефакт в git не кладётся — коммитится рецепт
(`MIS-QMS.spec`): воспроизводимость даёт он, а не файл.

**Куда класть `.exe` — не в `Program Files`.** База (`app.sqlite`) живёт **рядом с
исполняемым файлом**, и в `Program Files` Windows писать не даст: программа не сможет
создать базу. Обычная рабочая папка, например `C:\MIS-QMS-app\`.

Это же и делает бэкап тем, чем он объявлен: **бэкап = копия одного файла**. Оператор
видит `app.sqlite` рядом с программой, копирует его мышью, и возвращает так же —
приложение поднимется на возвращённой копии.

Увести базу в другое место (сетевую папку, другой диск) можно переменной окружения
`QMS_DB_URL`, например `sqlite:///D:/qms/app.sqlite`.

При первом запуске в пустой папке приложение само создаёт базу, доводит схему
миграциями до актуальной ревизии и засевает справочники. Новых миграций оно не создаёт.

## Статус

Этап — **проработка модели сущностей** (сессии 01–03, консолидация в
`docs/model/_CONCEPT.md`). Проектирование/построение не начато, стек не выбран.
Карта репозитория — `docs/_INDEX.md`.
