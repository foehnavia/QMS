[pytest]
# NOTE: keep this file ASCII-only - configparser-style files are read with the
# machine locale (cp1255 on the target machine).
pythonpath = src
testpaths = tests
addopts = -q
# SQLAlchemy warns about half-wired object graphs - treat that as a failure.
filterwarnings =
    error::sqlalchemy.exc.SAWarning
