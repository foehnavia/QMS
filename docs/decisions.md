# MIS-QMS — реестр архитектурных решений

> Пополняется по ходу дизайн-сессий. Ратифицированные развязки — из
> `50_MIS-QMS/PROPOSAL_MIS-QMS_structure` (волт) и последующих сессий.

| Дата | Решение | Детали / источник |
|---|---|---|
| 2026-07-22 | Канон модели — Session-03-latest; сессии 01–02 — история | сессии 01–03 |
| 2026-08-05 | Оформление — **Repo-central**: домен-линия `50_MIS-QMS` (PM-слой) + внешний репо (`docs/`+`src/`); данные автономны; канон — логическими блоками + индекс | PROPOSAL §3 |
| 2026-08-05 | Отдельная линия (домен), не подпроект; код — внешний приватный git-репо; данные изолированы (в git/волт никогда); хранение — блоками + оглавления | PROPOSAL §3 |
| 2026-08-06 | **Путь репо — `C:\MIS-QMS`** (рабочая машина), не `D:` — уточнение к ратификации | сессия QMS-001 |
| 2026-08-06 | **§6.4 — судьба Session-01/02/03:** архив в репо `docs/model/_history/`, доступ к знаниям на концепт-сессиях обеспечен | сессия QMS-001 |
| 2026-08-06 | **§6.3 (нарезка по сущностям) — отложена (Волна 0b):** сперва собрать полный предварительный концепт (сессии 04+) и закрыть открытые вопросы (State, WO, поток Deviation), затем резать, понимая полную картину. Волна 0a — репо + история + консолидированный концепт | сессия QMS-001 |
| 2026-08-06 | **Приватный GitHub-remote заведён — `github.com/foehnavia/QMS`.** Источник истины = remote; локальные клоны `C:\MIS-QMS` (раб.) / `D:\MIS-QMS` (дом.); работа с 2 машин. Личный проект, sensitive-данных нет | сессия QMS-001 |
| 2026-08-10 | **Итоговый концепт собран — `CONCEPT_full_rev1.00_EN` (канон) + `_RU` (перевод).** Заменяет `_CONCEPT.md` (→ архив; при конфликте приоритет у full). Размещён в репо `docs/model/` и в волте `50_MIS-QMS/` (зеркала) | сессия QMS-008 · `CONCEPT_full` §12 |
| 2026-08-10 | **R1 — state-linking атрибут возвращён:** спящий `state-depending dimension` (по умолчанию `none`, хранит номер связанного размера). Разворот отклонения хеджа `feature`/`stage` в s06. State по-прежнему не сущность | QMS-008 · `model/Characteristic.md` |
| 2026-08-10 | **R2 — маппинг ранний, до регистрации** отклонения (кнопки «Создать маппинг/привязать» в форме ввода). Ретроактивный маппинг — риск потери данных, только для срочного WO. Разворот «ленивого/ретроактивного резолва» s06 | QMS-008 · `model/CharacteristicGroup.md` |
| 2026-08-10 | **R3 — CG можно создавать по ходу** при заведении детали (не только отдельный админ-процесс) | QMS-008 · `model/reference/reference-data.md` |
| 2026-08-10 | **Уточнения rev1.00:** карточка отклонения — главный deliverable (автообзор прецедентов); исследование ↔ пара (Item, размер); поиск по уровням; терминология Инспекция → Исследование; расширены зона/тип | QMS-008 · `model/DeviationCard.md`/`Inspection.md`/`Search.md` |
| 2026-08-10 | **Нарезка канона выполнена (QMS-002 / Волна 0b).** `CONCEPT_full` разрезан на `docs/model/`: `_overview.md` (канон-вход: нарратив + процесс) + файл на сущность (Item, Characteristic, CharacteristicGroup+Mapping, Deviation, Finding, Inspection, DeviationCard, Import-Workflow, Search) + `reference/` (reference-data, output-document) + `specs/` (стабы UI). Прежние `CONCEPT_full_*` в репо → короткие баннеры «superseded». Причина: старт дизайн/сборки — детализация должна падать в правильные файлы | QMS-002 · этот файл |
| 2026-08-10 | **Единственный источник + зеркало.** Канон = нарезка `docs/model/`, правится только в репо-сессиях. Волт держит **одно** плоское read-only зеркало `50_MIS-QMS/CONCEPT_full_rev1.00_EN.md` (генератор `tools/build_mirror.py`, `source_version` = rev канона) для offline-сессий (телефон без репо). RU-зеркало — заморожено на rev1.00, по требованию. Регенерация — при закрытии сессии, изменившей `model/`; guard при открытии сверяет `source_version` (Слой 0). Сессия без репо канон не правит | QMS-002 · `docs/_INDEX.md` (Зеркало и синхронизация) · `50_MIS-QMS/code-registry` |
| 2026-08-10 | **Q-06 закрыт (развилки нарезки):** (1) Маппинг — **внутри `CharacteristicGroup.md`** (один абзац, намертво связан с g-позицией); (2) формы/UI — в **`specs/`**, не inline; (3) §12/архитектурные вопросы — в репо **`decisions.md`** (этот файл), волт `OPEN_QUESTIONS` — только PM/кросс-проектное | QMS-002 |
| 2026-08-10 | **Стек утверждён (QMS-009) и проверен на машине.** SQLite (источник истины) · SQLAlchemy 2.x + Alembic · openpyxl + regex ETL (без LLM) · **PySide6** UI (нативный RTL) · **docxtpl** → PDF через **Word/Office COM** · упаковка PyInstaller (onefile `.exe`). 5 фаз верификации зелёные на Store-Python 3.13.14 x64 (COM и PyInstaller пригодны). Полное описание — `docs/architecture.md` | QMS-009 · `architecture.md` · [[2026-08-10 meeting MIS-QMS architecture and stack]] |
| 2026-08-10 | **Источник = иврит-Excel** (`openpyxl`), Access/ODBC исключён; приложение — **источник истины** (read+write), не read-only вьюер | QMS-009 · `architecture.md` |
| 2026-08-10 | **RTL-документы:** docx генерятся `docxtpl` поверх реального RTL-бланка DS-QC.2-2 + явный `bidi`/right-align на ивритских абзацах | QMS-009 |
| 2026-08-10 | **Операционная модель Cowork ↔ Claude Code** согласована (репо — мост; Claude Code — руки+git+локальный диагност; read-only доступ к волту; наряды `docs/worklog/`). Формализация — INFRA-013, **гейт перед QMS-010** | QMS-009 · [[2026-08-10 meeting MIS-QMS architecture and stack]] |
| 2026-08-10 | **Уточнение политики данных: отдельные значения не sensitive.** Номер детали `מק"ט`, номер WO `מנה`/`פק"ע`, отдельное заключение на иврите сами по себе **не** sensitive — вне собранного документа ценности не несут, штатно попадают в БД и сохраняются, использовать можно где угодно (тесты/фикстуры/примеры). В git и волт не кладётся только **целый реальный документ/протокол с привязкой к компании** (собранный `אישור חריגה`, протокол целиком). Автономность БД — архитектурная изоляция рабочих данных, не вопрос конфиденциальности. Синхронизировано: `CLAUDE.md` §6, `README`, `_INDEX`, `architecture` §1, `model/Import-Workflow`; волт `_BRIEF_MIS-QMS`/`code-registry`. Зеркало `CONCEPT_full` — регенерировать. `RULES` §5 (Cus_Com обезличивание) не затронут | решение пользователя (INFRA-013) |
| 2026-08-11 | **Контроль-timeline проекта — в волте** (`50_MIS-QMS/ROADMAP_MIS-QMS`, rolling-wave: детально текущий+следующий спринт, чек-листы поставок/приёмки, без привязки к календарю). Репо несёт **исполнение** (`docs/worklog/NNNN`) + канон (`docs/model/`); второго роадмапа в репо нет — single source of truth. План сборки: Этап 1 — S1–S7, Этап 1.5 — S8 (прикидочно) | QMS-010 · `docs/_INDEX.md` · волт [[2026-08-11 meeting MIS-QMS development plan and roadmap]] |
| 2026-08-11 | **S1 принят (QMS-011 / наряд 0001) — ратификации физсхемы rev 0.1:** (1) `characteristic.state_depending` — nullable self-FK (не «номер»); проза `model/Characteristic.md` требует синхронизации под FK. (2) `decision_dev` **nullable** (NULL = решение не принято; регистрация шаг 3, решение шаг 8). (3) `explanation` NOT NULL с дефолтом пусто; «непусто» — доменная валидация на одобрении (S4). (4) `direction` NOT NULL (из слова max/min). (5) `attachment` — одно текстовое поле (ссылки). (6) ~~Код 99 — булев флаг `mapping.is_absent` + CHECK `target_xor_absent`~~ — **ПЕРЕСМОТРЕНО 2026-08-11 (QMS-013, rev 0.2):** флаг не хранил, *какой именно* позиции нет у детали, поэтому пара `g:99` канона на нём невыразима; код 99 переехал в таблицу `item_position_absent`, флаг и CHECK сняты, `mapping.g_position_id` стал NOT NULL. Отказ от сентинел-`g_position` остаётся в силе — обоснование S1 не оспорено. (7) Бизнес-номер `YYMMDD` — от системной даты присвоения | QMS-011 · наряд `docs/worklog/0001-foundation.md` · [[2026-08-11 meeting MIS-QMS S1 foundation build]] |

| 2026-08-11 | **S3 принят (QMS-013 / наряд 0003) — решения канон-слоя:** (1) визуальный CG-редактор — баллоны **поверх картинки чертежа**, не абстрактная раскладка; (2) **чертёж хранится в БД** (`characteristic_group.drawing`, PNG/JPEG ≤5 МБ, проверка по сигнатуре) — осознанное исключение из «вложения ссылками» (`architecture.md` §4), порог пересмотра ~200 МБ файла БД → флаг на S7; (3) **код 99 — отдельная таблица `item_position_absent`** (пара деталь↔g-позиция), флаг `mapping.is_absent` и CHECK сняты, `mapping.g_position_id` NOT NULL — **пересматривает ратификацию S1 №6**; (4) координаты баллонов **нормализованы 0..1**, не пиксели; (5) точка входа привязки — раздел «Группы характеристик» **и** карточка детали; (6) «ранние кнопки» R2 не строятся в S3 — S3 отдаёт переиспользуемый диалог с публичным входом `MappingDialog.run(...)`, проводка в форму ввода — S4; (7) перепривязка занятого размера — не молча: сначала «Очистить»; (8) правка CG с защитой занятых позиций; **индекс g-позиции неизменяем** у существующей позиции — на него ссылаются привязки всех деталей | QMS-013 · наряд `docs/worklog/0003-cg-mapping.md` · `architecture.md` §4–§5 |
| 2026-08-11 | **Правило «1 баллон = 1 размер» — с обеих сторон** (шире буквы Session-03 §4): на одну g-позицию у детали идёт один размер, и один размер привязан к одной позиции. Конфликта с состояниями (до/после ЭП) нет — канон разносит их по разным g-позициям **и** разным `local_number` | QMS-013 · `model/CharacteristicGroup.md` |

| 2026-08-17 | **S4 принят (QMS-014 / наряд 0004 + доводка `3cc0105`) — решения слоя ручного ввода.** Схема rev 0.2 **не менялась**. (1) **Решение — отдельное действие**, а не поле формы регистрации: порядок канона (регистрация — шаг 3, решение — шаг 8, после изучения прецедентов); `DecisionDialog` самостоятелен и в S5 переезжает в карточку без переделки. (2) **`update_registration`** — доменная правка шапки, в наряде не значилась, ратифицирована; **деталь после регистрации неизменна** (размеры находок принадлежат ей, перенос осиротил бы их) — перенос отклонения на другую деталь остаётся отдельным доменным действием, которого нет намеренно. (3) **`ncr=None` в `set_decision` = «не трогать»**, а не «стереть» (номер приходит от QA и вводится и на регистрации, и в решении; стереть — явной пустой строкой). Асимметрия с `decision_date=None` = «сейчас» осознанная: правило S3 «заменяет целиком» запрещает **стирание** пропуском, а не сохранение. (4) **Колонка «канон» — три достижимых состояния:** `g5` · «не привязан» · «размер ещё не заведён». **«Нет у детали (99)» недостижимо по построению** — код 99 отмечает g-позицию, которой у детали *нет*, а находка всегда про размер, который *есть*; трёхзначная формулировка наряда была ошибочна. (5) **Исследование — только на сохранённой находке** (ссылается на строку `finding`, а находки пишутся одной транзакцией по «Сохранить»); альтернатива «писать по действию» отвергнута — ломает транзакцию формы. (6) **Зона и тип отклонения из формы находки не заводятся** (в отличие от детали и CG по R3): их чистит администратор, быстрое добавление плодило бы синонимы-дубли. (7) **Порядок записи находок: сначала запись, потом удаление**, множество оставляемых собирается **по ходу записи** — иначе замена всех находок разом упирается в гард «должна остаться хотя бы одна» (дефект, найденный ревью), а расчёт множества заранее сносит только что созданные находки | QMS-014 · наряд `docs/worklog/0004-deviation-findings-inspection.md` · спека `specs/deviation-entry.md` |
| 2026-08-17 | **Конвенции UI и тестов вынесены в репо-`CLAUDE.md` §9.** Bidi (изолят для собственных строк, `ltr_field` для редакторов Qt), offscreen (годен для тестов, непригоден для снимков — база шрифтов пуста), **модальный диалог в тесте вешает прогон** (тест на успешное сохранение обязан перехватывать `show_error`, иначе регрессия не падает, а висит), «заменяет целиком» для доменных `update_*`, cp1255. Повод: наряды S2–S4 повторяли эти правила текстом «как в S2/S3» — признак того, что им место в конституции исполнителя, а не в каждом наряде | QMS-014 · `CLAUDE.md` §9 |

| 2026-08-17 | **S5 принят (QMS-015 / наряд 0005 + доводка `ee55428..5461e63`) — карточка отклонения и поиск прецедентов.** Главный deliverable Этапа 1. Схема rev 0.2 **не менялась** — вся функция выражена запросами. (1) **Карточка — отдельное окно**, а не раздел навигации и не переработка формы S4: карточка всегда про одно отклонение, пустой ей быть не от чего; открывается из списка (кнопка + двойной клик) и **сама после регистрации нового** отклонения (канон «opens as soon as a deviation is entered»), при правке существующего — нет. (2) **Прецеденты — по выбранной находке**, а не по отклонению целиком: пять размеров иначе сваливаются в одну кучу. (3) **В выдачу входят только отклонения с внесённым решением** (все уровни) — прецедент существует ради готового решения и обоснования; осознанное следствие: свежая нерешённая повторяемость не видна, статус-строка говорит об этом словами. (4) **Единица выдачи — отклонение целиком** (`Search.md`): **L2 сворачивается по отклонению** с сохранением сильнейшего совпадения (запрос идёт по находкам, иначе одно отклонение встаёт дважды, а счётчик «похожих» считает находки); L1a/L1b свёртки не требуют по построению (один размер — одна находка; на g-позицию у детали ровно один размер). (5) **L2 — условие `OR` (зона или тип)** с рангом «совпало по обоим — выше»: L2 работает там, где точного совпадения нет, требование обеих меток его выключает. (6) **Вкладка L2 по умолчанию — только при открытии** карточки с пустым L1; смена находки не уводит оператора с вкладки, выбранной руками. (7) **`DecisionDialog` переехал без единой правки** (проверено: файл не в изменённых), кнопка «Решение…» осталась **и в списке**, и в карточке — короткий путь, когда решение уже известно. (8) **Двойной клик в списке отклонений переключён с правки на карточку** — изменение принятого поведения S4; прежний путь остался кнопкой «Открыть…». (9) **`canon_labels_for_item` сверх наряда** ратифицирована: третье состояние «размер ещё не заведён» по определению не имеет характеристики, спрашивать его по объектам нельзя; строки состояний канона живут в домене (прецедент `errors.py`) — при первой надобности в другом языке выносятся в UI. (10) **Выбор строки прецедента физически держится в одной таблице** (выбор в любой очищает соседние): три независимые таблицы + предпочтение «первой, где что-то выбрано» открывали строку не той секции, а «последняя таблица, где меняли выбор» через `itemSelectionChanged` не отслеживается — повторный клик по уже выбранной строке сигнала не даёт. Дефект найден ревью по факту кода, недостаточность первой правки — тестом. (11) **Уточнение §9 конституции (bidi):** изолят должен быть **один — вокруг собранной строки**; два изолята подряд остаются двумя runs и в RTL раскладываются справа налево (`0.05 −` вместо `− 0.05`). «Печатать через изолят» недостаточно, если в ячейке больше одного токена | QMS-015 · наряд `docs/worklog/0005-deviation-card-search.md` · спека `specs/deviation-card.md` · [[2026-08-17 meeting MIS-QMS S5 deviation card]] |
| 2026-08-17 | **Q-09 закрыт — сторож зеркала по содержимому (вариант А).** `tools/build_mirror.py` штампует в зеркало `source_hash` (sha256 по набору канон-файлов), `--check` сверяет его с каноном «как он сейчас», печатает список файлов, оба хеша и вердикт, **ничего не пишет**; `source_version` остаётся для человека, ручной подъём `rev` больше не является условием исправности сторожа. Два свойства рецепта, обязательные и оба выученные на ошибках: **нормализация CRLF→LF** (иначе checkout на Windows красит сторож на байт-идентичном содержимом) и **хеширование в порядке строк относительных путей**, а не сравнением объектов путей — сравнение путей регистронезависимо на Windows и регистрозависимо на POSIX, из-за чего один и тот же канон давал два разных хеша и ломался ровно тот сценарий, ради которого сторож сделан (штампует Claude Code на Windows, проверяет Cowork из Linux-контейнера). Порядок хеширования печатается `--check` и платформенно-независим по построению. Артефакт пишется **внутрь репо** (`build/mirror/`, в `.gitignore`), в волт его переносит Cowork — Claude Code в волт не ходит (INFRA-013). Эталонная константа хеша в тестах считается по **синтетическому** канону: по настоящему она падала бы при каждой правке модели, ничего не сообщая о рецепте. Реворд `model/Characteristic.md` под ратификацию S1 №1 (self-FK, не номер) выполнен Cowork; зеркало перегенерировано и перенесено в волт, штамп `sha256:dd03fe0b…` подтверждён пересчётом из Linux-контейнера | QMS-015 · [[OPEN_QUESTIONS_MIS-QMS]] Q-09 · [[code-registry]] · `docs/_INDEX.md` |

| 2026-08-19 | **Язык интерфейса — English; шасси окна — LTR; направление данных — на уровне ячейки (QMS-016 / наряд 0007).** Язык интерфейса **не был решён ни разу**: `architecture.md` §3 выбирал PySide6 ради нативного RTL/bidi, но обосновывал это ивритом **в данных и в Excel-источнике импорта**; про язык подписей в каноне не было ни строки. Русские подписи затекли из нарядов S2–S5, спеки записали их as-built, ревью язык не проверяло — пять спринтов подряд (находка прогона №5, корзина **М**). Ратифицировано: (1) **интерфейс только English**, включая значения справочников, заголовки окон и доменные сообщения об ошибках — их читает оператор в модальном окне, значит это интерфейс; подписи берутся **словами канона** `docs/model/`, иначе спека и экран снова разойдутся. (2) **Шасси — LTR.** Прежний глобальный RTL склеивал два разных вопроса; класс дефектов S2–S5 (даты задом наперёд, разворот `±`, отрывающаяся пунктуация) порождён тем, что **интерфейсные** строки жили в RTL-контексте. (3) **Направление данных — три уровня** (шасси · ячейка · вкрапление): уровня «ячейка» в коде не было вовсе, и это, а не перевод строк, было главной работой наряда. Хелперы **не сняты**: бо́льшая часть содержимого таблицы отклонений ивритская, ячейки смешанные — они перестали быть заплатками под RTL-окно и стали механизмом. (4) **Числовая колонка объявляется списком, а не угадывается**: у `19.08.2026` сильных символов нет, и без объявления база досталась бы ей от соседа по строке. (5) **Гард на кириллицу в `src/`** (AST, докстроки и комментарии исключены намеренно — внутренний слой): без него язык уехал бы обратно на первом же спринте, ровно как уехал в первый раз | QMS-016 · наряд `docs/worklog/0007-ui-english-ltr.md` · `architecture.md` §3.1 · `CLAUDE.md` §9 |
| 2026-08-19 | **Правило смешанной ячейки: изолят — на токен, а не на строку (уточнение ратификации S5).** Снято со стенда реальных ивритско-английско-числовых строк (`tools/bidi_stand.py`), а не выведено умозрительно. Ратификация S5 «изолят один — вокруг собранной строки» **остаётся верной для того, на чём выведена**: атомарный токен без сильных символов внутри (`− 0.05`, `⌀ 3.75`, `+0.02 / −0.05`) обязан быть **одним** изолятом — два изолята подряд остаются двумя runs и в RTL раскладываются справа налево (`0.05 −`). Но на ячейке из **нескольких самостоятельных токенов** это правило не работает: один изолят вокруг всего задаёт только базу (её и так задаёт делегат), а сами токены остаются переставленными — `иврит · thread burr · − 0.05` показывалось как `иврит · − 0.05 · thread burr`, а пара допусков `+0.02 / −0.05` — как `0.05− / 0.02+`. Правило: **каждый самостоятельный токен — в своём изоляте** (`ui.common.joined`), порядок токенов достаётся базовому направлению ячейки. Оба правила — одно: **ровно один изолят на атомарный токен**; ни двух вокруг частей одного токена, ни одного вокруг нескольких | QMS-016 · наряд `0007` · стенд `build/screens/bidi-stand.png` · `CLAUDE.md` §9 |
| 2026-08-19 | **Выравнивание ячейки задаётся логически, сторону выбирает Qt.** Поверх `option.displayAlignment` Qt при отрисовке накладывает `QStyle.visualAlignment(direction, …)`, которая под RTL меняет левое на правое. Делегат, просивший `AlignRight` для ивритской ячейки, получал выравнивание **влево**: порядок слов был RTL, а строка липла к левому краю. Правильно — всегда `AlignLeft` в смысле «к началу строки». Дефект найден **снимком экрана** (ивритский справочник), а не тестом: тест сверял запрошенное выравнивание, а не отрисованное — теперь сверяет отрисованное. Отдельный довод в пользу того, что прогон со снимками — часть приёмки, а не украшение | QMS-016 · наряд `0007` · `tests/test_ui_direction.py` |

| 2026-08-19 | **Направление и выравнивание ячейки — два разных вопроса, с разными списками колонок** (решение Cowork по ревью наряда 0007). Один список `numeric_columns` нёс оба смысла сразу. Разделено: **направление** — всегда LTR у всего числового (дата, счётчик, идентификатор, величина): сильных символов у них нет, и без объявления база досталась бы им от соседа-иврита по строке. **Выравнивание вправо** — только у того, что глаз сравнивает **по величине** вниз по столбцу: `Nominal`, `Tolerance +/−`, `Value`, `Sign · value` (`magnitude_columns`); там разряды встают в столбик и разброс виден без чтения. Влево — даты, счётчики (`Findings`, `Inspections`, `Characteristics`, `Positions`), идентификаторы и номера: сравнивать нечего, а левый край держит их у подписи колонки. Величина — частный случай числовой колонки, дважды не перечисляется. Двусмысленности с `visualAlignment` у неё нет: `magnitude` всегда LTR, и `AlignRight` там означает ровно правый край | QMS-016 · ревью наряда `0007` · `ui/common.py::DirectionalDelegate` |
| 2026-08-19 | **`bind_direction` — только для `QLineEdit`; текстовой области Qt резолвит направление по абзацу сам** (ревью Р-2, подтверждено исполнением на PySide6 6.11.1). У `QPlainTextEdit` `block.textDirection()` даёт RTL для ивритской строки **и в LTR-поле, и в RTL-поле**, а `document().defaultTextOption()` остаётся `LayoutDirectionAuto`: хелпер на раскладку текста не влияет вовсе. Влияет он только на хром — полоса прокрутки перепрыгивает на другую сторону, стоит набрать первый ивритский символ. И главное: текстовая область держит абзацы **разных** направлений сразу (ивритское обоснование и латинский путь к протоколу в одном поле), поэтому одно направление на виджет там неверная единица. Хелпер теперь **отказывает** на текстовой области `TypeError`-ом: молча-бесполезный вызов повторил бы ошибку. Тест переписан на `document().firstBlock().textDirection()` — сверять `layoutDirection` значило сверять свойство, которое код сам же и выставил (**тот же класс ошибки**, что был найден в делегате снимком экрана, повторённый на файл дальше) | QMS-016 · ревью наряда `0007` · `tests/test_ui_direction.py` |

| 2026-08-19 | **Ратификация: единица направления — абзац (ячейка таблицы, строка списка, абзац текста), а не виджет и не окно.** Обобщение, под которое подводятся все частные правила QMS-016. Основание — **снимок на нативной платформе**: в одном `QPlainTextEdit` абзацы разных направлений (ивритское обоснование, латинская фраза, путь к протоколу) раскладываются каждый по-своему, и картинка в LTR- и RTL-виджете **одна и та же** — отличается лишь сторона полосы прокрутки. Значит направление, приписанное виджету, не описывает то, что нарисовано; тем более его не описывает направление, приписанное окну. Следствия, уже действующие: шасси LTR — это только шасси; ячейке направление считает делегат по её значению; `QLineEdit` — вырожденный случай (один абзац), потому и единственный, кому нужен `bind_direction`; текстовой области Qt резолвит абзацы сам. **Проверять направление надо на абзаце** (`block.textDirection()`), а не на свойстве виджета — свойство код сам же и выставил | QMS-016 · наряд `0007` + ревью · `CLAUDE.md` §9 |
| 2026-08-19 | **Ратификация: единица изоляции — атомарный токен, ровно один изолят на токен.** Ни двух изолятов вокруг частей одного токена, ни одного вокруг нескольких. Основание — стенд `tools/bidi_stand.py` на реальных по форме ивритско-английско-числовых строках, а не рассуждение. Формулировка S5 («изолят один — вокруг собранной строки») **не отменяется, а получает область действия**: она верна для атомарного токена — того, внутри которого нет сильных символов или который читается как одно целое (`− 0.05`, `⌀ 3.75`, `+0.02 / −0.05`, дата, бизнес-номер). Составная ячейка из нескольких самостоятельных токенов собирается `ui.common.joined`: один изолят вокруг всего задаёт только базу (её и так задаёт делегат), а токены остаются переставленными. Следствие для кода: величина со знаком — **атомарный токен и одна колонка** (`signed_label`), раздельные колонки разводили знак и его величину по краям соседних столбцов | QMS-016 · наряд `0007` + ревью · стенд `build/screens/bidi-stand.png` · `CLAUDE.md` §9 |
| 2026-09-01 | **Ратификация: индекс g-позиции выдаётся один раз и не переиспользуется.** Новая позиция получает `max(текущих) + 1`; руками индекс не вводится, дыра в середине не заполняется никогда. Основание — **не** ссылочная целостность (её держит суррогатный `g_position_id`), а общий словарь чертежа и базы: `g5` написан и на чертеже, и в протоколе контроля, и в записке оператора. Инструмент существует ради сравнения через время, и ярлык, молча поменявший смысл между ревизиями, ломает именно эту функцию — причём ни одна проверка этого не поймает. **Осознанный остаток:** удалив последнюю позицию, следующая получит тот же индекс. Строгое «никогда» требует монотонного счётчика на группе, то есть ревизии схемы; остаток узкий — удалить можно только позицию, которую ни разу ни с чем не связали, значит переиздан будет индекс, никогда не покидавший машину. Счётчик — кандидат в схемную правку, если прогон покажет чертежи со ссылками на удалённые индексы. | QMS-016 · наряд `0010` §10 (В-8) · `model/CharacteristicGroup.md` |
| 2026-09-01 | **Ратификация: диапазон g-позиций — жест ввода, а не хранимое правило.** «Отметить g1…g24» отмечает все существующие в интервале **в момент действия** и немедленно материализуется в перечень привязок (`mapping` + `item_position_absent`). В базе интервала нет, правила нет, пересчёта при чтении нет. Основание: после ратификации В-8 дыры законны, и запись «g1—g24» перестаёт быть однозначной — либо её смысл меняется задним числом при добавлении новой позиции, либо она ложно утверждает, что позиции 1…24 существуют. Индекс после В-8 — идентификатор, а не порядковый номер; диапазон предполагает и порядок, и плотность, а у идентификатора нет ни того, ни другого. Модель не меняется: связь и так хранится перечнем. | QMS-016 · наряд `0010` §12 (М-1) |
| 2026-09-01 | **Ратификация: вывод исследования подписывается `Acceptable` / `Not acceptable`.** Хранимые значения `approved` / `not_approved` **не меняются** — правится только подпись. Прежнее `Deviation approved` называло объект, к которому исследование не привязано (оно висит на находке), и дословно совпадало с `decision_dev = approved`, на который по ратификации S4 не влияет: две разные сущности под одной подписью в одной карточке. `Finding approved` тоже не годится — по `Finding.md` находка решения не несёт вовсе. Подпись называет не объект (его называют строка и заголовок колонки `Result`), а суждение; пара «acceptable — approved» держит различие, ради которого канон и разводил два поля: исследование говорит, что деталь **может** быть принята, решение — что с ней **сделано**. | QMS-016 · наряд `0010` §11 (В-9) · `ui/kit` |
| 2026-09-01 | **Конвенция теста: сверяй то, чем рисуют, а не подпись рядом.** Третий случай за цикл QMS-016: тест на выравнивание проверял запрошенное вместо отрисованного; тест на направление поля — свойство виджета вместо направления абзаца; тест на пилюлю исхода — текст рядом вместо кода, которым её красят. Во всех трёх тест был зелёным, а экран неверным. Правило: если значение влияет на отрисовку, тест берёт его из того же источника, что и отрисовка. | QMS-016 · наряды `0007`, `0011` + доводки · `CLAUDE.md` §9 |

| 2026-09-02 | **Баллоны убраны: чертёж группы приходит размеченным, позиции ведутся таблицей** (прогон QMS-016, шаг 4). Чертёж CG выпускается конструкторским отделом уже с метками `G1…GN` на выносках, поэтому расстановка баллонов оператором поверх картинки — двойная работа и главный источник ошибки привязки. Чертёж грузится как есть и служит визуальной справкой; `g_position.x`/`y` не заполняются (колонки остаются, миграция не оправдана). При создании группы задаётся **число позиций** — таблица разворачивается на `g1…gN`; строка = индекс · номинал · допуск `+` · допуск `−`. Привязка детали — таблица «g-индекс → локальный номер / 99». **Номинал и допуск остаются у группы** (внутри CG `G1` — один физический размер с одним допуском у всех деталей; своим у детали остаётся только локальный номер) — предположение сессии о переносе значений на деталь проверено и отклонено пользователем. **Подпись у g-позиции не заводится:** позиция — внутренняя механика перекрёстного поиска, пользователю не показывается. **Допуски формы (соосность к базам) заводятся обычными позициями**, вид допуска и база не хранятся — они на чертеже; GD&T на этапе 1 не разбирается. Правило «индекс выдаётся `max+1`, руками не вводится» сохраняет силу и при развороте таблицы совпадает с нумерацией чертежа. **Живёт в ветке `run/qms-016`** | QMS-016 · наряд `0014` · журнал `worklog/0006` · `model/CharacteristicGroup.md` |
| 2026-09-02 | **Экран группы перестраивается вокруг чертежа** (находка В-8 прогона): чертёж занимает ширину окна минус ~15 % с сохранением пропорций, добавляется масштабирование. Основание: вся работа на этом экране — читать выноски чертежа и переносить с них числа; при прежней компоновке чертёж занимал малую долю окна и масштабирования не было | QMS-016 · наряд `0014` |

| 2026-09-02 | **Предельные отклонения — со знаком, термины по ISO 286** (решение пользователя, прогон QMS-016). `tol_plus`/`tol_minus` — это **верхнее и нижнее предельные отклонения**, каждое несёт собственный знак: у посадок с натягом оба уходят в плюс (`+0.05 / +0.02`). Прежний показ дорисовывал знаки в шаблон поверх `abs(value)` и выводил такую пару как `+0.05 / −0.02` — поле допуска зеркально, данные при этом верны (дефект Р-2 ревью наряда 0014, блокирующий). Знак берётся из значения; проверка знака отменена, вместо неё инвариант **верхнее ≥ нижнего**. Подписи экрана: `Upper deviation` · `Lower deviation`, составная ячейка — `Limit deviations` (не `Deviations`: `Deviation` в системе занято сущностью — записью об отклонении производства). Имена полей базы не меняются, миграции нет. **Назначение геометрии канона уточнено пользователем:** номинал и отклонения нужны не для расчётов, а чтобы при привязке оператор видел расхождение между чертежом детали и тем, что несёт группа, — то есть подтверждал принадлежность детали этой группе | QMS-016 · наряд `0015` · журнал `worklog/0006` Р-2 |

## Открытые развилки (на будущие сессии)

- ~~Нарезка `docs/model/` по сущностям — границы файлов (§6.3); Q-06~~ — **закрыто:**
  нарезка выполнена, Q-06 решён (см. строки 2026-08-10 выше).
- ~~**Стек / архитектура** — дизайн-сессия~~ — **закрыто (QMS-009, 2026-08-10):** стек
  утверждён и проверен на машине (см. строки выше, `docs/architecture.md`). Следующее —
  INFRA-013 (операционная модель Cowork↔Claude Code), затем QMS-010 (разработка).
- **Q-07** — `מנה` vs глоссарий: пользователь трактует `מנה` = `פק"ע` (WO); глоссарий
  `_system/terminology` определяет `מנה` как «отмеренное количество, не весь WO». Модель
  следует пользователю; правка глоссария — по команде «обнови систему».
- **Item↔CG «многие-ко-многим»** — не материализовано на S1 (сейчас 0..1 через
  `characteristic→mapping→g_position→cg`). Материализовать (15-я таблица + правка §5) при
  появлении **CG второго уровня** (QMS-011, ревью S1).
- **Инвариант «находка ∈ деталь отклонения»** — в SQLite схемой не выражен; enforced
  **доменной проверкой в S2** (критерий приёмки S2). Композитный FK/дублирование `item_id`
  отвергнуты (денормализация + правка §5). (QMS-011, ревью S1).
- **Чертежи в БД — порог роста.** Скан 3–4 МБ × ~25 групп ⇒ файл БД под 100 МБ; это в
  пределах нормы для копирования, но полная выгрузка тянет блобы. Если файл перевалит за
  **~200 МБ** — вынести чертежи в отдельную таблицу с ленивой загрузкой. Флаг на **S7**
  (упаковка/бэкап). (QMS-013, ревью S3.)
- **Замена чертежа не переносит баллоны.** Координаты сохраняются намеренно, но при другом
  кадрировании скана баллоны переставляются вручную — совместить два скана нечем. Известное
  поведение, не дефект. (QMS-013.)
- **Диалог привязки пишет по действию, не по «Сохранить».** Кнопка подтверждает полноту и
  закрывает окно (подписи — «Готово» / «Закрыть»). Транзакция на весь сеанс привязки
  отвергнута: цена — потеря уже введённого при случайном закрытии. (QMS-013, ревью S3.)
- **Флаги на ETL-наряд S6:** (а) если реальная запись без направления вовсе — `finding.direction`
  сделать nullable (миграция); (б) если нужно отличать сентинел `quantity=9999` (выборка) от
  реального числа — добавить флаг. (QMS-011, ревью S1).
- ~~Расхождения из `_CONCEPT.md` §D (State, WO, terminology, scope)~~ — **закрыто:**
  примирено в s06 (QMS-005), консолидировано в rev 1.00, нарезано в `model/`.

- **`decision_date` заполнена уже при регистрации.** Колонка NOT NULL с дефолтом `now()`
  (ратификация S1 №2 сделала nullable только `decision_dev`), поэтому у только что
  зарегистрированного отклонения дата решения не пуста при отсутствующем решении. На
  поведение не влияет — «решение не принято» читается из `decision_dev`, — но выгрузка
  вводит в заблуждение. Правка = миграция ⇒ флаг на **S7**. (QMS-014, ревью S4.)
- **Состояние канона запрашивается построчно (N+1).** `canon_state` открывает сессию на
  каждую находку. На форме отклонения незаметно, на списках прецедентов **S5** — уже нет:
  нужен пакетный запрос состояния по набору номеров размеров. (QMS-014, ревью S4.)
- **Кнопки исследований включены по «в таблице есть строки», а не «строка выбрана»** —
  косметическая непоследовательность с остальными разделами; чинит первый наряд, который
  тронет раздел «Отклонения» (**S5**). (QMS-014, ревью S4.)

---

## QMS-016 · чек-пойнт «прогон сборки руками» (2026-09-03)

> Ратификации цикла. Все — в ветке `run/qms-016`; в `main` не влиты, порядок слияния — **Q-11**.
> Полный ход — `worklog/0006-run-qms-016.md`, наряды `0018`–`0022`.

**1. Привязка к канону — часть заведения детали** (наряд `0018`). Деталь с назначенной группой
**не существует в базе с неполной привязкой**: каждая g-позиция получает локальный номер либо
код 99; отказ от привязки **отменяет заведение** (`domain.items.discard_item`, с гардом на
деталь, у которой уже есть отклонения или находки). Основание: в форме находки известен только
локальный номер, и при неполной привязке размер молча заводится как не-CG — отклонение ложится
мимо канона. Ратификация S3 «диалог привязки пишет по действию» **не отменяется**: требование
достигается откатом заведения, а не отложенной записью. `seed_cg_characteristics` удалена —
размеры создаёт сама привязка.

**2. Формат группы: чертёж — справка, а не канва** (наряды `0014`, `0015`, ратифицировано
02.09, здесь фиксируется в реестре). Баллоны сняты, координаты не хранятся; позиции и привязка
ведутся таблицами; номинал и предельные отклонения принадлежат **группе**; отклонения несут
собственный знак по ISO 286, инвариант «верхнее ≥ нижнего», ноль без знака.

**3. «Done» в привязке — проверяющая, а не заблокированная** (наряд `0020`). Кнопка доступна
всегда; нажатие закрывает открытый редактор ячейки, спрашивает полноту у **состояния** и
называет нерешённые позиции. Формулировка S3 «включается по полноте» заменена: смысл тот же,
механизм иной. Побочно снят тупик пустой группы.

**4. Выбор из длинного списка — фильтрующее поле в форме** (наряд `0019`). Не модальный пикер:
отклонений за смену много, и лишнее окно на каждом не окупается. Отбор живёт **до ухода
фокуса**; закрытие всплытия состояния поля не меняет. `PickerDialog` остаётся там, где выбор
идёт не внутри формы.

**5. Ширины таблиц** (наряд `0020` §7–§9). Объявляются **поимённо**; ширина не меньше
заголовка; ячейка однострочная, длинное режется с подсказкой; центрирование только когда
таблица занимает ≥60 % области. **Ширина принадлежит величине, а не колонке экрана** — одно
значение на двух экранах имеет одну ширину. Счётчики и короткие фиксированные значения — по
заголовку, без запаса. Единица — `horizontalAdvance("0")`; **разметка оператора старше расчёта**,
знакоместа — производная величина.

**6. Близнецы по регистру в справочниках** (наряд `0021`). Сведение — доменная операция
`merge_values` (выживает форма с заглавной, ссылки перевешиваются), не разовый скрипт: то же
понадобится при импорте `.xlsx` в S6. Источник закрыт: сверка дубля и сида — через `casefold`.
Основание: тип отклонения — ключ описательного поиска, поэтому расхождение регистра ломало
выдачу, а не только вид.

**7. Описательный прецедент не выводится автоматически — ревизия ратификации S5** (наряд
`0022`). Отменяется автоматическая выдача L2 по условию «зона ИЛИ тип» с рангом. Описательный
прецедент — **результат многопараметрического поиска, который задаёт инженер**: несколько
параметров сразу, под конкретный случай, **без сохранения набора**; сохранение фильтров и
повторный поиск — позже. Смысл уровня сохраняется: найти детали, которые группой характеристик
не связываются, но достаточно похожи. **L1 не затронут.** Открытые вопросы — **Q-16** (состав
набора, где показывается, когда вводится сохранение), сцеплен с **Q-14**.

**8. Чертежи остаются в базе** — подтверждение решения S3 замером: 276 КБ на одной группе с
чертежом, порог 200 МБ ≈ полторы тысячи групп, табличная часть пренебрежима. В S7 расплывчатый
порог заменить измеренным правилом «вес чертежа × число групп».

**9. Порядок приёмки** (репо-`CLAUDE.md` §9а). Проверка входит в систему там же, где в неё
входит человек — у клавиатуры и мыши, а не у метода. «Прогон руками» — человек за клавиатурой;
синтезированные события им не являются. Правка, которую оператор увидит руками, не принимается
ни по зелёным тестам, ни по чтению кода.

---

## QMS-017 · дизайн-сессия «ревизия чертежа в модели» (2026-09-06)

> Ратификации сессии. Ветка `run/qms-016`, наряд `docs/worklog/0024-item-revision.md`.
> Источник — **Q-15**, переформулированный PM-циклом ver.3 из «как не сломать
> перепривязку» в «ревизия чертежа в модели». Слияние в `main` (**Q-11**, перемоткой) —
> после этого наряда: он и есть та переделка канон-слоя, ради которой слияние ждали.

**1. Ревизия — свойство чертежа детали, и только его.** Конструкторский отдел
перевыпускает чертёж при любом изменении (конструкция · добавлен размер на непроверявшуюся
зону · сдвинут допуск или номинал), новый выпуск несёт следующее обозначение `A` → `B`.
**У группы характеристик ревизии нет:** группа — наш конструкт, вне этой базы такого
документа не существует; в базе хранится чертёж **общего узла**, а не чертёж конкретной
детали.

**2. Владелец размера — пара (деталь, ревизия).** Ключ `characteristic` становится
`(item, revision, local_number)` вместо `(item, local_number)`. Основание — решение
пользователя «чтобы получать чистые данные»: номер `12` после перевыпуска может означать
другой физический размер, и общая запись на две ревизии молча смешала бы их. Номер детали
остаётся **одной** записью — ревизия это дочерняя запись, а не двойник детали.

**3. Ревизия входит в схему в одной точке.** Под ревизию уходят: размеры, связь с группой,
строки кода 99 (`item_position_absent`). **Маппинг и находка собственной ревизии не
получают** — оба висят на размере и наследуют её через него. Отдельной колонки «ревизия» у
`mapping` и `finding` не появляется.

**4. Ввод ревизии.** Обозначение, а не номер (вводится как выпущено); порядок ревизий
хранится явно — система обязана уметь назвать «предыдущую»; ровно одна ревизия детали —
**действующая**. При **заведении детали** ставится вручную (редкое осознанное действие,
молчаливый дефолт опасен). При **регистрации отклонения** подставляется действующая,
перевод на предыдущую — отдельным ручным действием: детали прежнего выпуска приходят из
цеха ещё два-три месяца после смены.

**5. Клонирование — один механизм, два случая.** Новая ревизия детали и новое поколение
группы делаются одинаково: **скопировать прежнее целиком, дать править только дельту.**
Форма «Добавить ревизию» открывается уже заполненной прежними локальными номерами, связями
с группами и строками 99. Именно это выравнивает цену: ревизия, где сдвинулся один допуск,
и ревизия, где переехали все номера, стоят одного и того же действия. Статистика
пользователя: **95 % перевыпусков не двигают номера вовсе либо двигают один-два**;
типичное изменение — допуск или величина размера.

**6. Группа не версионируется и значения в ней не правятся на месте** — не отложено, а
решено. Когда у семейства поехал допуск или номинал, группа **клонируется**: та же
раскладка `g1…gN`, новые значения; привязки уходящих в новую ревизию деталей копируются
**по локальному номеру**. Правка значений на месте заставила бы старые отклонения читаться
против допуска, которого в их время не было.

**7. Добавление позиции — в группу, а не дроблением группы.** Если у одной детали появился
размер, которого нет у остальных, позиция добавляется **в существующую группу**, остальные
держат её под кодом 99. Дробить группу из-за одного размера — худший размен: вся ценность
группировки в том, что чем больше деталей в группе, тем больше прецедентов у каждой.
**Ответ по новой позиции (привязка или 99) требуется только от действующих ревизий**;
прошлые остаются **без ответа** — это честное состояние, позиции не было в каноне, когда
они писались, а штамповать 99 задним числом значит утверждать проверку, которой не было.
Для поиска разницы нет: деталь без ответа и деталь с 99 одинаково отсутствуют в join по
`(cg, g_index)`. Третье состояние заводить не требуется, миграция прошлого — тоже.

**8. Группа никогда не отключается.** Она остаётся источником, который человек поднимает
вручную: то, что не связывается автоматически, помнит инженер за экраном. **Пометка вместо
отключения:** группа показывается прежним поколением, когда **ни одна деталь не держит её
в своей действующей ревизии**. Состояние **вычисляемое, не хранимое** — ни флага, ни
таймера на два месяца, забыть переключить невозможно в обе стороны.

**9. Перепривязка переехавших номеров между ревизиями — не в Этапе 1.** Основание: событие
редкое (см. п. 5), а лобовое решение упирается в семейства: меняется обычно не деталь, а
семейство, и это до сотни `item`'ов разом — перепривязывать их все в момент изменения
неподъёмно. Плановая форма, когда понадобится, — **ленивая**: связывать размер новой
ревизии с размером предыдущей **в момент появления отклонения** по этой детали, то есть по
мере поступления, а не разом. Принятое следствие: прецедент, у которого номер переехал,
автоматически не находится — поднимается руками.

**10. Пометки в выдаче — двух уровней, совпадение через ревизию не отсеивается.**
Строка: «та же деталь, другая ревизия» — всегда, когда ревизия прецедента отличается от
текущей; без этого нельзя судить о сравнимости (сдвинутый допуск делает прежнее «одобрено»
выданным против других пределов). Размер: **знак `!` на не-канонном размере** — у него за
номером не стоит g-позиция, совпадение держится на одном номере. У канонного размера
поправка не нужна: он разрешается через маппинг **своей** ревизии и остаётся верным сам
собой.

**11. Смена ревизии в форме ввода ничего не пересчитывает.** Локальные номера введены с
протокола и остаются как введены; меняется ревизия, против которой они читаются. Неверная
ревизия делала находки неверными — исправление делает их верными, без перецепки и без
сброса. Опасность «молча меняет смысл записанного» относится к истории, а не к форме,
которую ещё заполняют. Номер, которого в выбранной ревизии нет, попадает под действующее
правило автосоздания не-канонного размера и показывается пометкой «нет в этой ревизии».

**12. Несколько групп характеристик на деталь — направление подтверждено, Этап 1 не
трогает.** Связь `Item↔CG` остаётся «многие-ко-многим» в схеме и `0..1` в интерфейсе;
теперь она держится **ревизией**, а не деталью. Материализация — отдельная работа за
пределами Этапа 1 (прежняя развилка выше по файлу остаётся в силе).

**Отложено этой сессией (осознанно, чтобы не задерживать боевое наполнение):**
клонирование группы как операция одной кнопкой (пока руками — событие редкое);
вычисляемая пометка «нет деталей в действующей ревизии» (данные позволяют посчитать в
любой момент, хранить нечего); ленивая перепривязка номеров (создаётся вперёд, задним
числом достраивать нечего); подсветка ревизии ~2 месяца после смены (оформление). Ни один
из четырёх пунктов не требует миграции задним числом — отсрочка не создаёт долга в данных.

**13. Гард канон-хеша — и на приёмке, а не только на входе** (довесок PM-цикла ver.4, к
предметке ревизии не относится). `tools/worktree_check.py` проверяет канон на шаге 0
входного контроля; движение канона **посреди** исполнения он не видит — именно так вышло с
нарядом `0023` (сдвиг `8404f4eb` → `04dac41d` пришёлся на середину работы, покраснел тест
регрессии Q-09, отчёт назвал невоспроизводимое значение). Тот же гард прогоняется на
**приёмке**, расхождение канон-хеша начала и конца наряда — **стоп-условие выходного
контроля**. Правка репо-`CLAUDE.md`. Вариант «дисциплина: Cowork не пишет в `docs/model/`
от выдачи наряда до приёмки» рассмотрен и отклонён как единственная мера — это ровно тот
отказ «дисциплина на человеке», из-за которого Q-09 переводили с ручного `rev` на хеш.

### QMS-017 · доводка после прогона руками (2026-09-06)

> Прогон руками по принятому наряду `0024` (`RULES` §9а: проверка входит в систему там же,
> где в неё входит человек). Наряд принят, приёмка не отзывается: заявленное было сделано и
> проверено. Один дефект и шесть недостающих требований — не брак исполнения, а
> недописанная спека и невыраженные требования. Исполняет наряд `0025`.

**14. Секции точного поиска делятся по «как совпало», а не «чья деталь».** Найдено прогоном:
у детали `C1-10375A` g-позиция `g13` в ревизии `A` привязана к номеру `19`, в ревизии `B` —
к `66`; отклонения по обеим существуют, а прецедент не показывается ни одной секцией. Первая
искала по номеру — номер переехал; вторая искала по канону, но **исключала свою деталь**
строкой `item_id != own item`. Исключение было верным до ревизий и снабжено честным
объяснением в коде («своя деталь уже показана в первой секции»): пока у детали один набор
размеров, «та же деталь + та же g-позиция» означало «тот же номер». Ревизия сломала
импликацию. **Решение:** секция канона возвращает **все** совпадения по g-позиции, включая
другие ревизии своей детали; исключается ровно то, что показала соседняя секция (тот же
размер той же ревизии). Механизм уже есть — параметр `exclude_characteristic`, сохранённый
при снятии описательного уровня (наряд `0022`). **Канон был неполон:** `Search.md` требовал
пометок на совпадениях через ревизию, но не сказал, что канонная секция обязана включать
свою деталь. Исправлено — `Search.md` v1.04.

**15. Знак `!` — одно значение на всех экранах:** «номер прочитан против другой ревизии, и
опереться, кроме номера, не на что». Прошлая ревизия + канонный размер → помечена только
строка (g-позиция найдёт его при любой нумерации); прошлая ревизия + не-канонный → строка
**и** `!`; текущая ревизия → ничего. Правило действует и в прецедентах, и в списке отклонений
детали. Основание: метка, меняющая смысл от экрана к экрану, — тот же отказ, что
переиспользованный `g_index` и колонка, адресуемая номером.

**16. У детали появляется своя карточка.** Двойной щелчок по строке `Items` сейчас открывает
**привязку позиций** — это неверное поведение: привязка не равна детали. Карточка: номер,
`Item type`, `Connection type`, `Size class`; **только чтение**, правка — кнопкой `Edit`
(симметрично карточке отклонения). Внутри — не таблица привязок, а **ссылка** на неё (то же,
что кнопка `Mapping…`). Вход: двойной щелчок и новая кнопка `Card` в таблице. Кнопка
`Mapping…` в таблице остаётся — прямой вход в самую частую операцию.

**17. Ревизия в карточке детали — просмотр, назначение отдельно.** Выпадающий список ревизий
**только переключает показ** и ничего не меняет в базе. Смена действующей ревизии — **отдельная
кнопка с подтверждающим окном**. Основание: показ не должен уметь менять состояние (тот же
довод, что развёл `groups_of`), а смена действующей ревизии — редкое действие с последствиями,
которое не должно случаться от промаха мышью.

**18. Из карточки — список всех отклонений детали.** По **всем ревизиям**; строки прежних
ревизий несут те же две пометки (решение 15). Фильтры (даты, размеры, дополнительные размеры)
**не сейчас** — они часть общей фильтровой машинерии (часть 2 Q-14, после S6). Сейчас нужен
сам список.

**19. Ревизия видна везде, где виден номер детали.** Колонка `Revision` в таблице
`Deviations`; ревизия в шапке карточки отклонения — **только чтение**, менять через `Edit`
(там она уже есть и работает). Отсутствие ревизии рядом с номером детали считается дефектом,
а не мелочью: ревизия стала частью идентичности того, что показано.

**20. Общие таблицы защищаются от правки мимо намерения: правка только через `Edit`,
никакого набора внутри строки.** Повод — цена случайности: двойной щелчок и нажатая клавиша
заменяют содержимое ячейки одним символом. **Область правила — таблицы, которые показывают**
(`Items`, `Deviations`, справочники), где строка есть запись. **Диалоги, существующие ради
ввода, не затрагиваются:** в привязке набор локального номера прямо в строке — ратифицированное
решение S3 (запись по действию, `Done` лишь подтверждает, что ни одна позиция не осталась
нерешённой), и транзакция «на весь сеанс привязки» тогда была отвергнута осознанно. Там строка
— поле ввода, а не чужая запись.

**Подтверждено прогоном как работающее** (не требует правок): прямой путь через ревизии,
обе пометки на не-канонном размере, клонирование ревизии с предзаполнением, миграция базы
прогона `rev02 → rev03`, выбор ревизии до находок в форме отклонения.

### QMS-017 · доводка наряда 0025 (2026-09-06)

> Наряд `0025` принят: §1 закрыт и виден на снимке, канон-хеш сдачи `17b4d548…` подтверждён
> независимым пересчётом Cowork, 543 теста зелёные. Ниже — три уточнения по итогам приёмки.
> Два из трёх — правка формулировок Cowork, а не работы исполнителя.

**21. Исключение соседней секции — набор, а не один размер (правка формулировки Cowork).**
Наряд `0025` §1 говорил «исключать тот же размер той же ревизии», хотя проза того же абзаца
и критерий про дубль требовали «то, что показала соседняя секция». Узкая формулировка
неверна: секция по номеру ищет **по всем ревизиям детали** и возвращает по размеру на
ревизию, поэтому при неподвижном номере — самый частый случай — исключение одного размера
оставило бы дубль. Исполнитель сделал по прозе и по критерию и закрыл это отдельным тестом.
`Search.md` v1.05 приведён к тому же. Разбор: требование, сформулированное уже собственной
прозы, — тот же класс, что недописанная канонная секция в решении 14.

**22. Пометки различаются свойством, а не функцией: цвет — «нет канона», полужирность —
«другой выпуск».** Найдено приёмкой: `mark_unbound` (красный + полужирный) применялся и к
не-канонному размеру в прецедентах, и к **ревизии** в списке отклонений детали, а в самих
прецедентах ревизия помечалась только полужирным. Итог — **один факт выглядел по-разному на
двух экранах** (пометка прежнего выпуска: полужирная в прецедентах, красная полужирная в
списке детали) и **одно оформление означало два факта**, причём в одной строке прецедента они
могли встретиться вместе и различались только наведением мыши. Побочно: комментарий у
`UNBOUND_TOOLTIP` обещал единственное значение на всех экранах, а единственный второй вызов
эту подсказку перекрывал своей. **Решение:** цвет закреплён за «нет канона», полужирность — за
«другой выпуск»; две именованные функции вместо одной переиспользованной; пометка ревизии
выглядит одинаково везде. Красное и полужирное вместе читаются как сумма — «прежний выпуск и
без канона». Нового визуального языка не вводится. Основание — то же правило, что и в решении
15: метка не меняет смысл от экрана к экрану; здесь оно распространено с **знака** на
**оформление**.

**23. `§7` наряда оказался проверкой, а не работой — и это штатный исход.** Редактируемость в
таблицах-списках уже была снята конструкцией (`kit.data_table` нередактируема по умолчанию),
а `read_only=False` просят ровно три диалога, все три — ради ввода, то есть выведены нарядом
из-под правила. Исполнитель ничего не менял и добавил два теста: на шесть экранов разом и
парный к нему — что привязка набор в строке сохранила. **Требование, оказавшееся уже
выполненным, закрывается тестом, а не правкой:** иначе «так сложилось» ничем не отличается от
«так гарантировано» и разъезжается при первой же правке соседнего экрана.

**Попутно:** вход в §3 наряда был назван неточно (двойной щелчок открывал диалог позиций, а не
привязку) — довод не менялся, исполнено по доводу. Дефект оформления снова нашёл снимок, а не
тест: у таблицы карточки не были объявлены ширины колонок, она села по содержимому. Четвёртый
случай класса; под offscreen шрифт моноширинный, тестом это не ловится в принципе
(`CLAUDE.md` §9а.8) — правило уже записано, дополнять нечего.

### QMS-017 · конвенции проверки, записанные по итогам цикла (2026-09-06)

> Четыре формулировки, каждая выучена на своей ошибке этого цикла. Три первые были
> согласованы ещё до доводки наряда `0025` и **едва не потерялись**: разговор ушёл в объём
> правок и к ним не вернулся. Записаны здесь, потому что находка, оставшаяся в чате, считается
> потерянной ([[RULES]] §6).

**24. Адресовать по имени, а не по позиции** (репо-`CLAUDE.md` §9а.9). Колонка — по заголовку,
строка — по ключевому полю. Общее правило, частным случаем которого это является: **тест,
разделяющий с кодом ту самую величину, которую проверяет, не сторожит ничего.** Номер колонки
именно такая величина: автор сдвигает её разом в коде и в тесте.

**25. Тест, охраняющий правило канона, называет это правило** — файл и формулировку в
докстринге (репо-`CLAUDE.md` §9а.10). Основание — формулировка исполнителя: **тест,
написанный вместе с кодом одним заходом, сторожит замысел исполнителя, а не требование
канона.** Расхождение между ними прогоном не ловится в принципе: тест зелёный ровно потому,
что повторяет то, что код делает. Прецедент — тест, требовавший красного цвета на колонке
ревизии, то есть закреплявший дефект; он и позволил дефекту дожить до приёмки. Автоматического
гарда нет; названное правило переводит сверку «тест ↔ канон» из акта озарения в обход списка.

**26. `show_error` в тестовом режиме бросает исключение вместо показа окна**
(репо-`CLAUDE.md` §9, ниже правила о перехвате). Отказ повторился трижды (наряды `0004`,
`0019`, `0024`) и каждый раз стоил трёх заходов диагностики вслепую: прогон вставал без строки
вывода. Правило перехвата существует с S4 и три раза не сработало — значит оно дисциплина, а
не механизм. **Работа для ближайшего наряда, тронувшего UI;** до тех пор действует правило
перехвата.

**27. Частичный прогон после массовой правки проверяет не работу, а выборку**
(`worklog/README.md`). `--ignore` и `-k` после массовой правки запрещены до первого полного
прогона. Родственно правилу «отрицательное утверждение требует полного счёта»: усечённая
выдача и усечённый прогон одинаково выглядят доказательством, будучи выборкой.
---

## QMS-018 · вывод исследования и ссылка на протокол (2026-09-07)

> Разбор вскрылся при проектировании раскрытия строки в списке отклонений: экран должен был
> показать вывод исследования, и стало видно, что показывать нечего — поле вывода бинарно,
> обязательно и по смыслу является решением, а не результатом изучения. Три правки ниже —
> недоработки исходного дизайна, а не новая функциональность. Решение пользователя: править
> сейчас, не откладывая, потому что цель цикла — **более-менее финализированный экран
> отклонений**, а экран поверх неверной модели пришлось бы переделывать.

| # | Решение | Основание |
|---|---|---|
| 1 | **`decisionInsp` — три позиции вместо двух:** `approval possible` · `approval not possible` · `inconclusive`. Хранимые значения `approval_possible` / `approval_not_possible` / `inconclusive` | Исследование само по себе полярного вывода не несёт. «Полезный зазор в сборе уменьшился на 20 %» — измерение, не приговор. Бинарное поле заставляло инженера выдумывать вердикт |
| 2 | **`decisionInsp` — необязательное.** Пусто = «ещё не разбирали»; `inconclusive` = «разобрали, однозначного ответа нет» | Порядок работы: сначала крепится файл протокола, позиция и вывод дописываются позже. Два состояния пустоты — разные, и раньше их различить было нечем |
| 3 | **Новое поле `Conclusion`** — короткий вывод свободным текстом, необязательный, ≤ 500 знаков (3–4 предложения). Показывается одной строкой с обрезкой, полностью — в подсказке | Чтобы не открывать файл протокола каждый раз ради одного факта. Структурировать («величина + единица») отклонено: реальный вывод вида «−20 % полезного зазора» в такую рамку не лезет |
| 4 | **`Protocol` — ссылка на файл, а не длинный текст.** Протоколы оформляются исключительно файлами; хранится полный путь, как ввёл оператор; существование файла при вводе не проверяется; файл в базу не копируется. Поле остаётся обязательным | Канон и раньше говорил «приложенный документ-резюме», а код хранил произвольный текст с проверкой «непусто» — разошлись. Вариант «настраиваемая папка протоколов + имя файла» рассмотрен и **отложен до S7** (упаковка в `.exe`), где всё равно решается, где приложение держит настройки. Проверка существования не вводится сознательно: протокол может лежать на недоступном в момент ввода ресурсе, и ложный отказ дороже устаревшей ссылки |
| 5 | **Полярный вердикт с уровня отклонения не поднимается никуда.** Строка отклонения не красится и не ранжируется по позиции исследования; на одной шкале они не подаются | Подтверждение прежней нормы канона (`Inspection.md`, «Decision independence»), которую бинарное поле фактически размывало |

**Что тронуто в каноне:** `docs/model/Inspection.md` (rev 1.00 → 1.01) · `docs/model/Deviation.md`
(rev 1.01 → 1.02, строка «`decisionInsp` stays binary»). `_overview.md` не правится — его
формулировка «verdict is independent and does not dictate the decision» остаётся верной.
`_CONCEPT.md` не правится — помечен архивным с 2026-08-10.

**Радиус поражения проверен листингом кода:** поле вывода нигде не участвует в поиске
прецедентов — только в доменном слое исследований, форме исследования и показе в карточке.
Это и делает правку мелкой.

**Исполнение:** наряд `docs/worklog/0027-inspection-outcome-and-protocol-link.md` (модель),
следом наряд `0028` (раскрытие строки в списке отклонений, QMS-018).

### QMS-018 · решения по виду экрана списка отклонений (2026-09-07)

Приняты до наряда, чтобы модель строилась под окончательный вид, а не наоборот.

| # | Решение | Основание |
|---|---|---|
| 6 | **Раскрытых записей может быть сколько угодно**, не одна за раз | Отклонения сравнивают между собой — какие находки в одном, какие в другом. Гармошка (одно раскрытие) такое сравнение делает невозможным; предложение Cowork отклонено пользователем |
| 7 | **Исследования читаются ленивым запросом — на раскрытие, а не на экран.** Пакетных запросов на экран два: отклонения и находки всех видимых строк (вместе с числом исследований у каждой находки) | Свёрнутой строке от исследования нужен один признак — «есть или нет». Бриф §4.4 требовал три пакетных запроса на экран; отступление объявлено: запрет, который бриф ставит на самом деле — «никаких запросов на каждую строку» — соблюдён полностью, а после импорта (S6) вариант «всё на экран» станет дорогим |
| 8 | **Колонка находки в раскрытии — `Research`, пять состояний:** `Not researched` · `Approval possible` / `Approval not possible` / `Inconclusive` (когда позиция проставлена у всех исследований находки и совпадает) · `Researched · N` (позиции разошлись **или** проставлены не у всех) | Ни одно состояние не является суждением экрана. Отдельная метка `Mixed` рассмотрена и **отклонена пользователем** как малополезная; `Researched · N` — нейтральный факт «открой и прочитай», а не флаг |
| 9 | **`Measurement point` не показывается в списке отклонений и остаётся в карточке** (поле в базе остаётся). Так же `Machine` и `NCR` | Снятие из списка было отложено нарядом `0011` §4 как «сцеплено с раскрытием»; сцепка развязана. *Уточнено 2026-09-07 при пересчёте сетки: первая формулировка «снимается с экрана» была неточной — канва показывает эти поля в карточке* |
| 10 | **Колонка `Research` — и в раскрытии, и в таблице находок карточки**, одной реализацией | Одно и то же вычисление в двух местах не разводится |
| 11 | **Дизайн-круг не открывается.** Наряд идёт по принятой канве (направление 1a); числа берутся из её таблицы сетки, спека пишется как-построено после приёмки (решение 17) | Раскрытие в канве нарисовано целиком, с числами и с крайними состояниями; наши изменения — вычитание и замена подписи, не новая композиция. Цикл дизайна стоит доставки файлом и приёма архива поверх живого дерева (`worklog/README.md`), и платить её за две подписи незачем. Дизайн зовётся только если пересчёт ширин перестанет сходиться к 1152 px при окне 1280 |
| 12 | **Кнопка «три точки» вместо пяти кнопок над таблицей — вне обоих нарядов**, отдельной задачей потока | Принята на приёмке дизайна (`REVIEW_deviations-screen.md` §3), но не попала ни в часть 1 Q-14, ни в часть 2 — потерялась между половинами. Втягивание её в наряд раскрытия размывает его границы |
| 13 | **Виджет раскрытия — не дерево.** Таблица сохраняется; под раскрытой строкой вставляется служебная строка на всю ширину, в неё кладётся панель находок со своей шапкой и своими колонками | У уровней разные наборы колонок (отклонение: номер/деталь/наряд/дата/количество/решение; находка: `Dim.`/`Canon`/`Sign · value`/`Zone`/`Deviation type`/`Research`/`Inspections`). Дерево заставляет все уровни жить в одной сетке, и собирается только ценой пустых ячеек. Служебная строка не выбирается и не ловится стрелками клавиатуры — единица действия остаётся отклонением |
| 14 | **Кнопка `Find identical` — не входит в наряд `0028`.** Её 160 px уходят в зону запаса уровня находки | Кнопка нарисована в строке находки и принята на приёмке дизайна, но выдаёт результат «на экране списка, той же фильтровой машиной» (решение Q-16). Фильтровой машины нет — это часть 2 Q-14, после S6. Кнопка, которой некуда выводить, — кнопка, которую придётся переделывать |
| 15 | **Короткий вывод исследования в ячейке `Inspections` не показывается — только в подсказке по наведению**; целиком читается в карточке отклонения. В самой ячейке — **тип · позиция** | Колонка 340 px ≈ 45 знаков; тип (`Implantation torque test` — 24) и позиция (`Approval not possible` — 21) занимают их почти целиком. Вторая строка под вывод удвоила бы высоту панели: шесть находок по два исследования дали бы стену, и сравнение двух отклонений — то, ради чего раскрытие сделано множественным, — стало бы невозможным. Требование пользователя «результат должен быть виден сразу» выполняется **позицией**, которая стоит в ячейке, и сводной колонкой `Research` |
| 16 | **Сетка обоих уровней сходится, дизайн-круг не открывается.** Уровень отклонения при 1280: `23 + 200 + 120 + 92 + 90 + 80 + 180 + 367 = 1152`. Уровень находки: `30 + 72 + 70 + 92 + 196 + 176 + 168 + 340 = 1144` при лимите 1152 | Канва несёт таблицу сетки (`grid`) с ширинами, поведением и порядком ухода при сжатии — это готовая спека, а не картинка. Ширину уровня находки при 1280 канва не считала — дыра закрыта здесь, снятием `Find identical` (решение 14) |
| 17 | **Спека экрана `docs/specs/deviations-list.md` пишется как-построено, после приёмки `0028`** | Конвенция `docs/specs/README.md`: «наряд проектирует UI, принятый результат записывается сюда». Прежнее намерение Cowork «сначала спека, потом наряд» противоречило домашнему порядку и отменено |
### QMS-018 · приёмка наряда `0027` (2026-09-07)

**Принят по девяти исполнительским критериям.** Критерий 10 — прогон руками — за пользователем
и на исполнителе не числится: синтезированные события прогоном руками не являются.

Проверено Cowork **по диску**, а не по отчёту: `docs/model/` даёт канон-хеш
`sha256:09336970…` — тот же, что вписан в шапку наряда, то есть канон за наряд не двигался;
`migrations/versions/rev04_inspection_outcome.py` существует; `models.py` несёт трёхзначный
`CHECK` с `IS NULL` и поле `conclusion`; `widgets.py` — `set_test_mode` / `in_test_mode` /
`UnexpectedErrorDialog`; `conftest.py` включает режим **на уровне модуля**; `tokens.py` —
`DIALOG_HEIGHT_CARD = 960`; снимки `09-dialog-inspection.png` и `11c-card-inspections.png`
показывают четыре варианта позиции и колонки `Type · Result · Conclusion`.

| # | Решение приёмки | Основание |
|---|---|---|
| 18 | **`UnexpectedErrorDialog` наследует `BaseException` — утверждено как конвенция**, а не как небрежность. Записано в репо-`CLAUDE.md` §9 вместе со снятием пометки «не реализовано» | `show_error` зовут изнутри широких `except Exception`; обычное исключение поглотил бы первый же обработчик, и требование «отказ обязан разрывать прогон с читаемым текстом» стало бы невыполнимым. Менять на `Exception` нельзя — это вернуло бы дефект, ради которого механизм и заводился |
| 19 | **Высоты диалогов переезжают в канон дизайн-системы** (`design-system.md` §3, revision 1.7): short 320 · medium 620 · tall 820 · **card 960**. Диалог берёт наименьшую из объявленных, которая вмещает содержимое без обрезки | Вопрос исполнителя поставлен верно: `tokens.py` — реализация §10 один-в-один, но **источник** числа — канон, и «спека не вводит высоту, которой здесь нет» (§0 дизайн-системы). До сих пор высоты диалогов жили только в коде — предсуществующий пробел, который вскрыла эта правка. Канве высоты диалогов не принадлежат: канва диалогов в этих высотах не рисует, она витрина |
| 20 | **Оба дефекта вёрстки нашёл снимок, а не тест** — форма исследования не помещалась в `DIALOG_HEIGHT_SHORT`, карточка схлопывала список прецедентов при четвёртой секции. Класс §9а.8 подтверждён в третий раз | Тест, утверждающий, что виджет существует, слеп к тому, что он обрезан. Оба числа сняты замером, оба защищены снимком на нативной платформе |
| 21 | **Откат миграции: строки с `inconclusive` и с пустой позицией удаляются с громким сообщением** — вариант исполнителя утверждён. Сведение к `not_approved` отвергнуто | Это ровно то выдумывание вердикта, против которого затеян QMS-018, и вердикт уходит в выходной документ. Прецедент — `rev02`, где неконвертируемые отметки «позиции нет» исчезали вместе с таблицей. Третий вариант — **отказывать в откате** при наличии таких строк — рассмотрен и отклонён: откат нужен именно тогда, когда что-то пошло не так, и откат, который отказывает, бесполезен. Наряд отката по существу не описывал — пробел наряда, закрыт здесь |
| 22 | **`String(16)` → `String(32)` у `decision_insp`** — находка исполнителя: `approval_not_possible` это 21 знак, в прежние 16 не влезало | Наряд длину не назвал. Пробел наряда, закрыт исполнением |
| 23 | **`main` пушится после приёмки.** Наряд push не заказывал, ветка общая между двумя машинами | Источник истины — remote (`_system/INFRASTRUCTURE` §1). Принятая работа, лежащая только локально на одной из двух машин, — ровно тот двухмашинный риск, из-за которого remote и объявлен истиной |
### QMS-018 · приёмка наряда `0028` и доводка (2026-09-07)

**Принят по десяти исполнительским критериям.** Проверено Cowork по диску: канон не двигался
(`docs/model/` даёт `sha256:09336970…`, ни один файл не тронут); `deviation_view.py` несёт
`EXPANDER`, `CHIPS_ROLE`, `setSpan` и колонку `Research`; снимки `21-deviations-expanded.png`
и `01-section-4b-deviations-1280.png` просмотрены глазами. `main` запушен (`45d3145`).

| # | Решение | Основание |
|---|---|---|
| 24 | **Обе колонки — `Findings` и `Explanation` — остаются при 1280.** Место берётся оптимизацией: пол ширины таблицы 1216, сокращение заголовка `Dev. qty` → `Qty`, снятие завышений замером | Порядок сжатия канвы убирал `Findings` с доводом «находки уже показаны строками» — но только у **раскрытой** записи. У свёрнутой при 1280 про находки не видно ничего, то есть требование §4.1 брифа (различить два отклонения по одной детали, не открывая карточку) при минимальном окне не выполнялось вовсе. Решение пользователя: обе важны, искать оптимизацию |
| 25 | **Правило ширины таблицы получает пол: «не шире 90 % окна и не уже 1216 px»** (`design-system.md` §3, revision 1.8) | Ревизия пустого места. При 1920 90 % отдают 192 px фона — это воздух вокруг рабочей поверхности. При минимальном окне те же проценты отдают 128 px, пока колонки голодают, а пришли на экран за колонками. Пол оставляет по 32 px поля и возвращает сетке 64 px |
| 26 | **Объявленная ширина колонки — не нарисованная; ширина действительна только после замера на нативной платформе** (`design-system.md` §3 и репо-`CLAUDE.md` §9а.12) | Qt держит свой минимум секции, лист стиля вычитает отступы, текстовая площадь выходит на 6–8 px уже. Колонка раскрытия молча выросла с 23 до 34; `WO` при идеально сошедшейся сумме резала `W26007336`. Оба прошли зелёный прогон — сумма объявленных чисел сходится, а значение на экране обрезано |
| 27 | **Проверка, краснеющая на верной работе из-за платформы, переезжает в снимочный инструмент** (`CLAUDE.md` §9а.13) | Под offscreen шрифт моноширинный: замер запаса под пилюлю требовал 197 px там, где на нативной нужно 116. Такой тест красный на **верной** сетке, то есть проверяет платформу. Это вторая половина §9а.8, а не послабление: проверку переносят туда, где свойство воспроизводится, а не ослабляют |
| 28 | **Пилюля находки — 22 px** (как `pill` в Controls); состав высот исправлен: 40 = минимум строки · 66 = `40 + (22 + 4)` · 82 = `66 + 16`, где 16 — **строка `+N findings`, а не третья пилюля** | Ошибка Cowork: «20 px» в наряде и §10 снято с разметки канвы, а не с её арифметики; прозой описанный состав («chip 22, gap 4, padding 7») не закрывался ни при 20, ни при 22. Третьей пилюли не бывает ни при каком числе находок. В коде уже 22 — правился канон |
| 29 | **`Dev. qty` остаётся выровненной вправо**, вопреки колонке «Выравнивание» канвы | `CLAUDE.md` §9: вправо — то, что глаз сравнивает по величине вниз по столбцу. Канва витрина, канон старше (`worklog/README.md`). Строка «по центру» в §1 наряда `0028` — ошибка переноса, недействительна |
| 30 | **Счётчики в `DeviationRow` остаются**, вопреки §5.1 наряда — выбор исполнителя утверждён | Явный запрет «прочие экраны» старше §5.1: поля читает `item_deviations_dialog.py`. Цель §5.1 достигнута — список их не читает, запросов они не добавляют (подзапросы внутри того же `list_deviations`). Снятие поедет с нарядом, который тронет тот экран; отдельной задачи нет |
| 31 | **`WO` 92 → 100.** При 1920 восемь пикселей берутся из зоны запаса; при 1280 ширина назначается замером в общем бюджете 1216 | Вариант (а) исполнителя. Порог подсказки не спасал: он считался той же завышенной формулой и молчал ровно там, где значение обрезано — то есть значение было недостижимо с экрана |
| 32 | **Формула ширины и порог подсказки в `kit` — отдельная задача QMS-022**, не доводка | Счёт по знакам × коэффициент заменяется замером `QFontMetrics`. Это поведение **всех** экранов: подсказка молчит именно тогда, когда значение обрезано, то есть данные в системе качества становятся недостижимы с экрана. Исполнитель верно вывел это за границы наряда |

### QMS-018 · приёмка доводки `0028` (2026-09-07) — цикл закрыт

**Принята по всем шести критериям.** Проверено Cowork глазами на снимках `01-section-4-deviations.png`
и `01-section-4b-deviations-1280.png`: при 1280 у **свёрнутой** записи видны пилюли находок,
`W26007336` читается целиком, обоснование стоит двумя строками без обрезки; при 1920 вернулась
`Revision`, зона запаса на месте. Обрезанных значений — ноль на обеих ширинах. Тесты 621 → 625,
`main` запушен (`5640e17`).

| # | Решение | Основание |
|---|---|---|
| 33 | **Замер старше таблицы Д-1 — два числа изменены исполнителем, и это правильно.** `Decision` 132 → **143** (пилюля «Not decided» 116 плюс три слоя оправы, а не один), `Findings` 260 → **303** (самый широкий реальный тип отклонения требует 282 + оправа 21). Разница 54 px взята из `Explanation` 306 → **258**, сумма 1216 сохранена | Доводка прямо предупреждала: числа — отправная точка, не норма. При `Findings` 260 колонка пилюль резала бы сами пилюли, то есть отменяла бы смысл пункта, ради которого доводка заведена. `Explanation` при 258 выше своего замеренного пола 94 и двухстрочной вмещает текст целиком |
| 34 | **27 px колонки никогда не печатаются** — 1 px линия сетки, 20 px отступов листа стиля, 6 px собственных полей Qt. Величина **считается, а не зашита**; форма любой ширины экранной сетки: **«нужно тексту + 27»** | Это механизм за решением 26 («объявленная ≠ нарисованная»): раньше был симптом «6–8 px», теперь — число и его состав. Внесено в `design-system.md` §3, revision 1.9 |
| 35 | **`kit.FIT_LABEL` недобирает 7 px на всех экранах** — считает «заголовок + отступы ячейки», не зная про линию сетки и поля Qt. Заголовок `Revision` резался, и этого никто не видел, пока не появился замер. **Дефект сворачивается в QMS-022**, отдельной задачи не заводим | Тот же корень, что у порога подсказки: счёт вместо замера. Чинить надо в одном месте, а не по экрану; правка по месту здесь сознательно не делалась — исполнитель назначил ширину `Revision` замером на экране и `kit` не тронул |
| 36 | **`Explanation` — двухстрочная ячейка; правило `kit` «ячейка однострочная» остаётся общим умолчанием, двухстрочность объявляется поколоночно** | Реальное обоснование (413 px) не помещается в одну строку **ни при какой** ширине из бюджета — однострочное правило означало бы резать главный текст прецедента, то есть критерий 1 доводки недостижим. При этом цена нулевая: две строки по 15 px помещаются внутрь строки 40 px, вертикальный ритм не тронут, и канва рисовала `Explanation` двухстрочной с самого начала. Внесено в `design-system.md` §7: необъявленный перенос — дефект, объявленный — свойство колонки |
| 37 | **Ширина колонки меряется по самому широкому значению, которое допускает домен, а не по самому широкому в сегодняшней базе.** `Qty` — 56, а не замерные 47, потому что `9999` (метка выборки, которую пишет импорт) — законное значение | Колонка, подогнанная под сегодняшние данные, обрежет их в день, когда придут настоящие. Тот же довод объясняет `Findings` 303: мерено по длиннейшему типу отклонения справочника, не по тому, что лежит в демо-базе |
| 38 | **1728 при 1920 — потолок, а не цель.** Сетка даёт 1592, разница 136 px и есть зона запаса, которая «исчезает первой». Прочтение исполнителя верно | Зона запаса объявлена гибким треком без содержимого (канва, таблица `grid`). При 1280 запаса нет вовсе — сумма ровно 1216, и это тоже верно: запас исчезает первым |

### QMS-018 · доводка 2 — раскрытие роняет приложение (2026-09-07)

Найдено прогоном руками, **после** объявленной приёмки. Наряд `0028` переведён обратно в
«не принят»; доводка 2 дописана в тот же файл наряда.

| # | Решение | Основание |
|---|---|---|
| 39 | **ОТМЕНА решения «цикл закрыт».** Наряд `0028` **не принят**: критерий 11 (прогон руками) провален — при попытке раскрыть отклонение приложение зависает и закрывается. Заведена доводка 2 | Приёмка была объявлена до прогона руками, и это ошибка Cowork: десять исполнительских критериев и оба замера остаются проверенными, но неработоспособный экран старше любого замера. Запись «Итог QMS-018 · цикл закрыт» выше читать вместе с этой строкой |
| 40 | **Дефект недостижим для прогона по построению — и это факт, а не гипотеза.** Все **двенадцать** мест раскрытия в `tests/test_ui_expansion.py` зовут `view.toggle_expansion(0)` напрямую; ни одно не доставляет клик по стрелке через приложение. Путь `cellClicked → _on_cell_clicked → toggle_expansion`, единственный, которым пользуется человек, тестами не покрыт вовсе | Правило существовало и старше наряда: репо-`CLAUDE.md` §9а.6 («события доставляются через приложение») и §9а («проверка входит в систему там же, где в неё входит человек — у клавиатуры и мыши, а не у метода»). Оно не было применено к обработчику раскрытия. Это тот же класс, что три круга по полю выбора детали в QMS-016, ради которых §9а и написан |
| 41 | **Главная гипотеза причины: модель перестраивается изнутри сигнала.** `cellClicked` → `toggle_expansion` → `reload()` → `setRowCount(0)` уничтожает элемент, по которому представление прямо сейчас диспетчеризует клик, вместе со всеми панелями-виджетами. Помечена **[HYPOTHESIS]** до воспроизведения | Совпадает с наблюдением «виснет и закрывается без питоновской ошибки»: обращение к освобождённой памяти убивает процесс без трассы. Две запасные — рекурсия раскладки (`reload` → `apply_grid` → `resizeEvent` → `apply_grid`) и накопление объединений ячеек (`setRowCount(0)` не снимает `setSpan`). Проверяются в порядке возрастания цены; причина устанавливается **раньше**, чем строится обход ([[RULES]] §16) |
| 42 | **Прогон руками окупился в шестой раз подряд** — и впервые нашёл не недоделку, а падение | Пять предыдущих циклов он находил дефекты, невидимые зелёному прогону; здесь — то же самое на порядок дороже. Довод в пользу того, что критерий «прогон руками» не формальность и приёмка до него объявляться не должна |

**Итог QMS-018.** Часть 1 `Q-14` закрыта: строка отклонения показывает находки пилюлями,
раскрывается, исследования стоят при своей находке. Ревизии брифа дизайна 1.1 и 1.3 доведены до
кода. Два наряда и две доводки — `0027`, `0028`; тесты 546 → **625**; схема `rev 0.4`. Попутно
закрыт **QMS-021** (механизм `show_error`) и заведён **QMS-022** (замер вместо счёта в `kit`:
порог подсказки и `FIT_LABEL`).

### QMS-018 · стоп-дефект закрыт, доводка 2 принята (2026-09-07)

Проверено Cowork по диску: `centring_margin` — чистая функция с проверяемой неподвижной точкой,
сторож несходимости бросает `RuntimeError`, четыре клика через `QTest.mouseClick` по
`viewport()` на показанном виджете, `tools/expansion_probe.py` на месте, `docs/model/` не тронут
(канон-хеш прежний). Тесты 625 → 631, `main` запушен (`210366f`).

| # | Решение | Основание |
|---|---|---|
| 43 | **Причина стоп-дефекта — самореференсный расчёт отступа в `kit`, а не перестройка модели изнутри сигнала.** `_centre_columns` задавал отступ полотна листом стиля, а Qt считает этот же отступ частью `frameWidth()`: выход функции был её входом, и **неподвижной точки не существовало вовсе** (0 → 11 → 0). Гард «значение не изменилось» такого не ловит — значение не повторяется, а чередуется | Счётчики на нативной платформе: `_centre_columns` 401 (обрыв) → 1; `apply_grid` 0 → 1; `resizeEvent` 0 → 0. Рекурсия через границу Python ↔ C++ съедала настоящий стек раньше, чем Python поднимал `RecursionError` — отсюда «висит секунду-две и закрывается» без трассы |
| 44 | **Гипотеза Ф-4 верна в спусковом крючке и неверна в двигателе, и это существенно.** «Цикл не начнётся, пока нет панели» подтвердилось; но `apply_grid` за всё падение не вызван ни разу — цикл `apply_grid ↔ resizeEvent` не разгонялся | **Все три средства, предложенные Cowork, целились в этот несуществующий цикл** и снизили бы частоту входа в несходящийся расчёт, оставив его несходящимся. Ровно тот отказ, от которого страхует [[RULES]] §16: гипотеза о причине проверяется раньше, чем строится обход, — она определяет цену решения. Здесь страховка сработала: требование «назвать причину до починки» стояло в доводке, и исполнитель его исполнил вместо того, чтобы принять предложенное средство |
| 45 | **Буквальный главный критерий приёмки был невыполним, и разложение исполнителя утверждено.** Падающий процесс красного теста не даёт — он уносит прогон. Доказательство разложено на три: неподвижная точка чистой функции (арифметика, любая платформа) · четыре настоящих клика (дыра §9а.6 закрыта) · процесс-зонд на нативной платформе (127 → 0) | Записано в репо-`CLAUDE.md` §9а.14 как общая норма. Ошибка Cowork в формулировке критерия: «краснеет на старом коде» молча предполагает, что старый код доживает до конца теста |
| 46 | **Правка в `kit` — вынужденная и утверждена задним числом**, вопреки «чего не трогать» доводки | На уровне экрана эту причину чинить нечем: она в общем компоненте. Ограничение наряда исходило из гипотезы, которая оказалась неверной; исполнитель назвал отступление явно, а не сделал молча. `QMS-022` не тронут — это другая правка того же файла |
| 47 | **Второй настоящий случай образца «обработчик сигнала перестраивает свой виджет» — `mapping_dialog.py:114`** (`itemChanged → reload`, прикрыт частичным флагом `_loading`). Не тронут; заводится задачей **QMS-023** | Причина стоп-дефекта лежала в `kit` и закрыта для всех экранов сразу, но сам образец остаётся отдельным риском. Прочие совпадения по коду — сигналы кнопок либо перестройка другого виджета, ложные |
| 48 | **Процедурная ошибка Cowork: мостом сверх объявленного состава приехал третий путь** — `docs/design/design-system.md`. Объявлено было два | Ровно то необъявленное состояние, которое `worklog/README.md` («Доставка наряда — на чистом doc-слое») запрещает, и запрет этот написан по прецеденту наряда `0026`. Цена легла на внимательность исполнителя. Впредь состав пересчитывается листингом непосредственно перед выдачей релея, а не по памяти о том, что писалось в этот заход |

**Состояние QMS-018.** Наряд `0028` принят по шести исполнительским критериям доводки 2.
**Критерий 7 — прогон руками — за пользователем и не закрыт.** Задача закрывается только после
него: приёмка, объявленная до прогона руками, уже один раз оказалась преждевременной (решение 39).

### QMS-024 · исследование без протокола (2026-09-07)

Найдено **прогоном руками QMS-018**, уже после закрытия критерия 7: техника работает, а модель
не пускает верный рабочий случай.

| # | Решение | Основание |
|---|---|---|
| 49 | **Критерий существования записи исследования меняется с «есть файл» на «есть переиспользуемый вывод».** `Inspection.md` rev 1.01 → **1.02** | Часть отклонений решается одним чертежом: наружный 10.0 против внутреннего 9.9 — детали не сопрягаются, и никакое исследование этого не изменит. Вывод переиспользуем и стоит записи, а документа не существует. Прежний канон исключал `Tolerances review` из исследований целиком — исключение оказалось шире действительности |
| 50 | **Галочка `No protocol` — объявленное состояние, снята по умолчанию.** Отмечена → протокол не требуется и обязан быть пуст, **короткий вывод становится обязательным** | Состояние не выводится из пустого поля: пустой протокол — незаконченная запись, отмеченная галочка — решение инженера. «Файл есть, но не нужен» как состояние не заводится: третий случай пришлось бы объяснять на каждом экране |
| 51 | **Инвариант «файл ИЛИ вывод» держит ограничение схемы, а не форма** | Запись без того и другого невидима для поиска прецедентов, то есть бесполезна ровно в том, ради чего таблица заведена. Форму обходят импортом, скриптом и следующим нарядом; ограничение схемы обойти нельзя. Тот же довод, что перевёл Q-09 с ручного `rev` на хеш: механизм вместо дисциплины |
| 52 | **Рутинная проверка чертежа записи по-прежнему не обязывает.** rev 1.02 делает такую запись *возможной*, а не *обязательной* | Различение по глубине остаётся: таблица заведена под серьёзные документированные исследования, а запись без протокола — узкое исключение для вердикта, которому документ не нужен, не разрешение протоколировать каждый взгляд на чертёж |
| 53 | **Где живёт решение — на отклонении или на находке — остаётся открытым вопросом, не задачей** | Решение пользователя: «не решил, узнаем в процессе использования». Сейчас решение принимается по отклонению, обоснование накапливается на исследованиях; второй возможный вид — решение на находке, исследование несёт только объяснение. Проектировать под невыясненное употребление — то же, что проектировать фильтры под воображаемую выдачу (довод порядка работ Q-14) |

**Критерий 7 наряда `0028` пройден 2026-09-07** — прогон руками подтвердил работу раскрытия.
**QMS-018 закрыт.** Замечания пользователя по внешнему виду отложены им же до отработки
механизмов — идут в UX-доводку в конце, отдельной задачи не заводят.

**Исполнение:** наряд `docs/worklog/0029-inspection-without-protocol.md`.
### QMS-024 · приёмка наряда `0029` и доводка (2026-09-07)

Принят по восьми исполнительским критериям. Главный тест — на уровне базы: запись без файла и
без вывода отвергается вставкой прямым SQL, мимо домена, вместе с пятью запрещёнными видами
записи и обратной стороной. Тесты 631 → 657, `main` запушен (`fd3dcc0`).

| # | Решение | Основание |
|---|---|---|
| 54 | **`Tolerances review` въезжает в стартовый справочник.** `reference-data.md` rev 1.00 → **1.01**, стартовый набор назван поимённо: `Solidworks assembly`, `Implantation torque test`, `Tolerances review` | Канон rev 1.02 приводит этот тип примером законного случая, а стартовый набор его не нёс: первое такое отклонение в новой базе упиралось бы в ручной ввод значения справочника, и критерий приёмки зависел бы от состояния базы, а не от кода. Вопрос исполнителя поставлен верно |
| 55 | **Каждый вид контрола, впервые появляющийся в приложении, — правка темы, а не экрана, и подписывается снимком** (`design-system.md` §4, revision 1.10) | `QCheckBox::indicator` в теме описан не был, Qt рисовал платформенный по умолчанию — против этой палитры «ничего»: флажок выглядел подписью, кликать было не по чему. Дефект жил с наряда `0011` и ждал первого флажка в приложении. Ни один тест не утверждает «виджет виден» в том смысле, в каком это понимает человек — это снимок (§9а.8) |
| 56 | **Звёздочка обязательного поля ратифицирована, и именно живая:** `*` отражает обязательность **в текущем состоянии формы**, появляется и исчезает вместе с условием | Статическая пометка на всех обязательных полях поставила бы звёздочку почти везде и перестала бы читаться. Живая отвечает на единственный вопрос оператора в этот момент — что форма хочет, прежде чем сохранит. Приём был новым, договорённости не было; внесён в канон |
| 57 | **Пометка `No protocol` в карточке — подсказкой, не колонкой** | Тот же довод, которым снята колонка `Inspections`: отдельная колонка ради признака у одной записи из десяти — счётчик там, где показано содержимое |
| 58 | **Объявление состава приехавшего становится двусторонним** (`worklog/README.md`). Cowork перечисляет **свои** правки и называет это перечнем, а не гарантией; **исполнитель на шаге 0 сверяет фактический состав своим `git status` и дописывает его в шапку наряда** — эта запись и есть источник истины. Расхождение перечня с фактом — не стоп, если лишнее вне `docs/model/` и хеш сошёлся | Расхождение случилось **дважды подряд** (доводка 2 наряда `0028` — `design-system.md`; наряд `0029` — `CLAUDE.md`), и корень не в невнимательности: **Cowork не видит рабочего дерева**, shell на машине у неё нет, `git status` недоступен. Она может перечислить только то, что писала сама, а незакоммиченным лежит и остаток прошлых заходов. Формулировка «состав снят листингом перед выдачей» была обещанием, невыполнимым по построению. Ответственность перенесена туда, где есть инструмент её исполнить — та же логика «механизм вместо дисциплины» |

**Критерий 8 — прогон руками — за пользователем и не закрыт.** QMS-024 закрывается после него.

### QMS-024 · приёмка доводки `0029` (2026-09-07) — закрыт

Принята. `Tolerances review` в стартовом наборе, снимок `01-section-1c` показывает три
значения, тесты 657 → 659, `main` запушен (`dd2337b`). Фактический состав шага 0 снят
исполнителем своим `git status` и вписан в шапку — новая двусторонняя конвенция (решение 58)
отработала с первого раза.

| # | Решение | Основание |
|---|---|---|
| 59 | **Тест, сверяющий данные с той же константой, из которой они берутся, проверяет копирование, а не требование** (репо-`CLAUDE.md` §9а.16). Стартовый набор справочника перечисляется в тесте **дословно** | Находка исполнителя, и она сильнее самого пункта доводки: `test_seed_fills_the_starting_sets` сверял справочник с `REFERENCE_SEED` и остался бы зелёным при исчезновении `Tolerances review`. Настоящей дырой был не пропущенный элемент сида, а тест, не способный его заметить. Родственно §9а.10 и §9а.9 — три случая одной болезни: проверка повторяет реализацию вместо требования |
| 60 | **Адресация по позиции в инструменте снимков — тот же дефект, что в тестах.** Демо-база брала вид исследования как `list_values(...)[-1]`, и третий тип молча переназначил вид второму исследованию | §9а.9 действует и вне тестов. Поймано снимком: на раскрытии вместо `Implantation torque test` встал `Tolerances review` с приложенным файлом — наоборот смыслу типа |
| 61 | **Демо-база обязана содержать состояния, которые вводит наряд** | Без записи «без протокола» снимки показывали экран, снятый только на документированных записях, то есть не показывали сделанного. Заодно появились `+1 inspection` и `Researched · 3`, которых прежняя демо-база не давала |

---

### QMS-025 · решение переезжает на находку (2026-09-07)

Найдено работой на живых данных, в тот же день, что и QMS-024. **Закрывает открытый вопрос
«где живёт решение»** — употреблением, как и планировалось.

| # | Решение | Основание |
|---|---|---|
| 62 | **У находки появляется собственный исход:** `permitted` · `not permitted` · пусто (не решено). `Finding.md` rev 1.00 → **1.01**, строка «находка решения не несёт» снята | Отклонение с одной находкой читается со списка однозначно; с двумя и более — нет: экран говорит, что партия отклонена, и не говорит, **какой размер её отклонил**. Инженер идёт открывать записи, то есть делает работу, ради устранения которой база заведена |
| 63 | **Позиция исследования снимается вовсе.** `Inspection.md` rev 1.02 → **1.03**: у исследования остаются тип, вывод текстом и протокол | Три позиции были обходным решением отсутствующего места: у находки не было своего состояния, и «быстрый ответ» жить было негде, кроме как на исследовании. Как только исход появился у находки, поле стало **вторым вердиктом рядом с настоящим** — два почти одинаковых слова с разным смыслом бок о бок на экране. Вывод пользователя, и он точнее нашего: лишней была не колонка, а сущность в модели |
| 64 | **Правило «исследование не диктует решение» становится структурой.** У исследования нет поля решения — нарушить нечем | Третий раз за сессию один и тот же приём: механизм вместо дисциплины. Прежде правило охранялось словами в каноне и запретом красить строку |
| 65 | **Два решения, не одно.** Исход находки — прошёл ли **размер** (геометрия); `decisionDev` — что делать с **партией** (одобрить как есть · брак · сортировка · ремонт). Первое — вход для второго, не замена | Отклонение с двумя находками, одна прошла, вторая нет, уходит в **сортировку** — сортируют по признаку второй. Из «размер не прошёл» не следует «партия в брак», но без первого второе необъяснимо |
| 66 | **Связывающий инвариант:** отклонение может нести `approved — use as is` **только** когда у всех находок исход `permitted`. Проверяется **при сохранении решения по отклонению**; любое иное состояние находок, **включая пустое**, закрывает этот исход. Обратная сторона: находку нельзя перевести в `not permitted`, пока её отклонение стоит `approved` — отказ с объяснением | Формулировка пользователя, и она чище исходной: проверка живёт там, где принимается решение, а не размазана по форме находки. Форма находки свободна — «не решено» нормальное состояние свежерегистрированного отклонения. Молча снимать чужое решение нельзя: оно ушло в документ |
| 67 | **Инвариант держит домен в двух точках входа, не схема** — и это объявлено, а не умолчано | Инвариант межтабличный: зависит от набора дочерних строк, `CHECK` такого не выражает. Триггер выражает, но в SQLite теряется молча при пересборке таблицы миграцией — ожог `rev04`. Надёжность даёт тест, заходящий **мимо формы**, прямо в домен, на обе точки. Отличие от решения 51: тот инвариант был внутристрочный, и схема его выражала естественно |
| 68 | **Пилюля находки несёт две иконки:** наличие исследования и **исход находки**. Исход различается **формой** — галочка · крестик · пустой кружок, — а не цветом; слово в подсказке; пустой кружок показывается всегда | Требование пользователя. Форма вместо цвета — потому что `design-system.md` §1 требует, чтобы цвет не нёс смысл в одиночку, а места на слово в пилюле нет и добавлять его нельзя: оно сломает скан, ради которого всё делается. Пустое место неотличимо от «иконка не поместилась» |
| 69 | **Колонка `Research` замещается исходом находки**, те же 168 px — **сетка панели не трогается** | `Research` сводила то, что говорят исследования; после снятия позиции сводить нечего. Приятное следствие: вопрос ширины, заложенный было в наряд, исчез целиком — колонка не добавляется, а меняет владельца |
| 70 | **Несколько исследований на одну находку остаются возможны и неранжируемы** | Пользователь: практически всегда одна проверка на находку, но исключить нельзя — проблема с зоной может потребовать проверки герметичности **плюс** функциональной. Каждое излагает своё своими словами; ранжировать их нечем и не нужно — решает инженер, на находке |
| 71 | **Раскрытие панели прецедентов теми же тремя уровнями — принято, но отдельным нарядом `0031`** | У неё своя сетка, свои запросы и своя приёмка. Смешанная приёмка уже подводила: дефект в одной половине блокирует просмотр другой |

**Исполнение:** наряд `docs/worklog/0030-finding-outcome.md`; следом `0031` (прецеденты).

### QMS-025 · приёмка наряда `0030` (2026-09-07)

Принят по восьми исполнительским критериям. Проверено Cowork по диску: `rev06_finding_outcome.py`
существует, `finding.outcome` с `CHECK` в схеме, `decision_insp` вычищен полностью (ноль
вхождений), `docs/model/` не тронут — канон за наряд не двигался. Тесты 659 → 662,
`main` запушен (`ca5fd10`).

| # | Решение | Основание |
|---|---|---|
| 72 | **Тест миграции проверяет саму миграцию, а не сегодняшнее состояние базы** (репо-`CLAUDE.md` §9а.17). Каждый поднимает базу **до своей ревизии** | `rev06` уронила тесты про `rev04` и `rev05`, хотя обе отработали ровно как обещали. Тест, целящийся в `head`, краснеет от появления следующей миграции, ничего про себя не сообщая. Находка исполнителя, вынесенная им в общее правило — верно |
| 73 | **Единственный путь записи в базу — доменный слой; импорт не исключение** (репо-`CLAUDE.md` §9а.18). ETL складывает разобранные строки **через те же доменные функции, что и форма**, и это проверяется тестом | Прямое следствие решения 67: инвариант, живущий в домене, слабее схемного ровно на дыру «запись мимо домена». Наблюдение исполнителя (отклонение 4) точное — вопрос «где живёт решение» закрыт наполовину. Закрывается не триггером (отвергнут как хрупкий в SQLite), а дисциплиной **одного входа**, которую сторожит тест. Станет критерием приёмки спринта S6 |
| 74 | **Ширины таблицы находок в карточке переведены со знакомест на замер** — восьмая колонка вынесла таблицу за край диалога: объявлено 1051 при нужных 854 | §9а.12 в действии, второй экран подряд. Знакоместо под offscreen моноширинное и такого не ловит — ловит только замер на нативной платформе |
| 75 | **Адресация по имени вместо позиции — четвёртый случай класса за два наряда** (§9а.9). Демо-база снимков адресует находки и типы по имени | Четыре повторения одного класса — сигнал, что правило работает, но применяется задним числом. При следующем касании инструмента снимков рассмотреть механическую защиту вместо дисциплины |
| 76 | **Состав прироста тестов снят пофайловым сбором на входном состоянии через временный worktree, а не назван на глаз** | 659 → 662 при 18 новых — убыль объясняется целиком снятием позиции исследования: у тестов не стало предмета. Число, названное на глаз, скрыло бы это; сверка показала, что убыль одного рода |

**Критерий 9 — прогон руками — за пользователем.** QMS-025 закрывается после него; следом
наряд `0031` (раскрытие панели прецедентов).
