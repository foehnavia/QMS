# MIS-QMS — реестр архитектурных решений

> Пополняется по ходу дизайн-сессий. Ратифицированные развязки — из
> `50_MIS-QMS/PROPOSAL_MIS-QMS_structure` (волт) и последующих сессий.

| Дата | Решение | Детали / источник |
|---|---|---|
| 2026-07-22 | Канон модели — Session-03-latest; сессии 01–02 — история | сессии 01–03 |
| 2026-08-05 | Оформление — **Repo-central**: домен-линия `50_MIS-QMS` (PM-слой) + внешний репо (`docs/`+`src/`); данные автономны; канон — логическими блоками + индекс | PROPOSAL §3 |
| 2026-08-05 | Отдельная линия (домен), не подпроект; код — внешний приватный git-репо; данные изолированы (в git/волт никогда); хранение — блоками + оглавления | PROPOSAL §3 |
| 2026-08-06 | **Путь репо — `C:\MIS-QMS`** (рабочая машина), не `D:` — уточнение к ратификации | сессия QMS-001 |
| 2026-08-06 | **§6.4 — судьба Session-01/02/03:** архив в репо `docs/model/_history/`, доступ к знаниям на концепт-сессиях обеспечен | сессия QMS-001 |
| 2026-08-06 | **§6.3 (нарезка по сущностям) — отложена (Волна 0b):** сперва собрать полный предварительный концепт (сессии 04+) и закрыть открытые вопросы (State, WO, поток Deviation), затем резать, понимая полную картину. Волна 0a — репо + история + консолидированный концепт | сессия QMS-001 |
| 2026-08-06 | **Приватный GitHub-remote заведён — `github.com/foehnavia/QMS`.** Источник истины = remote; локальные клоны `C:\MIS-QMS` (раб.) / `D:\MIS-QMS` (дом.); работа с 2 машин. Личный проект, sensitive-данных нет | сессия QMS-001 |
| 2026-08-10 | **Итоговый концепт собран — `CONCEPT_full_rev1.00_EN` (канон) + `_RU` (перевод).** Заменяет `_CONCEPT.md` (→ архив; при конфликте приоритет у full). Размещён в репо `docs/model/` и в волте `50_MIS-QMS/` (зеркала) | сессия QMS-008 · `CONCEPT_full` §12 |
| 2026-08-10 | **R1 — state-linking атрибут возвращён:** спящий `state-depending dimension` (по умолчанию `none`, хранит номер связанного размера). Разворот отклонения хеджа `feature`/`stage` в s06. State по-прежнему не сущность | QMS-008 · `model/Characteristic.md` |
| 2026-08-10 | **R2 — маппинг ранний, до регистрации** отклонения (кнопки «Создать маппинг/привязать» в форме ввода). Ретроактивный маппинг — риск потери данных, только для срочного WO. Разворот «ленивого/ретроактивного резолва» s06 | QMS-008 · `model/CharacteristicGroup.md` |
| 2026-08-10 | **R3 — CG можно создавать по ходу** при заведении детали (не только отдельный админ-процесс) | QMS-008 · `model/reference/reference-data.md` |
| 2026-08-10 | **Уточнения rev1.00:** карточка отклонения — главный deliverable (автообзор прецедентов); исследование ↔ пара (Item, размер); поиск по уровням; терминология Инспекция → Исследование; расширены зона/тип | QMS-008 · `model/DeviationCard.md`/`Inspection.md`/`Search.md` |
| 2026-08-10 | **Нарезка канона выполнена (QMS-002 / Волна 0b).** `CONCEPT_full` разрезан на `docs/model/`: `_overview.md` (канон-вход: нарратив + процесс) + файл на сущность (Item, Characteristic, CharacteristicGroup+Mapping, Deviation, Finding, Inspection, DeviationCard, Import-Workflow, Search) + `reference/` (reference-data, output-document) + `specs/` (стабы UI). Прежние `CONCEPT_full_*` в репо → короткие баннеры «superseded». Причина: старт дизайн/сборки — детализация должна падать в правильные файлы | QMS-002 · этот файл |
| 2026-08-10 | **Единственный источник + зеркало.** Канон = нарезка `docs/model/`, правится только в репо-сессиях. Волт держит **одно** плоское read-only зеркало `50_MIS-QMS/CONCEPT_full_rev1.00_EN.md` (генератор `tools/build_mirror.py`, `source_version` = rev канона) для offline-сессий (телефон без репо). RU-зеркало — заморожено на rev1.00, по требованию. Регенерация — при закрытии сессии, изменившей `model/`; guard при открытии сверяет `source_version` (Слой 0). Сессия без репо канон не правит | QMS-002 · `docs/_INDEX.md` (Зеркало и синхронизация) · `50_MIS-QMS/code-registry` |
| 2026-08-10 | **Q-06 закрыт (развилки нарезки):** (1) Маппинг — **внутри `CharacteristicGroup.md`** (один абзац, намертво связан с g-позицией); (2) формы/UI — в **`specs/`**, не inline; (3) §12/архитектурные вопросы — в репо **`decisions.md`** (этот файл), волт `OPEN_QUESTIONS` — только PM/кросс-проектное | QMS-002 |
| 2026-08-10 | **Стек утверждён (QMS-009) и проверен на машине.** SQLite (источник истины) · SQLAlchemy 2.x + Alembic · openpyxl + regex ETL (без LLM) · **PySide6** UI (нативный RTL) · **docxtpl** → PDF через **Word/Office COM** · упаковка PyInstaller (onefile `.exe`). 5 фаз верификации зелёные на Store-Python 3.13.14 x64 (COM и PyInstaller пригодны). Полное описание — `docs/architecture.md` | QMS-009 · `architecture.md` · [[2026-08-10 meeting MIS-QMS architecture and stack]] |
| 2026-08-10 | **Источник = иврит-Excel** (`openpyxl`), Access/ODBC исключён; приложение — **источник истины** (read+write), не read-only вьюер | QMS-009 · `architecture.md` |
| 2026-08-10 | **RTL-документы:** docx генерятся `docxtpl` поверх реального RTL-бланка DS-QC.2-2 + явный `bidi`/right-align на ивритских абзацах | QMS-009 |
| 2026-08-10 | **Операционная модель Cowork ↔ Claude Code** согласована (репо — мост; Claude Code — руки+git+локальный диагност; read-only доступ к волту; наряды `docs/worklog/`). Формализация — INFRA-013, **гейт перед QMS-010** | QMS-009 · [[2026-08-10 meeting MIS-QMS architecture and stack]] |
| 2026-08-10 | **Уточнение политики данных: отдельные значения не sensitive.** Номер детали `מק"ט`, номер WO `מנה`/`פק"ע`, отдельное заключение на иврите сами по себе **не** sensitive — вне собранного документа ценности не несут, штатно попадают в БД и сохраняются, использовать можно где угодно (тесты/фикстуры/примеры). В git и волт не кладётся только **целый реальный документ/протокол с привязкой к компании** (собранный `אישור חריגה`, протокол целиком). Автономность БД — архитектурная изоляция рабочих данных, не вопрос конфиденциальности. Синхронизировано: `CLAUDE.md` §6, `README`, `_INDEX`, `architecture` §1, `model/Import-Workflow`; волт `_BRIEF_MIS-QMS`/`code-registry`. Зеркало `CONCEPT_full` — регенерировать. `RULES` §5 (Cus_Com обезличивание) не затронут | решение пользователя (INFRA-013) |

## Открытые развилки (на будущие сессии)

- ~~Нарезка `docs/model/` по сущностям — границы файлов (§6.3); Q-06~~ — **закрыто:**
  нарезка выполнена, Q-06 решён (см. строки 2026-08-10 выше).
- ~~**Стек / архитектура** — дизайн-сессия~~ — **закрыто (QMS-009, 2026-08-10):** стек
  утверждён и проверен на машине (см. строки выше, `docs/architecture.md`). Следующее —
  INFRA-013 (операционная модель Cowork↔Claude Code), затем QMS-010 (разработка).
- **Q-07** — `מנה` vs глоссарий: пользователь трактует `מנה` = `פק"ע` (WO); глоссарий
  `_system/terminology` определяет `מנה` как «отмеренное количество, не весь WO». Модель
  следует пользователю; правка глоссария — по команде «обнови систему».
- ~~Расхождения из `_CONCEPT.md` §D (State, WO, terminology, scope)~~ — **закрыто:**
  примирено в s06 (QMS-005), консолидировано в rev 1.00, нарезано в `model/`.
