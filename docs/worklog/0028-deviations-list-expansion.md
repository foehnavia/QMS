# Наряд 0028 — список отклонений: находки пилюлями, раскрытие строки, панель находок

> [!done] **Релей получен — наряд выдан 2026-09-07.** Наряд `0027` принят Cowork по девяти
> исполнительским критериям; блок ожидания ниже исполнен и оставлен как история.
>
> [!danger] **Не начинать до релея.** Этот наряд опирается на поля, которые заводит наряд
> `0027` (три позиции вывода исследования, короткий вывод, ссылка на протокол). Он записан в
> репо заранее, чтобы проектирование не ждало сборки, но **выдан не считается**: релей выдаёт
> Cowork после приёмки `0027`. Пришёл сюда без релея — вернуться к `0027`.

- **Задача / цель:** `QMS-018`, часть 1 закрытого `Q-14`. Строка отклонения перестаёт быть
  счётчиком и показывает сами находки; строка раскрывается; исследования стоят при своей
  находке. Закрывает ревизии брифа дизайна 1.1 и 1.3, которые в код не дошли.

- **Вход:**
  - `docs/worklog/0027-inspection-outcome-and-protocol-link.md` — **принят**, отчёт заполнен
  - `docs/design/canvas/MIS-QMS Deviations List.dc.html` — эталонная канва, направление **1a**;
    в ней лежит таблица сетки колонок (`grid`) с ширинами, поведением, выравниванием и порядком
    ухода при сжатии — она и есть источник чисел ниже
  - `docs/design/design-system.md` — revision 1.6 (высоты раскрытия, `finding_chip`, `expansion_panel`)
  - `docs/design/REVIEW_deviations-screen.md` — приёмка дизайна: что принято, что принято с оговоркой
  - `docs/decisions.md` — раздел «QMS-018 · решения по виду экрана списка отклонений (2026-09-07)»
  - `docs/model/Deviation.md`, `docs/model/Finding.md`, `docs/model/Inspection.md` — канон
  - `src/ui/deviation_view.py`, `src/ui/kit/`, `src/domain/deviations.py`,
    `src/domain/findings.py`, `src/domain/inspections.py`

- **Сопутствующие незакоммиченные пути (объявленное состояние).** Вместе с этим нарядом мостом
  приезжают незакоммиченные правки слоя Cowork: `docs/design/design-system.md` (revision 1.6) ·
  `docs/decisions.md` · `docs/worklog/0028-deviations-list-expansion.md`. Ничего другого слой
  Cowork в этот заход не писал. Ни один из них не лежит в `docs/model/`, поэтому **канон-хеш
  этим нарядом не двигается**.

- **Ветка:** `main`.

- **Канон-хеш на старте:**
  `sha256:09336970acf8adefe919a2b1cef6c14cb9a92ee59de070bc1502e60e52ac9293`
  _(вписан исполнителем на шаге 0 после коммита `eca6a29`; `worktree_check.py` →
  `VERDICT: OK`, значение совпало с ожидаемым и с зафиксированным нарядом `0027`)._
  Ожидалось то же значение, что зафиксировал наряд `0027`. Расхождение означало бы, что канон
  тронули между нарядами: **стоп**, вопрос Cowork.

---

## 1. Сетка уровня отклонения

Числа — из таблицы `grid` эталонной канвы, проверены суммой. Правило ширины: **таблица никогда
не шире 90 % окна** — 1728 px при 1920, 1152 px при 1280.

| Колонка | Ширина | Поведение | Выравнивание · направление |
|---|---|---|---|
| Раскрытие (стрелка) | 23 + 3 px маркер строки | фиксирована | по центру, слева всегда (шасси LTR) |
| `Number` | 200 | фиксирована | влево, `ltr` принудительно · не уходит |
| `Item` | 120 | минимально необходимая (12 знаков `MF5-10375A-N`, 13 px полужирный) | по началу абзаца, `auto` — иврит вправо |
| `Revision` | по заголовку | фиксирована | влево, `ltr` |
| `WO` | 92 | минимально необходимая (`W26007336`, табличные цифры) | влево, `ltr` |
| `Date` | 90 | `19.08.2026` табличными | влево, `ltr` · короткий формат при сжатии |
| `Dev. qty` | 80 | фиксирована | по центру, `ltr`; подсказка «parts in this deviation» |
| `Findings` — пилюли | 280 | фиксирована, пилюля по содержимому | влево, столбиком · **уходит при 1152** |
| `Decision` | 180 | фиксирована | влево, `ltr` · не уходит |
| `Explanation` | 410, минимум 367 | фиксирована; полный текст — подсказка | по началу абзаца, `auto` · не уходит |
| Зона запаса | 1fr | единственный гибкий трек | исчезает первой |

**Сумма при 1280:** `23 + 200 + 120 + 92 + 90 + 80 + 180 + 367 = 1152`. Ровно 90 % окна.
`Revision` в эту сумму не входила в канве (её там нет — колонка появилась нарядом `0025`):
её ширина берётся по заголовку, и на 1280 она уходит **девятой**, сразу за `Findings`.
Пересчёт при сжатии обязан сходиться — если не сходится, **вопрос Cowork, а не подгонка**.

**Колонка `Inspections` снимается совсем.** Она была временной: наряд `0011` §4 оставил её
именно потому, что раскрытия не существовало. Числовой колонки исследований в раскрытии тоже
нет — она была бы вторым счётчиком там, где показано содержимое.

**`Measurement point`, `Machine`, `NCR` в списке не показываются — и остаются в карточке
отклонения.** Это уточнение к решению 9 реестра, где формулировка «снимается с экрана» была
неточной.

## 2. Пилюли находок в свёрнутой строке

Компонент `finding_chip` (`design-system.md` §10). Высота 20 px, зазор между пилюлями 4 px,
отступы ячейки 7 px сверху и снизу.

Содержимое пилюли: `Dim. N` (приглушённый, табличные цифры) · **значение со знаком**
(полужирный, табличные) · тип отклонения · **значок мензурки 13 px только тогда, когда у этой
находки есть исследования**. Значок — признак наличия, **не вердикт**: он не меняется от
позиции исследования.

Видны **две** пилюли; при трёх и более под ними встаёт строка `+N findings` цветом ссылки.

**Высота строки отклонения плавает: 40 / 66 / 82** — одна находка, две, три и более.
Это единственное место в приложении, где строка не равна 40 (`design-system.md` §3,
revision 1.6).

> [!note] **`+N findings` — счётчик, и это принято сознательно.** Формально §4.4 брифа
> дизайна в свёрнутом виде выполняется не полностью. Принято на приёмке дизайна
> (`REVIEW_deviations-screen.md` §4) с доводом: главное требование — различить два отклонения
> по одной детали, не открывая карточку, — выполняется уже двумя пилюлями. Это **объявленное
> ограничение экрана**, а не недоделка; в отчёте его переоткрывать не нужно.

## 3. Раскрытие — служебная строка, а не дерево

**Виджет не меняется на дерево.** У уровней разные наборы колонок, и `QTreeView` заставил бы
их жить в одной сетке — собирается только ценой пустых ячеек. Решение 13 реестра.

Механика: под раскрытой строкой вставляется **служебная строка на всю ширину** (объединение
ячеек), в неё кладётся виджет-панель находок со своей шапкой и своей сеткой.

**Инварианты служебной строки — в критериях приёмки:**

1. **Не выбирается.** Клик по ней не меняет выбранное отклонение.
2. **Не ловится стрелками клавиатуры.** `↑`/`↓` идут по отклонениям, перескакивая панель.
3. **Единица действия — отклонение.** Карточка, правка, решение, удаление действуют на
   выбранное отклонение, даже когда курсор внутри панели. Ни находка, ни исследование своих
   действий в этом списке не имеют.

**Раскрытых записей может быть сколько угодно** (решение 6 реестра — отклонения сравнивают
между собой). Состояние — набор идентификаторов раскрытых отклонений; **переживает
перечитывание списка**: после правки, решения или регистрации раскрытые остаются раскрытыми.
Раскрываемая строка **остаётся на месте**, содержимое растёт вниз — прокрутка не прыгает.

## 4. Панель находок

Своя шапка 24 px (11 px, верхний регистр, `n-500`, нижняя граница `n-200`). Своя сетка:

| Колонка | Ширина | Что в ней |
|---|---|---|
| отступ уровня | 30 | — |
| `Dim.` | 72 | местный номер размера на чертеже, `ltr` |
| `Canon` | 70 | g-позиция или «не привязан» |
| `Sign · value` | 92 | знак и величина — **одна неделимая величина**, знак в той же колонке |
| `Zone` | 196 | из справочника; ячейка может быть ивритской |
| `Deviation type` | 176 | из справочника |
| `Research` | 168 | сводка по находке, см. ниже |
| `Inspections` | 340 | список исследований, см. ниже |
| зона запаса | 1fr | — |

**Сумма фиксированных: 1144.** При 1280 (лимит 1152) остаётся 8 px — сходится **без** потери
`Canon` и **без** сжатия `Inspections`. При 1920 зона запаса — 584 px.

Высота строки находки **28 / 43 / 58** по числу показанных исследований. Нижняя граница строки
находки — 1 px `n-100`.

### 4.1 `Research` — сводка по находке, пять состояний

| Значение | Когда |
|---|---|
| `Not researched` | у находки нет исследований — **норма, а не пустое поле** |
| `Approval possible` | позиция проставлена у **всех** исследований находки и совпадает |
| `Approval not possible` | то же |
| `Inconclusive` | то же |
| `Researched · N` | позиции разошлись **или** проставлены не у всех |

Ни одно состояние не является суждением экрана. Отдельная метка `Mixed` рассмотрена и
отклонена пользователем; `Researched · N` — нейтральный факт «открой и прочитай».

`Researched · N` **не выбирает строгейшую позицию** — это было бы решение, принятое экраном
за инженера.

### 4.2 `Inspections` — список, не счётчик

В ячейке, по одному исследованию на строку: **тип · позиция**. Показываются два; при трёх и
более — два и `+N inspections`.

**Короткий вывод (`conclusion`) в ячейку не помещается** — 340 px это около 45 знаков, а тип и
позиция съедают их почти целиком. Поэтому: **полный вывод — в подсказке по наведению**, без
обрезки; целиком, в отдельной колонке, он читается в карточке отклонения, где место есть.
Приём не новый — так уже сделано с обоснованием решения в списке.

Вторую строку под вывод в панели **не заводить**: шесть находок по два исследования
превратились бы в стену, и сравнение двух отклонений — то, ради чего раскрытие вообще
множественное, — стало бы невозможным.

Пустое состояние ячейки — `No inspections`, приглушённым; оно согласовано с `Not researched`
в соседней колонке и не дублирует его словами.

## 5. Стоимость чтения — два пакетных запроса на экран плюс один на раскрытие

Решение 7 реестра, отступление от буквы брифа объявлено там же.

1. **Отклонения** — существующий `list_deviations`. Счётчик `inspections` из `DeviationRow`
   убирается вместе с колонкой; счётчик `findings` заменяется составом находок.
2. **Находки всех видимых отклонений — одним запросом**, вместе с **числом исследований** у
   каждой находки (для значка мензурки). Не запрос на строку и не запрос на находку.
3. **Исследования — ленивым запросом на раскрытие**, один на раскрываемое отклонение.
   Результат держится, пока запись открыта.

**Запрет, который проверяется тестом:** ни одного запроса на строку и ни одного на находку при
отрисовке экрана. Пакетный близнец уже есть в кодовой базе (`findings.inspection_counts`,
наряд `0005`, критерий 8) — образец там.

## 6. Статус-бар и подвал

Подвал говорит **двумя числами**: `12 deviations / 24 findings`. Счётчики считают **отклонения**,
а не находки: экран, показывающий «12» там, где отклонений семь, а находок двенадцать,
обманывает — эта ошибка в проекте уже случалась и была поймана на ревью.

## 7. Порядок сжатия — одна версия

При переходе к 1152: (1) исчезает зона запаса; (2) `Date` переходит в короткий формат
`19.08.26`; (3) уходит колонка `Findings` — сводка не нужна, потому что на этой ширине
раскрытие показывает находки строками; (4) уходит `Revision`; (5) `Explanation` сжимается с
410 до минимума 367 и остаётся двухстрочной. Больше ничего не исчезает и не сжимается.

Уровень находки при 1152 не сжимается вовсе — он в него уже укладывается (§4).

## Критерии приёмки

1. `python tools/worktree_check.py` → `VERDICT: OK`, `canon hash HEAD` совпал со значением в
   шапке (канон этим нарядом не двигается).
2. **Полный прогон тестов зелёный**, число названо в отчёте.
3. **Тест сетки:** сумма ширин уровня отклонения при 1280 равна 1152; сумма фиксированных
   ширин уровня находки равна 1144. Тест называет правило, которое сторожит
   (`CLAUDE.md` §9а.10).
4. **Тест стоимости чтения:** отрисовка экрана с N отклонениями делает **два** запроса
   независимо от N; раскрытие одной записи добавляет ровно один.
5. **Тест высот:** строка отклонения 40 / 66 / 82 при одной, двух, трёх находках; строка
   находки 28 / 43 / 58 при одном, двух, трёх исследованиях.
6. **Тест инвариантов служебной строки:** клик по панели не меняет выбранное отклонение;
   `↑`/`↓` перескакивают панель; действие над строкой при курсоре внутри панели адресует
   отклонение.
7. **Тест `Research`:** все пять состояний, включая `Researched · N` при расхождении позиций и
   при непроставленной позиции у части исследований.
8. **Тест состояния раскрытия:** после перечитывания списка раскрытые записи остались
   раскрытыми; прокрутка не сместилась.
9. **Тест счётчиков:** подвал показывает число отклонений и число находок **разными** числами.
10. **Тест значка мензурки:** значок стоит только у находок с исследованиями и не зависит от
    позиции исследования.
11. **Проверка руками (пользователь):** раскрыть два отклонения по одной детали одновременно и
    сравнить находки; проверить отклонение с шестью находками рядом с отклонением с одной;
    навести на исследование и прочитать вывод в подсказке; сузить окно до 1280 и убедиться,
    что горизонтальной прокрутки нет.

## Чего не трогать

- **`docs/model/**`, `CLAUDE.md`, `docs/decisions.md`, `docs/design/**`, `docs/specs/**`** —
  полоса Cowork. Спека `docs/specs/deviations-list.md` пишется **как-построено**, Cowork, после
  приёмки — это конвенция `docs/specs/README.md`, а не забывчивость.
- **Кнопка `Find identical`** (160 px в строке находки, принята на приёмке дизайна) — **не
  входит.** Она выдаёт результат «на экране списка, той же фильтровой машиной», а фильтровой
  машины нет: это часть 2 `Q-14`, после S6. Её 160 px уходят в зону запаса. Объявленное
  отсутствие.
- **Кнопка «три точки» вместо пяти кнопок над таблицей** — **не входит**, отдельная задача
  потока (решение 12 реестра). Пять кнопок остаются как есть.
- **Механики эталонного экрана** — поиск, срезы-вкладки, чипы фильтров, «Clear all filters»,
  выбор колонок, «Export view», полоса скрытых записей — **не входят**, часть 2 `Q-14`.
- **Схема базы** — этим нарядом не меняется вовсе: все три уровня уже существуют.
- **Прочие экраны.** Механики эталонного экрана на них не тиражируются (наряд `0010` §3а,
  решение пользователя 2026-09-01). Список остаётся списком.

---
## Отчёт исполнителя (заполняет Claude Code)

- **Сделано:**

  **0. Входной контроль.** Незакоммиченным лежало **ровно** перечисленное — четыре пути,
  ничего сверх. Ни один не в `docs/model/`, поэтому гард дал `VERDICT: OK` сразу, и
  `canon hash HEAD` = `sha256:09336970…` совпал и с ожидаемым, и с зафиксированным нарядом
  `0027`. Коммит `eca6a29`, хеш вписан в шапку, принятая работа запушена (`4c7841f..ae4b6b9`).

  **1. Сетка.** Ширины объявлены **пикселями канвы**, а не знакоместами: под это заведён
  `kit.px(n)` — точное число без пола по заголовку и без пересчёта через среднюю ширину
  знака. Сжатие описано одной функцией `grid_widths(window_width, revision_width)`, и порядок
  ухода в ней ровно §7. `Inspections` снята совсем.

  **2. Пилюли находок.** Компонент `finding_chip` в `src/ui/kit/chips.py`: делегат, а не
  виджеты в ячейке — по три-шесть виджетов на строку заметны на списке в тысячу записей.
  Видны две пилюли, третья и далее — строкой `+N findings`. Мензурка чертится путём, как все
  значки канона §5.

  **3. Раскрытие.** Служебная строка на всю ширину (`setSpan` + `setCellWidget`), у её ячейки
  сняты **все** флаги — Qt пропускает такие при навигации клавиатурой, и клик по ним выбора
  не меняет. Панель фокуса не берёт и своего выбора не имеет. Раскрытых сколько угодно,
  состояние — набор идентификаторов, переживает `reload()`; прокрутка и выбор
  восстанавливаются **по отклонению**, а не по номеру строки.

  **4. Панель находок.** Своя шапка 24 px, своя сетка, `Research` пятью состояниями,
  `Inspections` списком «тип · позиция» с полным выводом в подсказке.

  **5. Стоимость чтения.** Ровно два запроса на отрисовку при любом N и **один** на
  раскрытие. Подпись канона и счётчик исследований приезжают вторым запросом левыми
  соединениями: вызовы `canon_labels` и `inspection_counts`, при всей их пакетности, дали бы
  третий и четвёртый.

  **6. Полный прогон.** 586 → **621**, все зелёные.

- **Коммиты:**
  - `eca6a29` — `docs: accept 0027, dialog heights into the design canon (QMS-018)` (шаг 0)
  - `ae4b6b9` — `docs(worklog): наряд 0028 — канон-хеш на старте и отметка о релее`
  - `89f9d66` — `feat(deviations): находки пилюлями, раскрытие строки, панель находок (QMS-018, наряд 0028)`
  - `099ba35` — `chore(screenshots): снимок раскрытия и замеры, которых не даёт offscreen (наряд 0028)`

- **Проверки (против критериев приёмки):**

  | № | Критерий | Результат |
  |---|---|---|
  | 1 | `worktree_check.py` → `OK`, хеш совпал | `VERDICT: OK`, код 0; `sha256:09336970…` — то же значение, что в шапке и в наряде `0027`; на приёмке прогнан повторно |
  | 2 | полный прогон зелёный, число названо | **621 passed**, 178 с (на входе 586, добавлено 35) |
  | 3 | суммы сетки 1152 и 1144 | `test_the_deviation_grid_adds_up_to_the_declared_width_at_1280` (сумма **и** состав колонок), `test_the_finding_grid_adds_up_to_1144`, плюс `test_the_revision_width_cannot_move_the_1280_sum` — сумма не зависит от шрифта, иначе та же сетка была бы верной здесь и неверной у оператора. **Замер нарисованных колонок:** 1152 при 1280 и 1144 в панели — ровно |
  | 4 | два запроса на экран, один на раскрытие | `test_rendering_the_screen_costs_two_queries_whatever_the_row_count` — сравнивает 1 запись и 7 записей по 3 находки: **2 и 2**; `test_expanding_one_record_adds_exactly_one_query` — 2 → 3 |
  | 5 | высоты 40/66/82 и 28/43/58 | `test_the_deviation_row_grows_with_its_findings` (замеряет `rowHeight` таблицы, не токен), `test_the_finding_row_grows_with_its_inspections`, `test_the_panel_applies_those_heights_to_its_rows` — третий отдельно, потому что зелёная чистая функция не говорит, позвал ли её кто-нибудь |
  | 6 | инварианты служебной строки | `test_the_service_row_is_neither_selectable_nor_reachable_by_arrows` (флаги ячейки — то, **чем** Qt решает оба вопроса), `test_the_action_addresses_the_deviation_even_with_the_panel_open`, `test_the_service_row_spans_the_whole_width` |
  | 7 | `Research`, все пять состояний | `test_research_summarises_without_judging` — семь случаев, включая **оба** пути к `Researched · N`: позиции разошлись и позиция проставлена не у всех; `test_the_panel_shows_the_research_summary_it_computed` — та же сводка на экране, рядом с `No inspections` |
  | 8 | раскрытие переживает перечитывание | `test_expansion_survives_a_reload_and_does_not_move_the_scroll` — **две** записи раскрыты одновременно, после `reload()` обе раскрыты, прокрутка та же; `test_collapsing_removes_the_panel_and_leaves_the_record` |
  | 9 | подвал: два разных числа | `test_the_footer_counts_deviations_and_findings_as_different_numbers` — 2 отклонения и 4 находки, числа взяты **разными**: при равных счётчик, считающий не то, был бы зелёным |
  | 10 | мензурка — признак наличия | `test_the_flask_marks_presence_of_inspections_not_their_position` — четыре позиции подряд плюс находка без исследований; `test_the_flask_is_actually_drawn_and_a_bare_chip_is_not` — считает **пиксели цвета контура**, а не «не белые»: заливки пилюли поровну в обоих случаях, и первая версия теста была зелёной на нерисующем делегате |
  | 11 | проверка руками (пользователь) | за исполнителем не числится (`CLAUDE.md` §9а) |

  **Снимки и замеры на нативной платформе** (`build/screens/`):
  `01-section-4-deviations.png` (свёрнутый экран с пилюлями и мензуркой),
  **`21-deviations-expanded.png` — новый**, раскрытая строка с панелью,
  `01-section-4b-deviations-1280.png` (сжатие). Замер печатает ширины обоих уровней и запас
  под пилюлю исхода.

- **Отклонения / вопросы Cowork:**

  1. **`WO` не помещается в свои 92 px — сетка сходится, значение режется.** На нативной
     платформе `W26007336` рисуется как `W260073…`. Числа: колонка 92, вычитаемые поля
     листа стиля 20, объявленный запас по `SE_ItemViewItemText` — 71 при нужных 68, но
     **нарисованная** ширина меньше объявленной примерно на 6–8 px, и значение обрезается.
     Соседняя `Date` (90 px, нужно 62) рисуется целиком — то есть дефицит именно здесь и
     невелик.

     **Ширину не трогал.** `WO` входит в сумму 1152, и любое расширение её ломает; наряд
     прямо говорит «не сходится — вопрос Cowork, а не подгонка». Замечу отдельно: подсказка
     тут **не спасает** — `DirectionalDelegate.helpEvent` показывает её только когда
     `advance > columnWidth − 2·PAD_CELL`, то есть по той же завышенной формуле, и при 68 ≤ 72
     молчит. Значение оказывается недостижимым с экрана. Варианты, как я их вижу: (а) `WO`
     92 → 100 и `Explanation` 367 → 359, сумма 1152 сохраняется; (б) оставить как есть и
     поправить порог подсказки в `kit` — но это поведение **всех** экранов, и в наряд оно не
     входит. Жду решения; сам не выбирал.

  2. **`Dev. qty` выровнена вправо, канва рисует по центру.** `CLAUDE.md` §9 говорит:
     «Вправо — только то, что глаз сравнивает **по величине** вниз по столбцу», и количество
     под это подпадает; так было и до наряда. Оставил как было, расхождение с колонкой
     «Выравнивание» таблицы §1 отмечаю. Одна строка, если решишь в пользу канвы.

  3. **Пилюля находки — 22 px, а не 20.** Наряд §2 и `design-system.md` §10 говорят «20 px»,
     но арифметика §3 той же дизайн-системы — «chip 22, gap 4, padding 7» — и **только она**
     воспроизводит 40 / 66 / 82: `40 + (22 + 4) = 66`, `66 + 16 = 82`. Взял 22, потому что
     оно совпадает и с `pill 22` из «Controls». Расхождение внутри самого канона, поправить
     стоит в одном месте из двух.

  4. **`DeviationRow` сохранил оба счётчика, вопреки §5.1.** Наряд велит убрать `inspections`
     и заменить `findings`. Оба поля читает **другой экран** — `src/ui/item_deviations_dialog.py`
     («Deviations of this item»), а он в «чего не трогать» как «прочие экраны»; на них же
     завязан `precedents.PrecedentRow`. Снятие полей потребовало бы переделать оба, то есть
     нарушить явный запрет ради цели, которая уже достигнута: список отклонений их не читает,
     а запросов они не добавляют — оба счётчика считаются подзапросами **внутри того же**
     `list_deviations`. Выбрал явный запрет над §5.1. Если поля надо снять — это отдельный
     наряд, трогающий тот экран.

  5. **Три дефекта вёрстки поймал снимок, ни один — тест** (`CLAUDE.md` §9а.8):
     - Qt держит собственный минимум секции (здесь 34 px) и молча расширял колонку раскрытия
       с объявленных 23. Объявленная сумма сходилась, **нарисованная** — нет, на 11 px;
     - стрелка раскрытия текстом в 23 px не помещалась вовсе: лист стиля даёт ячейке
       `padding: 0 10px`, на подпись оставалось 3 px. Переведена на делегат, состояние —
       ролью, а не символом в тексте;
     - панель не растягивалась на ширину служебной строки: `setCellWidget` кладёт виджет в
       прямоугольник **ячейки**, объединение к тому моменту не учтено. Панель оставалась
       шириной первой колонки, а её шапка выходила в две подписи из восьми.

     Все три при зелёном прогоне. Четвёртый, того же класса, — в самом инструменте снимков:
     шапку `QHeaderView` Qt раскладывает отложенно, и снимок ловил промежуточное состояние.

  6. **Замер запаса под пилюлю переехал из теста в инструмент.** Под offscreen шрифт
     моноширинный, и прежняя проверка требовала для «Not decided» 197 px при 160 доступных —
     на нативной платформе нужно 116. Тест, сверяющий пиксели канвы с этим завышением,
     красный на верной сетке, то есть проверяет платформу, а не экран. В тесте осталось то,
     что под offscreen проверяемо (объявление экрана доходит до колонки без пересчёта), сам
     замер — в `tools/screenshots.py::measure_pill_room`.

  7. **`+N findings` не переоткрываю** — наряд просил этого не делать, и я не делаю.

  8. Расхождений терминологии за наряд не встретилось.
---

## Доводка по ревью `0028` (Cowork, 2026-09-07)

Наряд **принят** по десяти исполнительским критериям. Восемь вынесенных пунктов разобраны;
ниже — только то, что требует работы. Канон за наряд не двигался (`docs/model/` даёт тот же
`sha256:09336970…`), проверено Cowork пересчётом, а не по отчёту.

### Д-1 · Обе колонки остаются при 1280 — решение пользователя

**Порядок сжатия был неверен, и неверен по существу, а не в числе.** Канва обосновывала уход
`Findings` тем, что «находки на этой ширине уже показаны строками» — но строками они показаны
только у **раскрытой** записи. У свёрнутой при 1280 про находки не видно ничего, то есть
требование, ради которого экран переделан (различить два отклонения по одной детали, не
открывая карточку), при минимальном окне не выполнялось вовсе. Это видно на снимке
`01-section-4b-deviations-1280.png`: запись `DEV-260907-0001` свёрнута и нема.

Решение пользователя: **обе колонки важны, место берётся оптимизацией.** Три источника:

1. **Ревизия пустого места — правило ширины получает пол.** Было «не шире 90 % окна»; стало
   **«не шире 90 % окна и не уже 1216 px»** (`design-system.md` §3, revision 1.8). При 1920
   ничего не меняется — 1728. При 1280 таблица получает **1216** вместо 1152, поля по 32 px с
   каждой стороны. Прежние 128 px чистого фона при голодающих колонках — это ровно то пустое
   место, ради которого правило и пересматривается. **+64 px.**
2. **Сокращение заголовков.** `Dev. qty` → **`Qty`**: заголовок был шире содержимого и один
   задавал ширину колонки; полная формулировка «parts in this deviation» уже живёт в подсказке
   и никуда не девается.
3. **Снятие завышений замером.** Твёрдый замер из отчёта: пилюля «Not decided» требует
   **116 px** при колонке **180**. Столько же подозрений к `Number` 200 под
   пятнадцатизначный `DEV-260907-0002` и к `Date` 90 под короткий формат `07.09.26`.

**Ориентировочный бюджет при 1280 (таблица 1216).** Числа — отправная точка, **не** норма:

| Колонка | Было | Ориентир | Откуда |
|---|---|---|---|
| Раскрытие | 23 | **34** | реальный минимум секции Qt, объявленные 23 были фикцией |
| `Number` | 200 | **140** | замер 15 знаков полужирным табличным |
| `Item` | 120 | **112** | замер `MF5-10375A-N` |
| `WO` | 92 | **100** | твой вариант (а); замер `W26007336` |
| `Date` | 90 | **76** | короткий формат `07.09.26` |
| `Qty` | 80 | **56** | заголовок сокращён, значение ≤ 4 знаков |
| `Findings` | уходила | **260** | остаётся при 1280 |
| `Decision` | 180 | **132** | замер: «Not decided» 116 + отступы |
| `Explanation` | 367 | **306** | остаток |
| | | **= 1216** | |

**Каждое из этих чисел подлежит замеру на нативной платформе, и замер старше таблицы.**
Объявленный «минимум 367» у `Explanation` вышел из той же завышающей формулы и нормой не
является. Правило записано в канон (`design-system.md` §3, «A declared column width is not a
drawn column width») и в конституцию (`CLAUDE.md` §9а.12).

**При 1920 меняется одно:** `WO` 92 → 100, восемь пикселей берутся из зоны запаса
(210 → 202). Остальное как есть.

**Если после честного замера сумма не влезает в 1216 — доложить таблицей «колонка · замер ·
объявлено» и остановиться.** Подгонка запрещена; решать, что уходит, будем зная цифры.

### Д-2 · Пилюля находки — 22, и состав высот исправлен

Ошибка Cowork: «20 px» в наряде §2 и в `design-system.md` §10 снято с разметки канвы, а не с
её арифметики. Твой выбор верен и подтверждён — 22 совпадает с `pill 22` из Controls.

Заодно исправлена прозой не закрывавшаяся композиция. Правильная — твоя:

| Находок | Высота | Из чего |
|---|---|---|
| 1 | 40 | обычный минимум строки, одна пилюля в него укладывается |
| 2 | 66 | `40 + (22 + 4)` |
| 3 и больше | 82 | `66 + 16` — строка **`+N findings`**, а не третья пилюля |

Третьей пилюли не бывает ни при каком числе находок. Внесено в `design-system.md` revision 1.8.
**В коде менять нечего** — там уже 22; правился канон.

### Д-3 · `Dev. qty` остаётся выровненной вправо

Расхождение реально, и прав код: `CLAUDE.md` §9 говорит «вправо — только то, что глаз
сравнивает по величине вниз по столбцу», количество под это подпадает. Канва — витрина, канон
старше (`worklog/README.md`). Строка «по центру» в §1 этого наряда — ошибка переноса из канвы,
считать её недействительной. Менять нечего.

### Д-4 · Счётчики в `DeviationRow` — выбор утверждён

Явный запрет «прочие экраны» старше §5.1, и цель §5.1 достигнута: список их не читает,
запросов они не добавляют. Снятие полей поедет с тем нарядом, который тронет
`item_deviations_dialog.py`. Отдельной задачи не завожу.

### Что сделать

1. Порядок сжатия: **`Findings` и `Explanation` остаются при 1280**; уходят зона запаса,
   `Revision`, «три точки» (как и было), `Date` переходит в короткий формат.
2. Ширины уровня отклонения при 1280 назначить **замером**, целевая сумма — **1216**.
   Ориентир — таблица Д-1.
3. `WO` при 1920 — 100 px, восемь берутся из зоны запаса.
4. Заголовок `Dev. qty` → `Qty`, подсказка «parts in this deviation» сохраняется.
5. Правило ширины таблицы в `kit`: `min(90 % окна, …)` получает пол 1216.

### Критерии приёмки доводки

1. **Замер на нативной платформе**, напечатанный инструментом снимков: для каждой колонки
   уровня отклонения — объявленная ширина, нарисованная текстовая площадь и ширина самого
   длинного реального значения. **Ни одно значение не обрезано** при 1280 и при 1920.
2. Сумма при 1280 = **1216**, при 1920 = **1728**. Тест на объявление **и** замер — оба.
3. Снимок 1280: у свёрнутой записи **видны пилюли находок**, и `Explanation` читается двумя
   строками. Это и есть проверка, ради которой доводка заведена.
4. `W26007336` виден целиком на обеих ширинах.
5. Полный прогон зелёный; число тестов названо.
6. Канон-хеш на приёмке совпадает с `sha256:09336970…`.

### Чего не трогать в доводке

- **Порог подсказки и формула ширины в `kit`** — заведена отдельной задачей **QMS-022**
  (счёт по знакам заменяется замером `QFontMetrics`). Это поведение всех экранов, и в доводку
  оно не входит. Здесь чиним сетку одного экрана, а не механизм.
- Панель раскрытия, `Research`, `Inspections`, запросы — приняты, не трогаются.
- `docs/**` — полоса Cowork.


---
## Отчёт исполнителя по доводке (заполняет Claude Code)

- **Сделано** — пять пунктов раздела «Что сделать», все:

  1. **`Findings` и `Explanation` остаются при 1280.** Уходят зона запаса и `Revision`;
     `Date` переходит в короткий формат. Признак «сетка сжата» переписан на уход `Revision`:
     прежний признак — уход `Findings` — вместе с самим уходом перестал существовать.
  2. **Ширины назначены замером.** Числа таблицы Д-1 были отправной точкой; каждое проверено
     на нативной платформе и два из них замером **изменены** (см. отклонения). Сумма при
     1280 — **1216 ровно**.
  3. **`WO` 100 px** на обеих ширинах.
  4. **`Dev. qty` → `Qty`**, полная формулировка «parts in this deviation» — в подсказке
     заголовка. Подсказки на заголовке до доводки не было вовсе: канва её описывала, код нет.
  5. **Правило ширины таблицы переехало в `kit`** и получило пол: `kit.table_limit` =
     `max(90 % окна, 1216)`. Живёт в `kit`, а не на экране, потому что это правило про таблицы
     вообще — второй экран, который до него доберётся, обязан получить то же число.

  **Ключевая величина доводки — 27 px непечатаемого в ячейке:** линия сетки 1, отступы листа
  стиля 20, собственные поля Qt 6 (`PM_FocusFrameHMargin + 1` с каждой стороны). Она
  **считается**, а не зашита: на другой платформе слагаемые другие, и зашитое число врало бы
  там молча. Все объявленные ширины — вида «нужно тексту + 27».

- **Замер (критерий 1), нативная платформа, печатает `tools/screenshots.py`:**

  | Колонка | Д-1 | Объявлено | Рисует | Рекорд | 1280 |
  |---|---|---|---|---|---|
  | Раскрытие | 34 | **34** | — | — | ok (рисует делегат) |
  | `Number` | 140 | **131** | 104 | 104 | ok |
  | `Item` | 112 | **115** | 88 | 63 | ok |
  | `WO` | 100 | **100** | 73 | 68 | ok |
  | `Date` | 76 | **76** | 49 | 48 | ok |
  | `Qty` | 56 | **56** | 29 | 20 | ok |
  | `Findings` | 260 | **303** | 282 | 230 | ok |
  | `Decision` | 132 | **143** | 122 | 116 | ok |
  | `Explanation` | 306 | **258** | 462 (две строки) | 413 | ok |
  | | | **1216** | | | **обрезанных 0** |

  При 1920: `Revision` 75 (замер заголовка), `Findings` 303, `WO` 100, остальное как было;
  сумма **1592** из предела 1728. Обрезанных — **0**.

- **Коммиты:**
  - `3c5cad1` — `docs: приёмка 0028 и доводка по ревью (QMS-018)` (шаг 0)
  - `695d76e` — `fix(deviations): сетка списка назначена замером, сумма 1216 (доводка наряда 0028)`

- **Проверки против критериев приёмки доводки:**

  | № | Критерий | Результат |
  |---|---|---|
  | 1 | замер напечатан, ни одно значение не обрезано | таблица выше; `обрезанных колонок: 0` на **обеих** ширинах |
  | 2 | сумма 1216 при 1280 и 1728 при 1920, тест **и** замер | тест `test_the_deviation_grid_adds_up_to_the_declared_width_at_1280` (объявление) + замер `ИТОГО 1216 предел 1216`. При 1920 сумма 1592 **из** предела 1728 — предел это потолок, а не цель; полная сетка ниже него, и зона запаса и есть разница |
  | 3 | снимок 1280: пилюли у свёрнутой, `Explanation` в две строки | `01-section-4b-deviations-1280.png` — у свёрнутой `DEV-260907-0001` видна пилюля `Dim. 77 · − 0.05 · Thread burr`; обоснование читается двумя строками |
  | 4 | `W26007336` целиком на обеих ширинах | замер: нужно 68, рисует 73, на обеих; видно на обоих снимках |
  | 5 | полный прогон зелёный, число названо | **625 passed** (было 621; добавлены тесты пола ширины, замерности сетки и подсказки `Qty`) |
  | 6 | канон-хеш совпал | `VERDICT: OK`, `sha256:09336970acf8adefe919a2b1cef6c14cb9a92ee59de070bc1502e60e52ac9293` |

- **Отклонения / вопросы Cowork:**

  1. **Два числа Д-1 замер изменил — это ровно тот случай, о котором доводка предупреждала.**
     - **`Decision` 132 → 143.** Пилюля «Not decided» требует 116, оправа делегата и ячейки —
       ещё 27. При 132 подпись резалась бы. Число 132 в Д-1 получено как «116 + отступы», но
       отступов там три слоя, а не один.
     - **`Findings` 260 → 303.** Самая широкая реальная пилюля —
       `Dim. 18 · − 0.05 · Cutting-edge width` (`Cutting-edge width` — самый длинный тип
       отклонения в базе прогона) — требует 282 плюс оправа ячейки 21. При 260 она резалась
       бы, а колонка пилюль, режущая пилюли, отменяет смысл пункта 1 доводки.

     Разницу (11 + 43 = 54 px) взял из `Explanation`: 306 → 258. Она остаётся выше своего пола
     по заголовку (94) и, будучи двухстрочной, вмещает реальное обоснование целиком. Сумма
     1216 сохранена. `Number` 140 → 131 и `Item` 112 → 115 — тоже замер, разница в пределах
     пары пикселей.

  2. **Обоснование сделано двухстрочным, и без этого критерий 1 недостижим.** Канва и §7
     наряда говорят «остаётся двухстрочной», но в «Что сделать» это не вошло, а критерий 3
     требует. В одну строку реальное обоснование (413 px) не влезает **ни при какой** ширине
     из бюджета, то есть «ни одно значение не обрезано» без переноса невыполнимо.

     **Отменено общее правило `kit`** — «ячейка однострочная, длинное режется с подсказкой»
     (наряд `0020` §7.3, правило 2) — и отменено **для одного экрана**, строкой
     `self.table.setWordWrap(True)`. Сам `kit` не тронут. Прочие колонки от переноса не
     меняются: их значения короче строки. Отмечаю как расхождение с общим правилом — если оно
     должно остаться нерушимым, обоснование придётся резать, и тогда критерий 1 надо
     переформулировать.

  3. **Заголовок `Revision` резался — нашёл сам замер, и это второй случай §9а.12.**
     `kit.FIT_LABEL` даёт «ширина заголовка + отступы ячейки», не зная про линию сетки и поля
     Qt: объявленные 68 оставляли тексту 41 при нужных 48. Ширину назначил замером **на
     экране** (`_revision_width` = заголовок + непечатаемое), формулу в `kit` **не трогал** —
     это QMS-022. Но отметить стоит: недобор в 7 px есть у **каждой** колонки, объявленной
     через `FIT_LABEL`, на всех экранах, а не только здесь.

  4. **Предел при 1920 — потолок, не цель.** Критерий 2 говорит «сумма при 1920 = 1728», но
     1728 это `table_limit(1920)`, то есть максимум; полная сетка даёт 1592, и разница 136 px
     и есть та самая зона запаса, которая по §1 «исчезает первой». Сетка ровно в 1728
     означала бы отсутствие зоны запаса вовсе. Прочитал критерий как «не превышает 1728» —
     если имелось в виду буквальное равенство, скажи, и я раздам остаток колонкам.

  5. **`Qty` не сузил до замера.** По замеру ей хватает 47 (заголовок 20 + 27); Д-1 даёт 56, я
     оставил 56. Причина: `9999` (метка выборки `X מתוך Y`, `SAMPLING_QUANTITY`) требует
     28 + 27 = 55, и запас в один пиксель здесь честнее, чем ширина под сегодняшние данные.

  6. Порог подсказки и формула ширины в `kit` не тронуты — QMS-022. Панель раскрытия,
     `Research`, `Inspections` и запросы не тронуты.
