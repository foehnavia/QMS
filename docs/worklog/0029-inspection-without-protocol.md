# Наряд 0029 — исследование без протокола: галочка и инвариант «файл или вывод»

- **Задача / цель:** `QMS-024`. Исследование можно завести **без файла протокола** — по
  галочке `No protocol`, и тогда короткий вывод становится обязательным. Инвариант: **каждая
  запись несёт файл протокола ИЛИ вывод**, и держит его схема, а не форма.

- **Повод — прогон руками QMS-018 (2026-09-07).** Часть отклонений решается одним чертежом:
  наружный диаметр 10.0 против внутреннего 9.9 — детали не сопрягаются, и никакое исследование
  этого не изменит. Для таких случаев в словарь добавлен тип `Tolerances review`, к которому
  документа не прилагается вовсе. Форма требует протокол, и такая запись не сохраняется —
  схема, придуманная под документированные исследования, не пускает верный рабочий случай.

- **Вход:**
  - `docs/model/Inspection.md` — канон **rev 1.02** (правка приезжает вместе с нарядом, шаг 0):
    секция «When a row is created» переписана, в «Fields» добавлен `No protocol` и инвариант
  - `docs/decisions.md` — раздел «QMS-024 · исследование без протокола (2026-09-07)»
  - `CLAUDE.md` — §9, §9а (конвенции UI и тестов), §3в, §3б
  - `docs/worklog/README.md` — протокол наряда
  - `src/db/models.py`, `src/domain/inspections.py`, `src/ui/inspection_dialog.py`,
    `src/ui/card_dialog.py`, `migrations/versions/`

- **Сопутствующие незакоммиченные пути (объявленное состояние, состав снят листингом перед
  выдачей):** `docs/model/Inspection.md` · `docs/decisions.md` ·
  `docs/worklog/0029-inspection-without-protocol.md`. Ничего другого слой Cowork в этот заход
  не писал.

- **Ветка:** `main`.

- **Фактический состав doc-слоя на шаге 0 доводки (снят `git status` исполнителя,
  `README.md` «Объявление состава — двустороннее»):**
  `docs/model/reference/reference-data.md` · `docs/model/Inspection.md` ·
  `docs/design/design-system.md` · `docs/worklog/README.md` · `docs/decisions.md` ·
  `docs/worklog/0029-inspection-without-protocol.md` — **шесть путей, совпало с перечнем
  Cowork, сверх перечня ничего.** Файл наряда на диске 29 087 байт: отчёт исполнителя,
  затёртый мостом, восстановлен целиком (проверено по шести отклонениям и таблице критериев).

- **Канон-хеш на старте:**
  `sha256:6441291af22d549125d49a2c29ac8e16e98f0d5e9329940372fdb3bef7baada1`
  _(вписан исполнителем на шаге 0 после коммита `4316d75`; `worktree_check.py` →
  `VERDICT: OK`, значение совпало с ожидаемым)._
  - до правок: `sha256:09336970acf8adefe919a2b1cef6c14cb9a92ee59de070bc1502e60e52ac9293`
  - после правок, ожидаемое: `sha256:6441291af22d549125d49a2c29ac8e16e98f0d5e9329940372fdb3bef7baada1`

  Хеш рабочей копии, посчитанный Cowork; подтверждает исполнитель гардом на шаге 0. Не
  сошлось — **стоп**, вопрос Cowork.

---

## 0. Шаг 0 — закоммитить приехавший канон

`worktree_check.py` на старте даст `CANON MOVED` — ожидаемо, `Inspection.md` приезжает
незакоммиченным. `git status` → сверить состав с перечнем выше (сверх перечисленного —
**стоп**) → отдельный коммит `docs: canon rev 1.02 — inspection without a protocol (QMS-024)`
→ `worktree_check.py` обязан дать `VERDICT: OK` → вписать `canon hash HEAD` в шапку и сверить
с ожидаемым.

## 1. Схема и миграция `rev05`

| Поле | Было | Станет |
|---|---|---|
| `no_protocol` | нет | новое, `Boolean` NOT NULL, **по умолчанию 0** |
| `protocol` | `Text` NOT NULL | **nullable** |
| `conclusion` | `String(500)` nullable | без изменений в объявлении; обязательность даёт инвариант |

**Инвариант держит `CHECK`, а не форма.** Смысл: запись, не несущая ни файла, ни вывода,
невидима для поиска прецедентов — то есть бесполезна ровно в том, ради чего таблица заведена.
Форму можно обойти импортом, скриптом или следующим нарядом; ограничение схемы обойти нельзя.

```
(no_protocol = 0 AND protocol IS NOT NULL AND trim(protocol) <> '')
OR
(no_protocol = 1 AND (protocol IS NULL OR trim(protocol) = '')
                 AND conclusion IS NOT NULL AND trim(conclusion) <> '')
```

Отмеченная галочка требует **пустого** протокола намеренно: «файл есть, но он не нужен» —
состояние без смысла, и допускать его значит заводить третий случай, который придётся
объяснять на каждом экране.

**Перенос существующих строк:** у всех есть протокол → `no_protocol = 0`, ограничение проходят
без правки данных. Проверить это тестом, а не предположением.

**Откат.** Строки с `no_protocol = 1` в старой схеме непредставимы (там `protocol` NOT NULL, а
у них его нет). Удалять **с громким сообщением**, по прецеденту `rev04` и `rev02` — и по тому
же доводу: подставить выдуманный путь к файлу означало бы вписать в базу документ, которого не
существует. Снять `CHECK` перед изменением значений, как в `rev04`.

## 2. Доменный слой

- `create_inspection` / `update_inspection` принимают `no_protocol`. Значений по умолчанию у
  `update_inspection` **не появляется** (`CLAUDE.md` §9).
- Валидация — **доменная, с человеческим текстом**, поверх схемного ограничения. Ограничение
  ловит любой путь записи; доменная проверка объясняет оператору, что не так, вместо
  `IntegrityError`:
  - галочка снята → протокол обязателен (как сейчас);
  - галочка отмечена → протокол обязан быть пуст, **вывод обязателен**;
  - текст отказа называет, чего именно не хватает, — не «нарушено ограничение».
- Существование файла протокола по-прежнему **не проверяется** (решение 4 реестра).

## 3. Форма исследования

- Галочка **`No protocol`** под полем протокола, **снята по умолчанию**.
- Отмечена → поле пути и кнопка выбора файла **блокируются и очищаются**; подпись поля
  `Conclusion` получает признак обязательности.
- Снята обратно → поле протокола снова активно и пусто; введённый вывод не теряется.
- Порядок на форме не меняется: тип → позиция → вывод → протокол → галочка.

## 4. Карточка отклонения

В секции исследований запись без протокола читается как полноценная: тип, позиция, вывод.
Кнопка открытия протокола для неё **неактивна** — и это не отказ, а отсутствие файла; если
неактивная кнопка без объяснения выглядит поломкой, поставить рядом приглушённую пометку
`No protocol`. Решает исполнитель по снимку, а не по предположению.

## Критерии приёмки

1. `worktree_check.py` → `VERDICT: OK`; `canon hash HEAD` вписан и совпал с ожидаемым.
2. Полный прогон зелёный (`--ignore`/`-k` — только после первого полного прохода), число
   названо. На входе 631.
3. **Тест ограничения схемы:** запись без протокола и без вывода **отвергается на уровне базы**
   — попытка вставки в обход домена не проходит. Это главный тест наряда: он проверяет
   инвариант, а не форму.
4. **Тест:** галочка отмечена + вывод → сохраняется; галочка отмечена без вывода → отказ с
   человеческим текстом; галочка снята без протокола → отказ.
5. **Тест миграции:** накат на базе с существующими строками не теряет строк и проставляет им
   `no_protocol = 0`; откат описан, работает, и строки с `no_protocol = 1` удаляются с
   сообщением.
6. **Тест формы настоящими событиями** (§9а.6): клик по галочке блокирует поле протокола и
   очищает его; повторный клик возвращает поле активным. Не вызовом метода — кликом.
7. Снимок формы с отмеченной галочкой — в `build/screens/`.
8. **Прогон руками (пользователь):** завести исследование типа `Tolerances review` с отмеченной
   галочкой и выводом, сохранить, увидеть его в карточке и в раскрытии списка.

## Чего не трогать

- **`docs/**`** — полоса Cowork; на шаге 0 только закоммитить приехавшее.
- **Экран списка отклонений, панель раскрытия, сетка и замеры** — приняты QMS-018, не
  трогаются. Запись без протокола обязана в них лечь как есть, без правок экрана.
- **`kit`: порог подсказки и `FIT_LABEL`** — задача **QMS-022**.
- **`mapping_dialog.py`** — задача **QMS-023**.
- **Где живёт решение — на отклонении или на находке** — открытый вопрос, в этом наряде не
  решается и модели не касается.
- **Трёхпозиционный вывод** — принят нарядом `0027`, не пересматривается.

---
## Отчёт исполнителя (заполняет Claude Code)

- **Сделано:**

  **0. Шаг 0.** Гард дал `CANON MOVED`, как и предсказано. После коммита `4316d75` —
  `VERDICT: OK`, `canon hash HEAD` = `sha256:6441291a…`, совпал с ожидаемым; вписан в шапку.
  Состав приехавшего разошёлся с объявленным — см. отклонение 1.

  **1. Схема и миграция `rev05`.** `no_protocol` (`Boolean` NOT NULL, по умолчанию 0),
  `protocol` стал nullable, `CHECK` несёт инвариант «файл ИЛИ вывод» слово в слово по §1.
  Накат — **в два прохода**: колонка появляется и заполняется до создания констрейнта, иначе
  пересборка таблицы копировала бы строки через него (урок `rev04`). Откат снимает `CHECK`
  до правки значений и удаляет строки без протокола громко.

  **2. Домен.** `no_protocol` в обеих функциях, **без умолчаний**. Прежний `_check_protocol`
  заменён на `_check_record(protocol, conclusion, no_protocol)` — три нехватки, три текста, и
  каждый называет **следующее действие**, а не нарушенное правило.

  **3. Форма.** Галочка под полем протокола, снята по умолчанию. Отмечена → поле пути и
  кнопка выбора блокируются и **очищаются**, подпись становится `Conclusion *:`. Снята →
  поле снова активно и пусто, **вывод не теряется**. Порядок полей не изменился.

  **4. Карточка.** Запись без протокола читается полноценно; кнопка открытия неактивна, и
  подсказка на ней **и на строке** говорит почему.

  **5. Прогон.** 631 → **657**, все зелёные.

- **Коммиты:**
  - `4316d75` — `docs: canon rev 1.02 — inspection without a protocol (QMS-024)` (шаг 0)
  - `9e2be41` — `feat(inspection): исследование без протокола, инвариант «файл ИЛИ вывод» (QMS-024, наряд 0029)`
  - `964f8e3` — `feat(ui): галочка No protocol и запись без файла в карточке (QMS-024, наряд 0029)`

- **Проверки против критериев приёмки:**

  | № | Критерий | Результат |
  |---|---|---|
  | 1 | `worktree_check.py` → `OK`, хеш вписан и совпал | `VERDICT: OK`; `sha256:6441291af22d549125d49a2c29ac8e16e98f0d5e9329940372fdb3bef7baada1` |
  | 2 | полный прогон зелёный, число названо | **657 passed** (на входе 631, добавлено 26) |
  | 3 | **главный:** запись без файла и без вывода отвергается **базой** | `test_a_record_with_neither_a_file_nor_a_conclusion_is_refused_by_the_schema` — вставка **прямым SQL, мимо домена**; плюс `test_the_schema_refuses_every_shape_the_invariant_forbids` (все **пять** запрещённых видов) и обратная сторона `test_the_schema_admits_both_legitimate_shapes` (оба законных) |
  | 4 | галочка + вывод → сохраняется; без вывода → отказ; галочка снята без протокола → отказ | `test_the_form_saves_a_record_without_a_protocol`, `test_the_form_refuses_a_ticked_box_without_a_conclusion` (проверяет и то, что текст **не** говорит про constraint), `test_the_form_still_refuses_an_unticked_box_without_a_protocol`; на доменном уровне — `test_the_domain_explains_what_is_missing_instead_of_an_integrity_error` |
  | 5 | миграция: строки не теряются, `no_protocol = 0`; откат работает и удаляет громко | `test_rev05_keeps_every_existing_row_and_marks_it_as_documented` (сверяет **все три строки поимённо**, а не число колонок), `test_rev05_admits_a_row_without_a_protocol_and_refuses_an_empty_one`, `test_rev05_downgrade_drops_what_the_old_schema_cannot_hold`, `test_rev05_downgrade_is_silent_when_every_row_carries_a_file`, `test_rev05_adds_a_column_not_a_table` |
  | 6 | форма — **настоящими событиями** | `test_a_real_click_on_the_box_disables_and_clears_the_protocol_field`, `test_a_second_real_click_gives_the_protocol_field_back`, `test_the_no_protocol_box_is_unticked_by_default`. `QTest.mouseClick` по центру индикатора, на **показанном** виджете (§9а.5, §9а.6) |
  | 7 | снимок формы с отмеченной галочкой | **`build/screens/22-dialog-inspection-no-protocol.png`** — новый; плюс пересобран `09-dialog-inspection.png` (снятая галочка) |
  | 8 | прогон руками (пользователь) | за исполнителем не числится (`CLAUDE.md` §9а) |

- **Отклонения / вопросы Cowork:**

  1. **Состав приехавшего разошёлся с объявленным — второй наряд подряд.** Объявлено три
     пути, на диске **четыре**: сверх перечня `CLAUDE.md` (§9а.14 и §9а.15, ратификация по
     итогам доводки 2 наряда `0028`). Конфликта с работой нет, канон-хеш сошёлся, поэтому не
     останавливался. Отмечаю, потому что в доводке 2 то же случилось с
     `docs/design/design-system.md`, а формулировка «состав снят листингом перед выдачей» в
     этом наряде звучит как усиленная гарантия — и она не сработала.

  2. **Найден и исправлен дефект отрисовки флажка — снимком, не тестом.** На первом снимке
     галочка `No protocol` выглядела **подписью**: `QCheckBox::indicator` в `kit/theme.py`
     описан не был, и как только на виджет лёг лист стиля, отрисовка перешла к нему — в
     **снятом** состоянии не рисовалось ничего, кликать было не по чему. Ровно `CLAUDE.md`
     §9 В-6; радиокнопка этого не показывала, её подстиль описан с наряда `0019`.

     Правка в `kit/theme.py` — это `kit`, но **не** порог подсказки и не `FIT_LABEL`, то есть
     не полоса QMS-022. Флажок описан квадратом, а не кругом: переключатель и выбор из
     нескольких не должны выглядеть одинаково. Сторожат **два** теста пикселями: видимость
     обоих состояний и то, что фигуры не поменялись местами (замерено: доля закрашенного 0.867
     у флажка против 0.776 у радиокнопки).

     Замечу цену: `kit/theme.py` красит **все** экраны, и до этого наряда флажков в
     приложении не было ни одного — дефект существовал с наряда `0011` и ждал первого флажка.

  3. **Тип `Tolerances review` в стартовый справочник не добавлен.** Наряд говорит «в словарь
     добавлен тип» в прошедшем времени — то есть его завёл пользователь в своей базе, и
     «что сделать» правки `src/seed/reference.py` не заказывает. Я добавил его **только в
     демонстрационную базу инструмента снимков** (`tools/` — моя полоса), иначе снимок
     критерия 7 нечем было бы снять. **Вопрос:** должен ли он приезжать в стартовом наборе
     новой базы? Канон rev 1.02 приводит его примером, и без него критерий 8 требует от
     пользователя сперва завести значение руками.

  4. **Пометка `No protocol` в карточке сделана подсказкой, а не колонкой.** §4 наряда
     оставил выбор за исполнителем «по снимку, а не по предположению». Отдельная колонка ради
     признака у одной записи из десяти — это счётчик там, где показано содержимое (тот же
     довод, которым наряд `0028` снял колонку `Inspections`). Подсказка стоит **и на строке,
     и на кнопке**: неактивная кнопка без объяснения читается как поломка.

  5. **Обязательность вывода показана звёздочкой в подписи** (`Conclusion *:`), а не только
     текстом отказа: оператор узнаёт о ней **до** сохранения. Приём в приложении новый —
     если для обязательных полей есть другая договорённость, скажи.

  6. Расхождений терминологии за наряд не встретилось.
---

## Доводка по ревью `0029` (Cowork, 2026-09-07)

Наряд **принят** по восьми исполнительским критериям. Проверено по диску: канон-хеш совпал,
главный тест — на уровне базы, `theme.py` описывает `QCheckBox::indicator`. Пять вынесенных
пунктов разобраны; работы требует один.

### Д-1 · `Tolerances review` въезжает в стартовый справочник — **делать**

Вопрос поставлен верно: канон rev 1.02 приводит этот тип примером законного случая, а
стартовый набор его не нёс. Значит первое же такое отклонение в новой базе упирается в ручной
ввод значения справочника, а критерий приёмки зависит от состояния базы, а не от кода.

**Канон правится, хеш двигается:**

- `docs/model/reference/reference-data.md` — rev 1.00 → **1.01**: стартовый набор типов
  исследований назван поимённо — `Solidworks assembly`, `Implantation torque test`,
  **`Tolerances review`**;
- `docs/model/Inspection.md` — строка «Fields · Type» приведена к тому же перечню.

**Сделать:** добавить `Tolerances review` в стартовый набор справочника (`src/seed/reference.py`),
там же, где заводятся два прежних типа. Демо-базу инструмента снимков привести к тому же
источнику, а не держать своё значение.

**Критерии:** новая пустая база несёт три типа исследований; тест перечня стартового набора
называет все три; снимок `Reference data` показывает их.

### Д-2 · Дефект отрисовки флажка — принято, и норма записана

Правка в `kit/theme.py` вынужденная и верная: подстиль `QCheckBox::indicator` не был описан, и
Qt рисовал платформенный по умолчанию — против этой палитры «ничего». Дефект жил с наряда
`0011` и ждал первого флажка в приложении.

Записано в `design-system.md` §4 (revision 1.10) как норма, а не как случай: **каждый вид
контрола, впервые появляющийся в приложении, — правка темы, а не экрана, и подписывается
снимком.** Ни один тест не утверждает «виджет виден» в том смысле, в каком это понимает
человек.

### Д-3 · Звёздочка обязательного поля — ратифицирована, и именно живая

Приём новый, договорённости не было. Утверждаю в том виде, в каком ты его сделал: **`*`
отражает обязательность в текущем состоянии формы**, появляется и исчезает вместе с условием.
Статическая пометка на всех обязательных полях поставила бы звёздочку почти везде и перестала
бы читаться; живая отвечает на единственный вопрос оператора в этот момент — что форма хочет,
прежде чем сохранит. Внесено в `design-system.md` §4.

### Д-4 · Пометка `No protocol` подсказкой, а не колонкой — принято

Довод тот же, которым снята колонка `Inspections`: отдельная колонка ради признака у одной
записи из десяти — счётчик там, где показано содержимое.

### Д-5 · Состав приехавшего разошёлся второй раз — исправлен **процесс**, не формулировка

Ошибка Cowork, и корень не в невнимательности. **Cowork не видит рабочего дерева**: shell на
машине у неё нет, `git status` недоступен. Она может перечислить только то, что писала сама, а
незакоммиченным лежит ещё и то, что осталось от прошлых заходов. Фраза «состав снят листингом
перед выдачей» была обещанием, невыполнимым по построению.

`docs/worklog/README.md` правлен: объявление состава становится **двусторонним** — Cowork
перечисляет свои правки и называет это перечнем, а не гарантией; **исполнитель на шаге 0
сверяет фактический состав своим `git status` и дописывает его в шапку наряда**, и эта запись
и есть источник истины. Расхождение перечня с фактом — не стоп, если лишнее вне `docs/model/`
и хеш сошёлся. Ответственность перенесена туда, где есть инструмент увидеть.

### Сопутствующие незакоммиченные пути

Перечень правок Cowork в этот заход (**перечень, не гарантия состава** — см. Д-5):
`docs/model/reference/reference-data.md` · `docs/model/Inspection.md` ·
`docs/design/design-system.md` · `docs/worklog/README.md` · `docs/decisions.md` ·
`docs/worklog/0029-inspection-without-protocol.md`. Фактический состав — за шагом 0.

### Канон-хеш доводки

- до правок доводки: `sha256:6441291af22d549125d49a2c29ac8e16e98f0d5e9329940372fdb3bef7baada1`
- после, ожидаемое: `sha256:0ceab244f0b8cc33a57302ad741123738b0516524cf7fcec21bbba8010be7463`

### Чего не трогать

- Схема, миграция `rev05`, `CHECK`, форма и карточка — приняты, не трогаются.
- `kit`: порог подсказки и `FIT_LABEL` — **QMS-022**. Правка `theme.py` в эту полосу не входила
  и утверждена отдельно (Д-2).
- `mapping_dialog.py` — **QMS-023**.
- Вопрос «где живёт решение» — открытый, не решается.


---
## Отчёт исполнителя по доводке (заполняет Claude Code)

- **Шаг 0.** Фактический состав снят своим `git status` и вписан в шапку: **шесть путей,
  совпал с перечнем Cowork, сверх перечня ничего.** Гард дал `CANON MOVED`, после коммита —
  `VERDICT: OK`, `canon hash HEAD` = `sha256:0ceab244…`, совпал с ожидаемым.

  **Отчёт по наряду цел.** Файл 29 087 байт, проверен по содержимому, а не по размеру:
  таблица восьми критериев на месте, все шесть отклонений на месте (последнее —
  «Расхождений терминологии за наряд не встретилось»). Восстанавливать нечего.

- **Сделано (Д-1):**
  1. **`Tolerances review` в стартовом наборе** — `src/seed/reference.py`, там же, где два
     прежних, с ссылкой на канон rev 1.01.
  2. **Демо-база снимков приведена к общему источнику** — своё `ensure_value` снято, тип
     приезжает стартовым набором.
  3. **Снимок** `01-section-1c-reference-inspection-types.png` — новый: `Inspection type ·
     3 values`, все три значения на экране.

- **Коммиты:**
  - `c135319` — `docs: canon rev 1.01 — Tolerances review в стартовом справочнике (QMS-024)` (шаг 0)
  - `3424a40` — `feat(seed): Tolerances review в стартовом наборе справочника (QMS-024, доводка 0029)`

- **Проверки против критериев доводки:**

  | Критерий | Результат |
  |---|---|
  | новая пустая база несёт три типа | `test_a_fresh_database_carries_those_three_and_nothing_else` — база поднимается миграциями **с нуля**, тем же путём, каким появляется у оператора |
  | тест перечня называет все три | `test_the_starting_set_of_inspection_types_names_all_three` — значения названы **дословно** |
  | снимок `Reference data` показывает их | `build/screens/01-section-1c-reference-inspection-types.png` |
  | канон-хеш | `VERDICT: OK`, `sha256:0ceab244f0b8cc33a57302ad741123738b0516524cf7fcec21bbba8010be7463` |
  | полный прогон | **659 passed** (на входе 657, добавлено 2) |

- **Отклонения / вопросы Cowork:**

  1. **Прежний тест стартового набора не сторожил ничего, и это стоит отметить отдельно.**
     `test_seed_fills_the_starting_sets` сверяет содержимое справочников **с самим
     `REFERENCE_SEED`** — он зелен при любом его содержимом, в том числе при пропаже
     `Tolerances review`. Ровно `CLAUDE.md` §9а.9: тест, разделяющий с кодом ту самую
     величину, которую проверяет. Прежний тест оставлен (он проверяет другое — приведение
     регистра и идемпотентность), а рядом встал тест, называющий три значения дословно.
     **Это, а не сама правка сида, было настоящей дырой:** пункт Д-1 сам по себе — одна
     строка, и без нового теста она бы ничем не охранялась.

  2. **Побочный эффект третьего типа, найденный снимком.** Демо-база адресовала вид
     исследования **по позиции** — `list_values(...)[-1]`, — и появление третьего значения
     молча переназначило вид второму исследованию: на снимке раскрытия вместо
     `Implantation torque test` встал `Tolerances review`, да ещё и с приложенным файлом,
     то есть ровно наоборот смыслу типа. Тот же §9а.9, только в инструменте. Переведено на
     адресацию по имени.

  3. **В демо-базу добавлена запись без протокола** — третьим исследованием той же находки.
     Без неё снимки показывали экран, снятый только на документированных записях, то есть не
     показывали того, что сделал наряд. Заодно на снимке раскрытия стало видно `+1 inspection`
     и `Researched · 3` — состояния, которых прежняя демо-база не давала. Это `tools/`, моя
     полоса; экраны не тронуты.

  4. **Д-5 принято к исполнению.** Фактический состав теперь снимается своим `git status` и
     пишется в шапку — сделано в этой доводке и будет делаться дальше. Замечу, что механизм
     уже сработал: перечень и факт **совпали**, и запись в шапке это фиксирует, а не просто
     утверждает.

  5. Расхождений терминологии за доводку не встретилось.
