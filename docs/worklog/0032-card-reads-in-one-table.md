# Наряд 0032 — карточка читается как одна таблица: высоты по содержимому, одна шапка, копируемое обоснование

- **Задача / цель:** `QMS-025`. Карточка отклонения перестаёт быть лоскутным одеялом из четырёх
  шапок. Высоту раздаёт содержимое, а не жёсткие коробки; две таблицы прецедентов сливаются в
  одну; шапка раскрытой панели становится подписью колонок, а не заголовком новой таблицы;
  обоснование копируется, не редактируясь.

- **Повод — прогон руками (2026-09-07), снимок карточки `DEV-260906-0001`.** Пользователь
  дословно: «две таблицы, две шапки отклонений плюс две шапки самих находок… это просто
  какое-то лоскутное одеяло, в котором разобраться сложнее, чем в обычной таблице». И: «до
  миллиметра вымерять позицию слайдера — это неправильно».

- **Диагноз Cowork, с числами.** Симптом — дублирующиеся шапки; причина — дефицит высоты,
  созданный жёсткими низами. `INLINE_TABLE_HEIGHT = 150` стоит низом у таблицы находок, у
  таблицы исследований и у области прецедентов сразу. Таблице находок с одной строкой нужно 70
  (шапка 30 + строка 40), она получает 150. Секция исследований пуста — таблица спрятана, но
  коробка с фразой и кнопкой держит около сотни. Области прецедентов гарантировано ровно
  столько, сколько хватает на две строки, — отсюда и вымеривание слайдера. Плюс горизонтальная
  прокрутка: таблица прецедентов объявлена на 1448 при `DIALOG_FULL = 1180`.

- **Вход:**
  - `docs/design/design-system.md` — **revision 1.13**, §7: «One level, one header» и
    «Only one region of a dialog stretches» — обе нормы написаны под этот наряд
  - `docs/decisions.md` — решения 114–119
  - `CLAUDE.md` — §9, §9а (в том числе §9а.21 о замере до и после), §3б, §3в
  - `src/ui/card_dialog.py` · `src/ui/common.py` (`FindingsPanel` после наряда `0031`) ·
    `src/ui/kit/tokens.py` (`INLINE_TABLE_HEIGHT`, `DIALOG_*`) · `src/ui/kit/widgets.py`
  - `src/domain/precedents.py` — `precedents_same_dimension`, `precedents_same_position`
  - `tools/grid_probe.py` — снимать замер тем же инструментом, что и в `0031`

- **Перечень правок Cowork в этот заход** (перечень, **не гарантия состава** —
  `worklog/README.md`): `docs/design/design-system.md` · `docs/decisions.md` ·
  `docs/worklog/0032-card-reads-in-one-table.md`. Фактический состав — за твоим `git status`
  на шаге 0, и его же впиши в шапку.

- **Ветка:** `main`. **Канон-хеш:** `docs/model/` не менялся, хеш обязан совпасть.

- **Тестов на входе: 689** (снято с твоего отчёта по `0031`, а не из прошлого наряда —
  решение 113).

---

## 1 · Высоту в карточке раздаёт содержимое

Жёсткий низ `INLINE_TABLE_HEIGHT` снимается со всех трёх мест, где он сейчас стоит, и заменяется
правилом «высота по содержимому» (`design-system.md` §7, «Only one region of a dialog
stretches»):

- **Таблица находок:** шапка плюс фактические строки. Одна находка — одна строка. Потолок —
  шесть строк, дальше прокрутка внутри таблицы; шесть, потому что дальше карточка перестаёт
  быть обзором.
- **Секция исследований:** когда исследований нет, она — **одна строка** компактного пустого
  состояния (§8), а не коробка с фразой посередине. Кнопка `Open protocol…` остаётся на своём
  месте и неактивной. Когда исследования есть — шапка плюс строки, потолок четыре.
- **Область прецедентов** объявляется **единственной растягивающейся**: она забирает всё, что
  осталось.

**Нижний предел области прецедентов задаётся видимым, а не пикселями:** две строки прецедента
плюс одна раскрытая панель целиком. Если при текущей высоте окна это не выходит — растёт
диалог (`DIALOG_HEIGHT_CARD` уже 960), а не сжимается сравнение.

## 2 · Горизонтальной прокрутки в области прецедентов нет

1448 объявленных против 1180 содержимого. Разницу снять с колонок, у которых **полный текст уже
лежит в подсказке**, — `Explanation` и `Characteristic` первыми: у них укорочение ячейки ничего
не теряет. Если двух не хватает, следующей идёт `Item`, и число называется в отчёте.

Считать помнящи, что **объявленная ширина — не нарисованная ширина** (§3 канона, плюс §9а.22 о
минимуме секции). Замер, не расчёт.

## 3 · Две таблицы прецедентов сливаются в одну

Сейчас вкладка `Exact precedents (L1)` держит `same_dimension` и `same_position` — две
`PrecedentTable`, у каждой своя шапка. Колонки у них одинаковые. По норме §7 «One level, one
header» это **одна таблица с групповыми строками**.

- Одна `PrecedentTable` на вкладку. Строки обеих выборок в ней подряд.
- Принадлежность к выборке несёт **групповая строка** во всю ширину — та же формулировка, что
  сейчас стоит подписью секции: «By canon: position g13 — other items and other revisions (1)».
  Она называет группу и считает её, чего шапка не умела.
- Группа с нулём строк показывается **своей групповой строкой с нулём**, а не исчезает: «по
  канону совпадений нет» — это ответ, а его отсутствие оператор прочтёт как «поиск не работал».
- Отклонение, попавшее в обе выборки, показывается в обеих группах. Это не дубль, а два разных
  ответа на два разных вопроса.

**Групповая строка — третий вид служебной строки** в этой таблице (после строки панели). Она не
выбирается, не ловится стрелками, не раскрывается. Потребителей таблицы пройти **листингом**
заново — правило `worklog/README.md`; в наряде `0031` этот пункт нашёл восемь потребителей, и
вставка второго вида служебной строки их снова касается.

## 4 · Шапка раскрытой панели — подпись колонок, а не заголовок таблицы

Шапка **остаётся**: семь колонок чисел без имён — ребус, и компактность, купленная снятием
имён, оплачивается ошибками чтения. Но выглядеть она обязана подчинённо (§7 канона): без
заливки, без линейки сверху, `12/500` цветом `n-500`.

Смысл правки — чтобы раскрытая панель читалась как **подробность строки**, а не как новая
таблица. Именно повтор оформления шапки, а не сам факт подписей, делает из карточки лоскутное
одеяло.

## 5 · Обоснование копируется, не редактируясь

Копируют не своё, а **чужое** — обоснование прецедента переносят в своё отклонение. Поэтому в
двух местах:

- **Поле `Explanation` в шапке карточки** становится выделяемым мышью и клавиатурой
  (`TextSelectableByMouse | TextSelectableByKeyboard`). Правка через него **не** появляется:
  запись идёт формой, как и была.
- **Обоснование прецедента** — кнопка `Copy explanation` рядом с `Open precedent…`, действует
  на выбранную строку; и `Ctrl+C` в таблице кладёт в буфер обоснование выбранной строки
  **целиком**, а не обрезанный текст ячейки.

Правка по месту в таблицах по-прежнему запрещена (§7 канона) — копирование ею не является.

## 6 · `Measurement point` скрывается с возможностью быстрой реанимации

Решение пользователя: статистики по нужности пока нет, а место колонка занимает. Поэтому —
**скрыть, не удалять**.

- Колонка убирается из показа **одним объявленным местом** (именованная константа рядом с
  описанием колонок, с комментарием «скрыта до набора статистики, наряд `0032`»), а не
  вычёркиванием из списка колонок.
- Поле модели, миграция, домен, форма находки, запись — **не трогаются вовсе**. Данные пишутся
  и хранятся как раньше.
- Возврат обязан стоить **одного изменения в одном месте**. Проверить это самому: вернуть,
  убедиться, что колонка на месте, вернуть обратно.

---

## Критерии приёмки

1. **Замер до и после** тем же `tools/grid_probe.py` (§9а.21): высоты секций карточки и
   области прецедентов, ширины колонок таблицы прецедентов. Числа — в отчёте.
2. Карточка с **одной** находкой: таблица находок высотой в одну строку; пустая секция
   исследований — одна строка; вся освободившаяся высота у области прецедентов. Замером.
3. Область прецедентов при высоте окна 760 показывает **две строки прецедента и одну раскрытую
   панель целиком**. Замером, не на глаз.
4. **Горизонтальной прокрутки в области прецедентов нет.** Сумма объявленных ширин, ширина
   содержимого, что и на сколько укорочено — в отчёте.
5. Одна таблица прецедентов вместо двух; групповые строки на месте; **группа с нулём строк
   показана**, а не пропущена.
6. **Тест настоящим кликом** (§9а.6, §9а.19): выбор не встаёт на групповую строку;
   `Open precedent…` и `Copy explanation` при ней неактивны; раскрытие групповой строкой не
   ломается.
7. Перечень пройденных потребителей `PrecedentTable` — заново, **листингом**, в отчёте.
8. Шапка панели подчинённого оформления; **снимок** карточки с двумя раскрытыми прецедентами —
   в `build/screens/`, поверх прежнего.
9. **Тест копирования:** `Ctrl+C` и кнопка кладут в буфер **полное** обоснование, не обрезанное
   по ячейке. Поле карточки выделяется и не редактируется.
10. `Measurement point` не показан; возврат стоит одного изменения — **показать в отчёте, каким
    именно**.
11. Полный прогон зелёный, число названо (на входе **689**). Прирост — пофайловым сбором.
12. **Прогон руками — за пользователем.**

## Чего не трогать

- **Список отклонений** — ни высот, ни сетки, ни панели. Панель общая после `0031`: правка
  оформления её шапки обязана быть **параметром**, а не сменой вида для обоих экранов.
  Список остаётся с прежней шапкой панели; доказать замером (критерий 1).
- **Модель, схема, миграции, домен** — в этот заход не меняются вовсе, включая
  `Measurement point`.
- **Вкладка `Descriptive (L2)`** — заглушка.
- **Правка по месту в таблицах** — запрещена каноном; копирование ею не является.
- **Импорт из Excel (S6)** — отдельный наряд после разведки.

---

## Отчёт исполнителя

- **Шапка.** Фактический состав doc-слоя на шаге 0 — **четыре** пути против трёх
  объявленных: `CLAUDE.md` · `docs/decisions.md` · `docs/design/design-system.md` ·
  `docs/worklog/0032-card-reads-in-one-table.md`. Лишний — `CLAUDE.md`, обычный остаток
  прошлого захода. Канон-хеш `sha256:d6e5ff2d…` **совпал**, прогнан дважды. Тестов на
  входе **689**, на выходе **698**. Все семь путей «Входа» проверены по диску.

- **Коммиты:**
  - `e16afd0` — `docs: наряд 0032 и канон к нему — приехавшее Cowork`
  - `3d828df` — `feat(kit): высота по содержимому, подчинённая шапка панели, пересчёт
    центрирования`
  - `8a9f57c` — `feat(ui): панель укладывается в узкую таблицу, Measurement point
    скрывается`
  - `764bc14` — `feat(ui): карточка читается как одна таблица`
  - отчёт — коммитом ниже.

- **Критерий 1 · Замер до и после** (`tools/grid_probe.py --card`, нативная платформа,
  окно 1180×960, отклонение **с одной находкой** — тот случай, о котором §1):

  | что мерим | до | после |
  |---|---|---|
  | таблица находок | **150** (нужно 70) | **70** |
  | коробка находок | 226 | **146** |
  | таблица исследований (пусто) | 130 | **0** |
  | коробка исследований | 112 | **92** |
  | область прецедентов | **214** | **314** |
  | сумма колонок прецедентов | 1448 | **1119** |
  | колонок за правым краем | **7** и **4** (две таблицы) | **0** |

  **Список отклонений не изменился ни на пиксель** — `grid_probe.py --diff` на том же
  замере, что и в `0031`: `СОВПАЛО: list-before.json == list-after.json`, 10 колонок,
  6 строк, 3 панели, все числа равны. Это и есть доказательство §4, что смена
  оформления шапки панели — **параметр**, а не смена вида для обоих экранов.

- **Критерий 2 · Высоту раздаёт содержимое.** Числа выше. Держит `kit.fit_table_height`;
  высота ставится **после наполнения**, а не при сборке — в конструкторе строк ещё нет, и
  посчитанная там высота была бы высотой пустой таблицы. Ровно на этом первая попытка и
  дала ноль: поймал замер, не тест. Сторожат
  `test_the_findings_table_is_as_tall_as_its_rows` (сверяется с арифметикой
  `kit.table_height`, а не с числом 70) и
  `test_an_empty_inspections_section_takes_no_table_height`.

- **Критерий 3 · Нижний предел области — видимым.** `precedent_floor()` = две строки
  прецедента (110) + групповая строка (32) + панель в самой высокой из трёх её высот (82)
  + полоса вкладок (34) = **258**. Замер при окне 760: область прецедентов ровно 258, то
  есть предел держится; ужимается шапка (282 → 198), а не сравнение. Сторожит
  `test_the_precedents_area_is_the_only_region_that_stretches` — сверяет и то, что
  соседние секции фиксированы, то есть растягивается **одна** область.

- **Критерий 4 · Горизонтальной прокрутки нет.** До наряда за правым краем стояли **7**
  колонок в одной таблице и **4** в другой; после — **0**.

  **Двух колонок не хватило, и это замер, а не оценка.** Потребность колонки считалась как
  `max(заголовок, самое длинное реальное значение) + 27` непечатаемого; сумма потребностей
  вышла **1373** — уже больше полотна. Значит вопрос стоял не «где взять 312», а «как
  разложить 1120», и назначать пришлось **все** ширины:

  | колонка | было | нужно | стало | отчего так |
  |---|---|---|---|---|
  | (раскрыватель) | 30 | — | **30** | §3 наряда `0031`, не трогается |
  | `Deviation` | 153 | 131 | **132** | потребность плюс округление |
  | `Date` | 132 | 89 | **92** | то же |
  | `Item` | 125 | 89 | **92** | то же; наряд назвал её третьей |
  | `Revision` | 68 | 75 | **68** | `FIT_LABEL`; недобор 7 px — **QMS-022** |
  | `WO` | 125 | 95 | **96** | потребность плюс округление |
  | `Characteristic` | 230 | 195 | **120** | обрезается, полный текст в подсказке |
  | `Sign · value` | 118 | 93 | **96** | потребность плюс округление |
  | `Decision` | 150 | 85 | **150** | рисует пилюля, ей нужна оправа |
  | `Explanation` | 270 | 440 | **196** | обрезается, полный текст в подсказке |
  | `Insp.` | 47 | 54 | **47** | `FIT_LABEL`; тот же недобор, **QMS-022** |
  | **сумма** | **1448** | 1373 | **1119** | полотно **1120** |

  `Revision` и `Insp.` оставлены `FIT_LABEL` намеренно, хотя замер показывает у обеих
  недобор в 7 px: это известный дефект формулы, общий для всех экранов, и он ваш
  (**QMS-022**), а не мой.

- **Критерий 5 · Одна таблица, групповые строки, группа с нулём.** Сторожит
  `test_a_group_with_no_matches_is_still_shown`: по номеру совпадений нет, по канону одно,
  **обе** группы на экране, счётчики `(0)` и `(1)`. Видно и на снимке.

- **Критерий 6 · Тест настоящим кликом.** `test_the_group_row_is_not_a_precedent`: выбор
  ставится настоящим событием, строка распознана служебной, `selected_deviation()` даёт
  `None`, **обе** кнопки погашены, клик по раскрывателю групповой строки ничего не
  раскрывает и ничего не роняет. Четыре утверждения одним тестом, потому что различает
  верное от неверного именно их сочетание.

- **Критерий 7 · Потребители `PrecedentTable` — листингом заново.** В `0031` их было
  восемь; второй вид служебной строки коснулся их всех и добавил новых.

  | потребитель | что стало |
  |---|---|
  | `selected_deviation` | `None` на **обоих** видах служебной строки — через `is_service_row` |
  | `is_service_row` | новый: один вопрос вместо двух; разводить их по местам вызова значило бы заводить второй шанс забыть про один — на таком пропуске в `0029` вырос `TypeError` доводки 3 |
  | `open_precedent` | берёт единственную таблицу; аргумент оставлен ради прежних точек вызова |
  | `_current_table`, `_on_table_selected`, `_active_table`, `_syncing_selection` | **удалены** вместе со второй таблицей: они существовали только чтобы кнопка не открывала «первую непустую» из двух |
  | `_refresh_open_button` | гасит **обе** кнопки; признак берёт у той же `selected_deviation`, которая потом и открывает |
  | `fill` / `_render` | `fill` принимает **группы**; `_render` раскладывает группы, строки и панели |
  | `toggle_expansion` | ключ — пара «группа · отклонение»; групповая строка не раскрывается |
  | `_stretch_panels` | укладывает панель по полотну и **пересчитывает центрирование** |
  | `resizeEvent` | новый: перекладывает панели по новой ширине (см. отклонение 2) |
  | `groups()` / `rows_of()` | новые: полный текст обоснования и состав выдачи берут отсюда, а не из ячейки |
  | тесты `test_ui_card`, `test_ui_item_card`, `test_ui_revision`, `test_ui_screens_0011` | **33 упавших**; обращения по номеру строки переведены на адресацию по группе и по номеру отклонения |
  | `tools/screenshots.py` | считает группы, раскрывает пропуская **оба** вида служебных строк |

  **Что листинг дал сверх ожидаемого.** Два теста — «двойной клик во второй секции» и
  «кнопка берёт последнюю тронутую таблицу» — потеряли **предмет**: они стерегли выбор
  между двумя таблицами, а таблица теперь одна. Заменены одним
  `test_the_row_that_was_chosen_is_the_row_that_opens`, который сторожит то же требование
  на двух группах: открывается ровно та запись, на которой стоит выбор.

- **Критерий 8 · Шапка панели и снимок.** `build/screens/23-card-precedents-expanded.png`
  поверх прежнего: одна таблица, две групповые строки со счётчиками, два раскрытых
  прецедента, шапки панелей без заливки и линеек. Сторожит
  `test_the_panel_header_is_subordinate_in_the_card_and_not_in_the_list` — **сравнением
  экранов**: в карточке имя объекта подчинённое, в списке прежнее, сетка при этом общая.

- **Критерий 9 · Копирование.** `test_the_explanation_of_a_precedent_is_copied_in_full`:
  различающая величина — **разница** между текстом ячейки и текстом в буфере, поэтому
  обоснование в тесте многострочное: ячейка складывает абзацы в одну строку, буфер
  получает исходный текст. `test_ctrl_c_copies_the_same_text_as_the_button` сверяет два
  входа **между собой**, а не каждый сам с собой.
  `test_the_explanation_of_the_card_is_selectable_but_not_editable` — обе половины сразу:
  выделяется и **не** правится.

- **Критерий 10 · `Measurement point`.** Скрыта одним объявленным местом —
  `HIDDEN_FINDING_COLUMNS` в `deviation_dialog.py`, рядом с описанием колонок.
  **Возврат стоит одного изменения в одном месте, и это проверено исполнением:**

      как есть:      [… 'Deviation type', 'Outcome', 'Inspections']
      кортеж пуст:   [… 'Deviation type', 'Measurement point', 'Outcome', 'Inspections']
      вернули назад: [… 'Deviation type', 'Outcome', 'Inspections']

  Ни списки ширин, ни расчёт индексов, ни сборка строк о сокрытии не знают — они
  по-прежнему перечисляют все колонки. Модель, миграция, домен и форма находки не тронуты.
  Сторожит `test_the_measurement_point_column_is_hidden_not_removed` — **обе** половины:
  колонки не видно, но объявление на месте; проверка только первой была бы зелёной и на
  сборке, где колонку вычеркнули.

- **Критерий 11 · Прогон зелёный: 698** (на входе 689), без `-k` и `--ignore`. Прирост
  сверен пофайловым сбором: `+11` новых, `−2` потерявших предмет.
  `tools/screenshots.py` — «обрезанных колонок: 0».

- **Критерий 12 · Прогон руками** — за пользователем.

- **Отклонения / вопросы Cowork:**

  1. **Три ошибки наблюдения подряд, и все три — про `isVisible()`.** Стоит того, чтобы
     назвать классом.
     (а) `kit.centring_margin` вычитал ширину вертикальной полосы **по `isVisible()`**, а
     у ребёнка непоказанного окна оно ложно всегда (§9а.5). Отступ считался так, будто
     полосы нет: на раскрытой таблице полотно ужималось до 1054, отступ оставался прежним,
     и колонка уезжала за край при сумме, не менявшейся ни на пиксель. Правка в `kit`:
     признак — `maximum() > 0`, «полосе есть что прокручивать», и он верен и до показа.
     (б) Замер прокрутки: `isVisible()` ложно всегда, `isHidden()` истинно только у явно
     спрятанного, сравнение суммы с полотном не значит ничего (kit подгоняет отступы
     полотна под сумму). Единственная честная величина — `maximum` полосы, и она у
     `QTableView` считается **в колонках**, а не в пикселях.
     (в) Сама сумма колонок наблюдалась неверно, пока замер не начал делать **проход
     отрисовки**: без него полотно отдавало 638 px вместо 1136 — вложенная в прокрутку
     вкладка своей ширины ещё не знала.

  2. **Панель укладывается на `resizeEvent`, и без этого её не видно целиком.** Ширина
     панели считается от полотна, а полотно к моменту вставки строки своего размера ещё не
     знает: раскрытие укладывало панель по устаревшему числу, и на снимке она выходила
     короче таблицы на треть, **теряя две последние колонки**. Замер после доводки
     раскладки при этом показывал верные числа — то есть дефект был виден только картинкой.
     Флаг от рекурсии обязателен: укладка двигает отступы полотна, а это снова
     `resizeEvent` (§9а.15).

  3. **Сетка панели (1144) шире таблицы прецедентов карточки (1119).** Разницу добирает
     последняя колонка `Inspections` (`FindingsPanel.fit_to`) — у неё полный текст уже в
     подсказке. Это **новое** поведение общей панели, и я объявляю его как решение
     исполнителя: наряд про ширину панели не говорил, а без укладки §4 не выполняется
     вовсе. Если такой эластичности у общей панели быть не должно — скажите, альтернатива
     одна: своя узкая сетка для диалога, то есть второй набор чисел.

  4. **`Explanation` ужалась с 270 до 196 px** — это цена отказа от горизонтальной
     прокрутки при ширине карточки 1180, и цену платит главный текст прецедента. Полный
     текст остаётся в подсказке и в `Copy explanation`, но читать его в строке стало
     труднее. Настоящее лечение — шире окно (`DIALOG_FULL` 1180 при 11 колонках тесно),
     и это ваше решение, а не моё.

  5. **Пустая секция исследований — 92 px, а не «одна строка».** Кнопка `Open protocol…`
     обязана остаться (§1 наряда прямо это говорит), и вместе с ней строка объяснения даёт
     92 px: я поставил их **в одну строку**, кнопка первой, чтобы место действия не
     зависело от наличия данных. Меньше без снятия кнопки не выходит.
