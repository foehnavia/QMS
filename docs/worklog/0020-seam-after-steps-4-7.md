---
part_of: MIS-QMS/docs/worklog
order: 20
task: QMS-016
branch: run/qms-016
status: rework
issued: 2026-09-03
author: Cowork
executor: Claude Code
---

# 0020 — шов после шагов 4–7: накопленный долг одним заходом

> Наряд цикла QMS-016. Ветка `run/qms-016`, в `main` не писать ничего.
> Основание — долг к шву, `RUN_PROTOCOL_QMS-016` §6, и журнал
> `docs/worklog/0006-run-qms-016.md`.
> **Блокирующих находок здесь нет.** Шов взят раньше плана (решение пользователя 2026-09-03):
> шаги 9–14 — это экраны прецедентов, и судить их на тесных таблицах значит плодить находки про
> ширину вместо находок про функцию.

## 1. Рамка

Долг копился с 02.09 и трижды отодвигался блокирующими находками (`0016`–`0019`). Собран здесь
целиком. Порядок разделов — от того, что видно на каждом экране, к тому, что видно в одном.

**Границы записи (INFRA-013).** Этот наряд — **только код и тесты**. Репо-`CLAUDE.md` §9,
спеки `docs/specs/` и реестр решений ведёт Cowork; исполнителю их править не нужно, а
формулировки, которые туда пойдут, названы здесь, чтобы код и документ не разошлись.

## 2. Вход (проверить существование первым действием)

| Путь | Что это |
|---|---|
| `docs/worklog/0006-run-qms-016.md` | журнал прогона, находки №6, №10, №11, №15, №16 и ревью |
| `src/ui/kit/widgets.py` · `src/ui/kit/tokens.py` · `src/ui/kit/theme.py` | таблицы, размеры, лист стиля |
| `src/ui/mapping_dialog.py` | привязка: «Done», ярлык позиции, редактор строки |
| `src/ui/item_view.py` · `src/ui/deviation_dialog.py` · `src/ui/cg_view.py` · `src/ui/card_dialog.py` | четыре вызова привязки |
| `src/ui/item_dialog.py` | `run()` — запрет заведения |
| `src/ui/cg_editor.py` | редактор группы: полноэкранный разворот, ASCII-минус, знак у нуля |
| `src/ui/item_positions_dialog.py` | словарь состояния привязки, вторая сторона |
| `src/domain/reference.py` | регистр значений справочников |
| `tests/test_ui_kit.py` · `tests/test_ui_cg.py` · `tests/test_ui_smoke.py` | покрытие |

## 3. Что сделать

### 3.1 Ширина таблиц — предел у каждой колонки (№10, №11)

Решение пользователя 02.09, разобранное и уточнённое им же. Правится **умолчание
`kit.data_table`**, а не шестнадцать экранов.

* **Класс содержимого вместо ширины на колонку.** Колонка объявляет класс, класс несёт предел в
  знаках: идентификатор ≈ 10 · величина ≈ 8 · дата ≈ 12 · состояние/короткий ярлык ≈ 14 ·
  счётчик ≈ 6 · свободный текст (объяснение, ивритское заключение) ≈ 60 · ссылки ≈ 40. Числа —
  ориентир, подстраиваются по месту; важно, что ширина становится свойством **типа данных**, а
  не конкретного экрана.
* **Ни одна колонка не тянется.** Ширина таблицы = сумма колонок и от размера окна не зависит.
* **Свободный текст остатка не забирает** — у него тоже предел, просто щедрый (правка
  пользователя: колонка-остаток возвращает растягивание на всё окно, то есть ту самую болезнь).
* **Обрезка обязательно с подсказкой** — предел без усечения по многоточию и полного значения в
  подсказке превращается в потерю данных на экране.
* **Центрирование с деградацией.** Таблица уже своей области → по центру, лишнюю ширину забирают
  отступы. Сумма колонок шире области → таблица занимает всю ширину и прокручивается вбок,
  отступы исчезают. Без второй половины сломались бы широкие экраны.
* **Отступы — роль поверхности, а не подкрашенный фон:** «утопленная» (`surface-sunken`),
  приглушённый серо-синий, ощутимо темнее белой карточки и бледнее акцента. Значение
  подбирается на экране; важно, что это **роль канона**.
* **Видимые границы колонок** — минимальные разделители, чтобы клик по ячейке был уверенным.

> [!warning] **Выравнивание канона не меняется.** Первое предложение сессии — центрировать
> значения — пользователь разобрал и отклонил: первопричина в ширине, а не в выравнивании.
> Величины остаются вправо, идентификаторы и счётчики влево (канон §6).

### 3.2 Привязка: один вход вместо четырёх вызовов (Р-1/0018)

`warn_incomplete_mapping` стоит только на `item_view.py:217`. Молчат `deviation_dialog.py:527`
(ранняя привязка R2), `cg_view.py:168` («Mapping…» из раздела групп), `card_dialog.py:594`
(привязка из карточки). Одна работа, четыре места, разное поведение.

Все четыре проводятся через **один** помощник — тот, что уже написан. Новых развилок не
заводить: если какому-то из входов предупреждение не нужно, назвать это в отчёте, а не решать
молча.

### 3.3 «Done» становится проверяющей, а не заблокированной (№15)

Сейчас полнота закодирована в **доступности** кнопки. Оператор набирает номер в последнюю
позицию и по мышечной памяти тянется к кнопке — она мертва, потому что набранное лежит в
открытом редакторе ячейки и в состояние ещё не ушло.

Новое поведение: кнопка **всегда доступна**; нажатие (1) закрывает открытый редактор ячейки,
чтобы набранное ушло в состояние, (2) спрашивает полноту, (3) если позиции остались — называет
их и окно не закрывает.

**Ратифицировано Cowork:** это меняет конвенцию S3 «„Done“ включается по полноте» — способ, не
смысл. Смысл (подтвердить, что ни одна позиция не забыта) сохраняется. Тем же движением
снимается **Р-2/0018**: у пустой группы кнопка сейчас мертва навсегда.

### 3.4 Полноэкранный разворот окон с чертежом (№16)

Диалог на Windows кнопки разворота по умолчанию не получает. Разрешить явно
(`Qt.WindowMaximizeButtonHint` либо открытие развёрнутым) — **привязке и редактору группы**.
Чертёж — рабочая поверхность, а не иллюстрация.

### 3.5 Мелочи, накопленные с 02.09

* **№6 · регистр значений справочников.** Первая буква приводится к заглавной при сохранении;
  уже введённые нормализуются. Аббревиатуры (`NP`, `C1`, `IntHex`) правило не задевает —
  трогается только первый символ, если строка не выглядит аббревиатурой.
* **Р-1 (старый) · правка занятой позиции прямо в строке** — сейчас отбивается; разрешить.
* **Р-3 · три написания ярлыка позиции.** Единое: **`g13`** — строчная, как в каноне модели и в
  спеках. Метка конструктора на чертеже (`G13`) отличается только регистром и читается как тот
  же идентификатор; менять канон ради регистра дороже, чем привыкнуть. Применяется везде:
  привязка, карточка, позиции детали, редактор группы.
* **Р-4 · ASCII-минус в редакторе, канонический в показе** — привести к одному.
* **Р-5 · знак у нуля.** По ISO 286 нулевое отклонение пишется **без знака**; убрать `+0`.
* **Р-1/0019 · скругление индикатора считать от рисуемой коробки**, а не от содержимого:
  сейчас радиус выведен из `INDICATOR_SIZE`, а коробка больше на две рамки. Верно ровно при
  `BORDER_WIDTH = 1`.
* **Словарь состояния привязки сводится** к трём словам в обоих диалогах: `linked` ·
  `absent (99)` · `undecided`.

### 3.6 Тесты

* **Пиксельные тесты самодостаточны.** `test_the_choice_marks_the_selected_option` зависит от
  того, поставил ли лист стиля соседний тест: запущенный в одиночку он мерит родной стиль
  Windows. Тест, зависящий от порядка запуска, не проверка, а лотерея.
* **Дымовое покрытие внутрь диалогов.** Сейчас оно закрывает четыре раздела; действия **внутри**
  диалогов по-прежнему зовутся по имени метода, а не нажатием. Дефект того же класса там
  возможен.
* **События доставляются через приложение**, а не прямой посылкой в виджет — во всех тестах, где
  проверяется поведение, а не обработчик.

## 4. Чего НЕ трогать

- Схему, миграции, `main`.
- Выравнивание канона (§3.1) — решение пользователя.
- Механики эталонного экрана (**Q-14**): поиск, срезы, чипы фильтров, выбор колонок, экспорт,
  раскрытие строки в три уровня. Их отсутствие — не дефект; заводятся отдельной задачей после
  прогона.
- Репо-`CLAUDE.md`, `docs/specs/`, `docs/decisions.md` — слой Cowork.
- Контракт `FilterCombo` и подписи `run(...)`.

## 5. Критерии приёмки

1. Ни одна колонка не тянется; ширина таблицы — сумма колонок; таблица центрируется, отступы —
   роль «утопленная поверхность»; обрезка с подсказкой; границы колонок видимы. Проверено
   **снимками** на трёх экранах: список отклонений, карточка (обе секции прецедентов), привязка.
2. Выравнивание прежнее: величины вправо, идентификаторы и счётчики влево.
3. Все четыре вызова привязки ведут себя одинаково; расхождения, если оставлены, названы.
4. «Done» доступна всегда; нажатие с незакрытыми позициями называет их и окно не закрывает;
   набранное в последней ячейке засчитывается **без** `Enter`. Проверяется нажатием, не
   свойством.
5. Привязка и редактор группы разворачиваются на полный экран.
6. Регистр справочников, ярлык `g13`, ASCII-минус, ноль без знака, словарь состояний — по §3.5.
7. Индикатор радиокнопки остаётся кругом при `BORDER_WIDTH = 2` (проверить временно подняв
   токен, вернуть обратно).
8. Пиксельные тесты проходят **поодиночке**; дымовое покрытие достаёт до действий внутри
   диалогов.
9. Тесты зелёные, число назвать явно (было 466).

> [!note] Прогон руками (человек за клавиатурой) по §3.3 и §3.1 делает **оператор** после
> сдачи — как в наряде `0019`. Исполнитель закрывает то, что закрывается снимками и тестами, и
> прямо говорит, что остаётся за оператором.

## 6. Отчёт исполнителя

### Входной контроль

Все двенадцать строк раздела «Вход» проверены **до начала работы**; за ними шестнадцать
файлов, и все существуют: `docs/worklog/0006-run-qms-016.md` · `src/ui/kit/widgets.py` ·
`src/ui/kit/tokens.py` · `src/ui/kit/theme.py` · `src/ui/mapping_dialog.py` ·
`src/ui/item_view.py` · `src/ui/deviation_dialog.py` · `src/ui/cg_view.py` ·
`src/ui/card_dialog.py` · `src/ui/item_dialog.py` · `src/ui/cg_editor.py` ·
`src/ui/item_positions_dialog.py` · `src/domain/reference.py` · `tests/test_ui_kit.py` ·
`tests/test_ui_cg.py` · `tests/test_ui_smoke.py`.

### Сделано

**§3.1 · Ширина — свойство класса содержимого.** Семь классов в `tokens`
(`WIDTH_IDENTIFIER` … `WIDTH_LINK`), таблица классов и определитель по подписи — в
`kit.widgets`. Экран может назвать класс сам (`content=`), и один уже назвал: у привязки
`Canon geometry` — не свободный текст, а компактная составная ячейка, по имени это не
угадывается.

Числа подобраны **замером строк в шрифте канона**, а не на глаз: `DEV-260903-0001` — 14.9
знакоместа, отсюда идентификатор 16; `+0.05 / −0.05` — 11.3, отсюда величина 12; пилюля
`Not decided` с её отступами — отсюда состояние 16. Колонка не сужается ниже собственной
подписи: на первом снимке `Inspections` читалось как `spectior`.

Ни одна колонка не тянется (`Interactive`, `stretchLastSection` снят). Полотно белое, поле
вокруг — роль `SURFACE_SUNKEN`; границы колонок видимы. Обрезка объясняется подсказкой:
делегат отвечает полным текстом ровно тогда, когда текст не поместился.

**Центрирование сделано отступом листа стиля**, и это не первый вариант:
`setViewportMargins` Qt держит под свои заголовки, и правка разводила шапку с телом (видно
снимком). Отступ — часть коробки виджета, полотно и шапка едут вместе; селектор ограничен
типом, иначе отступ наследуют дети и шапка получает его вторично.

**§3.2 · Один вход вместо четырёх.** `ui.item_dialog.open_mapping` — все четыре кнопки
привязки (экран деталей, ранняя привязка формы отклонения, раздел групп, карточка) теперь
ведут себя одинаково. Расхождений не оставлено.

**§3.3 · «Done» проверяет.** Кнопка доступна всегда; нажатие сперва **закрывает открытый
редактор ячейки** (`commitData` + `closeEditor`), затем спрашивает полноту и называет
оставшиеся позиции, не закрывая окна. Тем же движением снят Р-2/0018: у пустой группы кнопка
была мертва навсегда.

**§3.4 · Разворот.** `WindowMaximizeButtonHint` у привязки и редактора группы.

**§3.5 · Мелочи.** Регистр справочников (`capitalised`, аббревиатуры нетронуты) ·
`g13` одним ярлыком через `position_label`/`position_index` — и показ, и разбор в одном месте ·
ASCII-минус в редакторе заменён каноническим · ноль пишется без знака · радиус индикатора
считается от рисуемой коробки · словарь состояний сведён к `linked` · `absent (99)` ·
`undecided` · правка занятой позиции в строке стала перепривязкой (снять и поставить в одной
транзакции).

**Регистр вскрыл настоящую дыру, а не только вид.** Проверка дубля была регистрозависимой:
`thread root` и `Thread root` уживались в одном справочнике. Сделана регистронезависимой, и
следом — поиск `ref()`; заодно четыре одинаковых помощника «найти или завести» по тестам и
инструментам сведены в доменный `ensure_value`.

**§3.6 · Тесты.** Пиксельные применяют тему сами и проходят поодиночке. Дымовое покрытие
достаёт **внутрь диалогов**: восемь форм, каждая кнопка нажимается, исключения слотов ловятся
`sys.excepthook`. События доставляются через приложение.

### Критерии приёмки

| # | Критерий | Состояние |
|---|---|---|
| 1 | Колонки не тянутся, ширина — сумма, центрирование, обрезка с подсказкой, границы | ✅ на списке отклонений и в карточке (обе секции); **частично** в привязке — см. «замеченное», п. 1. Снимки: `01-section-4-deviations.png`, `11-dialog-card.png`, `05-dialog-mapping.png` |
| 2 | Выравнивание прежнее | ✅ не трогалось: делегат направления не менялся, `magnitude_columns` те же |
| 3 | Четыре вызова привязки ведут себя одинаково | ✅ все через `open_mapping`; расхождений нет |
| 4 | «Done» доступна всегда, называет пробел, засчитывает набранное без `Enter` | ✅ **нажатием**: `test_done_is_always_available_and_names_what_is_missing`, `test_done_counts_what_was_typed_without_enter` |
| 5 | Привязка и редактор группы разворачиваются | ✅ `test_the_drawing_windows_can_be_maximised` |
| 6 | Регистр, `g13`, ASCII-минус, ноль без знака, словарь | ✅ снимки `12-dialog-item-positions.png` (`g1`, `linked`, `Limit deviations`) и `04-dialog-cg-editor.png`; тесты `test_the_position_label_is_the_same_everywhere`, обновлённые ожидания справочника |
| 7 | Индикатор остаётся кругом при `BORDER_WIDTH = 2` | ✅ проверено подъёмом токена: тест зелёный при 1 и при 2, токен возвращён |
| 8 | Пиксельные тесты проходят поодиночке; дымовое покрытие внутрь диалогов | ✅ `-k indicator` в одиночку зелено; `test_every_dialog_action_survives_being_pressed` — восемь диалогов |
| 9 | Тесты зелёные, число названо | ✅ **476 тестов** (было 466) |

### Что остаётся за прогоном руками

Прямо: **критерии 1 и 4 в части «как это ощущается» закрываю снимками и тестами, но не
руками** — человека за клавиатурой у меня нет (определение §8.3 наряда 0019).

Оператору на прогоне:

1. **§3.3 — «Done» на живой сборке.** Набрать номер в последнюю позицию и, **не нажимая
   `Enter`**, нажать «Done»: окно обязано закрыться. С незакрытой позицией — остаться открытым
   и назвать её.
2. **§3.1 — ширины на реальных данных.** Снимки сняты на демонстрационной базе; на
   производственном наборе (длинные ивритские заключения, номера с суффиксами) пределы классов
   могут потребовать подстройки — числа на то и объявлены в одном месте.
3. **§3.4 — разворот окна** проверяется мышью за заголовок: у диалога это свойство окна, и
   тест видит только флаг.

### Число тестов

**Было 466 → стало 476**, прирост +10:

* `tests/test_ui_kit.py`: четыре теста §3.1 — колонки не тянутся, класс несёт предел,
  центрирование с деградацией, подсказка у обрезанного значения.
* `tests/test_ui_cg.py`: пять — «Done» доступна и называет пробел, «Done» засчитывает
  набранное без `Enter`, перепривязка в строке, разворот окон, один ярлык позиции.
* `tests/test_ui_smoke.py`: один — дымовое покрытие внутрь диалогов (восемь форм, каждая
  кнопка нажимается).

Переписаны без изменения числа: ожидания регистра справочников (шесть файлов), ярлык `g1`
вместо `1` в трёх тестах редактора, ноль без знака, «Done» больше не заблокирована.

### Коммиты

* `feat(ui):` наряд 0020 — шов: ширины классами, один вход в привязку, «Done» проверяющая.
* `docs(worklog):` этот отчёт.

### Замеченное по ходу — Cowork

1. **Центрирование не доезжает до таблиц внутри разделителя, и причина не в замысле.**
   `QWidget::setGeometry` в Qt **не виртуальный**: раскладка зовёт его в C++, мимо питоньей
   замены, поэтому пересчёт висит на `resizeEvent` и `showEvent`. Таблица привязки лежит внутри
   `QSplitter` и до показа настоящей ширины не получает; на снимке она не отцентрирована.
   Пересчёт на отрисовке пробовал — он ломает шапку (отступ меняется после того, как она
   разложена). Чинится либо переносом центрирования в раскладку набора (`kit.column` кладёт
   таблицу в центрирующий контейнер), либо тем, что таблица сама держит максимальную ширину.
   Не стал: это правка компоновки всех экранов, а наряд просил умолчание.
2. **`self.size` в форме детали перекрывал `QWidget.size()`** — всякий вызов размера у этого
   диалога падал «QComboBox is not callable». Поймано инструментом снимков, переименовано в
   `size_class`. Стоит держать в голове при следующих формах: имена полей формы живут в одном
   пространстве с Qt-API.
3. **Регистр справочников — не косметика.** Проверка дубля была регистрозависимой, то есть до
   этого наряда `thread root` и `Thread root` могли лежать рядом как разные значения. Правка
   заодно нашла четыре копии «найти или завести» по тестам и инструментам.
4. **Класс «свободный текст» нельзя угадывать по имени.** `Canon geometry` попала в него по
   слову «geometry» и растянула привязку за край экрана. Определитель по подписи — умолчание,
   а не правило; экраны с составными ячейками объявляют класс сами. В канон вида это стоит
   записать именно так.
5. **Порог «короткого списка» и семь классов ширины — материал для канона вида** (§3.4 наряда
   0019 остался незаписанным). Числа сейчас живут только в `tokens`.


---

## 7. Доводка — §3.1 ОТКЛОНЁН (Cowork по проверке оператором, 2026-09-03)

> Приёмка §3.1 отзывается. Остальные разделы наряда (§3.2–§3.6) в силе и приняты.

### 7.1 Что видно на экранах (ФАКТ, четыре снимка)

| Экран | Колонка | Что не так |
|---|---|---|
| Reference data | `Value` | «Connect Abutment», «Straight Multy-Unit», «Cutting-edge width», «Inner bore depth» **переносятся на две строки** |
| Reference data | `Used by` | «1 record» обрезано до «1 recor…» — обрезан **заголовочный по смыслу** текст в шесть знаков |
| Reference data | вся таблица | поле по бокам **шире самой таблицы**: центрирование выглядит поломкой, а не приёмом |
| Items | `Connection`, `Size class` | заметно **шире содержимого** (`C1`, `SP`, `General`) |
| Items | `Item type`, `Groups` | **уже содержимого**: «Connect Abutment» и «C1 SP375 Int. Con. Zone» переносятся |
| Characteristic groups | `Group` | переносится; `Drawing` («yes») при этом широкая |

Общий итог оператора: «ничего не попало в нужную длину; половина обрезана, половина слишком
узкая; двухстрочные ячейки там, где хватает одной».

### 7.2 Почему так вышло

**Класс определяется по подписи колонки — то есть угадыванием.** Исполнитель сам назвал слабое
место в отчёте: `Canon geometry` по имени попала в «свободный текст». Тот же механизм развёл
`Connection` (короткое содержимое, широкий класс) и `Item type` (длинное содержимое, узкий
класс). Догадка по имени работает, пока имена не врут, — а они врут.

**Заголовок не учтён как содержимое.** `Characteristics` — пятнадцать знаков, `Measurement
point` — семнадцать; колонка не может быть уже собственного заголовка, иначе он переносится.

**Перенос вообще разрешён.** Ячейка обязана быть однострочной: длинное режется с подсказкой, а
не разъезжается вниз.

### 7.3 Что сделать — ширина объявляется поимённо

**Решение пользователя: хватит классов по догадке.** Класс остаётся умолчанием для колонки,
которая ничего не объявила; **каждый экран объявляет ширину своих колонок явно**.

Три правила, обязательных для всех:

1. **Ширина ≥ ширины заголовка.** Заголовок не переносится никогда; это нижняя граница.
2. **Ячейка однострочная.** Перенос выключен, длинное — многоточие плюс подсказка с полным
   значением.
3. **Центрирование только когда таблица занимает не меньше ~60 % области.** Уже — таблица
   прижимается к левому краю. Поле шире самой таблицы читается как поломка, а не как приём.

**Формула ширины (решение пользователя 2026-09-03):**

> **ширина = max(заголовок, самое длинное реальное значение) × 1.25**, округление вверх.

Запас в четверть — не перестраховка, а требование: значения будут длиннее уже завтра. Названия
групп характеристик отличаются цифрами и добавочными словами, и стоит внести в имя состояние —
`C1 SP375 Int. Con. Zone` вырастет ещё. Поэтому у **текстовых** колонок база берётся не по
сегодняшнему рекорду, а по нему **плюс место под добавочное слово**, и уже к этому применяется
четверть.

Колонки свободного текста (`Explanation`) под формулу не идут: у них предел с обрезкой, иначе
таблица уедет за экран.

**Ширины по колонкам** (знакоместа шрифта канона; в скобках — от чего считано):

| Экран | Колонка | Ширина | База |
|---|---|---|---|
| **Reference data** | Value | **26** | `Straight Multy-Unit` 19, `Internal Connection` 19 → +слово → ×1.25 |
| | Used by | **12** | `2 records` 9 → ×1.25 |
| | State | **12** | заголовок 5, значения короткие |
| **Items** | Item number | **15** | `MF5-10375A-N` 12 → ×1.25 |
| | Item type | **24** | `Straight Multy-Unit` 19 → ×1.25 |
| | Connection | **13** | **заголовок 10** — он и есть база, содержимое короче |
| | Size class | **13** | **заголовок 10** — то же |
| | Characteristics | **16** | **заголовок 15** — уже минимум, ужать нечем |
| | Groups | **36** | `C1 SP375 Int. Con. Zone` 23 + добавочное слово ≈ 29 → ×1.25 |
| **Characteristic groups** | Group | **36** | то же, что `Groups` — одно и то же значение |
| | Positions | **11** | заголовок 9 |
| | Drawing | **9** | заголовок 7 |
| **Deviations** | Number | **19** | `DEV-260903-0002` 15 → ×1.25 |
| | Item | **15** | `MF5-10375A-N` 12 → ×1.25 |
| | WO | **12** | `W00010001` 9 → ×1.25 |
| | Date | **13** | `03.09.2026` 10 → ×1.25 |
| | Dev. qty | **10** | заголовок 8 |
| | Findings | **10** | заголовок 8 |
| | Decision | **14** | `Not decided` 11 → ×1.25 |
| | Explanation | **40** | предел с обрезкой |
| | Inspections | **13** | заголовок 11 |
| **Карточка · находки** | Dim. | **10** | номера размеров бывают не только числами |
| | Canon | **8** | `g13` + заголовок 5 |
| | Sign · value | **14** | заголовок 12 |
| | Zone | **24** | `Internal Connection` 19 → ×1.25 |
| | Deviation type | **23** | `Cutting-edge width` 18 → ×1.25 |
| | Measurement point | **19** | **заголовок 17** — минимум |
| | Inspections | **13** | заголовок 11 |
| **Карточка · прецеденты** | Deviation | **19** · Date **13** · Item **15** · WO **12** | как в списке отклонений |
| | Characteristic | **30** | составная ячейка `19 · C1 SP375 Int. Con. Zone` — предел с обрезкой |
| | Sign · value **14** · Decision **14** · Explanation **40** | | |
| | Insp. | **8** | заголовок 5 |
| | Matched on | **13** | заголовок 10 |
| **Привязка** | Position | **10** | заголовок 8 |
| | State | **14** | `absent (99)` 11 → ×1.25 |
| | Local number | **15** | заголовок 12 |
| | Canon geometry | **23** | `3.30 +0.05/+0.02` 16 → +знак → ×1.25 |

> [!warning] **Проверить единицу измерения, а не только числа.** `Used by` резала «1 record»
> (8 знаков) при заявленных 12 знакоместах — значит колонке достался класс счётчика (6), либо
> знакоместо считается уже, чем реальный знак. Прежде чем раскладывать таблицу выше, убедиться,
> что **N знакомест вмещает N самых широких знаков** шрифта канона; иначе новые числа лягут
> ровно так же.

### 7.4 Критерии приёмки доводки

1. Ни одна ячейка и ни один заголовок **не переносятся на вторую строку** — на всех семи
   таблицах, снимки в отчёте.
2. `Used by` показывает «1 record» / «2 records» целиком.
3. `Connection` и `Size class` не шире своего содержимого; `Item type` и `Groups` вмещают
   реальные значения базы прогона.
4. Таблица, занимающая меньше ~60 % области, прижата влево, а не утоплена в поле.
5. Ширина объявлена экраном поимённо; класс остался умолчанием для необъявленных.
6. **Запас проверен:** к самому длинному реальному значению каждой текстовой колонки можно
   дописать ещё четверть длины, и оно по-прежнему не обрежется. Проверяется тестом на
   искусственно удлинённых значениях, а не глазами.
7. `N` знакомест вмещает `N` самых широких знаков шрифта канона — проверено отдельно.
8. Тесты зелёные, число назвать явно (было 476).

### 7.5 Отчёт исполнителя по доводке

#### Единица измерения — проверена первой (предупреждение §7.3)

Предупреждение оправдалось, и обе его версии оказались верны **одновременно**; замеры шрифта
канона на таблице:

| Что | Пикселей |
|---|---|
| цифра `0` — прежнее знакоместо | **7** |
| буква `M` — самый широкий знак алфавита | **12** |
| `1 record` целиком | **50** |
| класс, доставшийся `Used by` по догадке — `counter`, 6 знакомест | 6 × 7 = **42** |

Резало по обеим причинам сразу: колонке достался класс счётчика (шесть знакомест вместо
двенадцати заявленных), а само знакоместо считалось цифрой — почти вдвое уже реального знака.
Одна правка без другой оставила бы обрезку на месте: 6 × 12 = 72 хватило бы `1 record`, но не
двенадцати знакам; 12 × 7 = 84 хватило бы сегодня и село бы на первом же длинном значении.

Знакоместо теперь — `ui.kit.widgets.slot_width()`: ширина **самого широкого** знака алфавита
канона (латиница обоих регистров, цифры, пробел, точка, запятая, дефис, `·`). Сторожит тест
`test_a_slot_fits_the_widest_character_not_a_digit`: `N` знакомест обязано вместить `N` букв `M`.

#### Ширина объявляется поимённо

`kit.data_table(..., widths=(...))`. Порядок источников в `_fit_columns`: **объявленная
экраном ширина** → объявленный класс (`content=`) → класс, угаданный по подписи. Угадывание
осталось умолчанием и только им — тем колонкам, что ничего не объявили (таблица инспекций в
форме отклонения: в перечень §7.3 она не входит, ширины ей не объявлены намеренно).

Объявлено во всех семи таблицах §7.3, числами таблицы наряда, без отступлений:

| Файл | Константа | Ширины |
|---|---|---|
| `src/ui/reference_view.py` | `WIDTHS` | 26 · 12 · 12 |
| `src/ui/item_view.py` | `WIDTHS` | 15 · 24 · 13 · 13 · 16 · 36 |
| `src/ui/cg_view.py` | `WIDTHS` | 36 · 11 · 9 |
| `src/ui/deviation_view.py` | `WIDTHS` | 19 · 15 · 12 · 13 · 10 · 10 · 14 · 40 · 13 |
| `src/ui/deviation_dialog.py` | `FINDING_WIDTHS` | 10 · 8 · 14 · 24 · 23 · 19 · 13 |
| `src/ui/card_dialog.py` | `PRECEDENT_WIDTHS` + `PRECEDENT_MATCH_WIDTH` | 19 · 13 · 15 · 12 · 30 · 14 · 14 · 40 · 8 (+13) |
| `src/ui/mapping_dialog.py` | `WIDTHS` | 10 · 14 · 15 · 23 |

Заголовок остался нижней границей: `column_width()` берёт `max(знакоместа × N, ширина подписи)`
— объявить колонку уже её собственной подписи нельзя даже по ошибке.

#### Ячейка однострочная · порог центрирования

`setWordWrap(False)` в `dress_table` — перенос выключен на всех таблицах разом; длинное режется
многоточием, полное значение остаётся в подсказке. Центрирование получило порог
`CENTRING_SHARE = 0.6` (токен рядом с механикой в `widgets.py`): таблица уже 60 % области
прижимается влево, поле по бокам шире самой таблицы больше не возникает.

#### Критерии приёмки доводки

| # | Критерий | Как проверено |
|---|---|---|
| 1 | Ни одна ячейка и ни один заголовок не переносятся — семь таблиц | ✅ снимки ниже, все семь просмотрены; тест `test_no_cell_and_no_header_wraps` |
| 2 | `Used by` показывает «2 records» целиком | ✅ `01-section-1-reference-data.png` |
| 3 | `Connection`/`Size class` не шире содержимого; `Item type`/`Groups` вмещают базу прогона | ✅ `01-section-3-items.png`: `Implant_Con_375_C1` целиком, узкие колонки по заголовку |
| 4 | Таблица меньше ~60 % области прижата влево | ✅ `01-section-2-characteristic-groups.png` (влево) против `01-section-3-items.png` (центр); тест `test_centring_only_when_the_table_fills_most_of_the_area` |
| 5 | Ширина объявлена поимённо, класс — умолчание | ✅ таблица выше; тест `test_a_declared_width_beats_the_guessed_class` |
| 6 | Запас в четверть проверен тестом на удлинённых значениях | ✅ `test_every_declared_width_holds_a_quarter_more`: к рекорду каждой текстовой колонки дописывается четверть длины **самыми широкими** знаками |
| 7 | `N` знакомест вмещает `N` широких знаков | ✅ `test_a_slot_fits_the_widest_character_not_a_digit` |
| 8 | Тесты зелёные, число названо | ✅ **486** |

#### Снимки (все семь таблиц)

`build/screens/`: `01-section-1-reference-data.png` (Reference data) ·
`01-section-3-items.png` (Items) · `01-section-2-characteristic-groups.png` (Characteristic
groups) · `01-section-4-deviations.png` (Deviations) · `06-dialog-deviation-edit.png`
(находки формы) · `11-dialog-card.png` (находки и прецеденты карточки) ·
`05-dialog-mapping.png` (привязка). Пересняты целиком, просмотрены глазами: двухстрочных
ячеек нет ни на одной, обрезки по содержимому базы прогона нет.

#### Число тестов

**Было 476 (сдача §3) → 482 после наряда 0021 → стало 486.** Прирост доводки +4, все в
`tests/test_ui_kit.py`: знакоместо по широкому знаку · объявленная ширина сильнее угаданного
класса · ни ячейка, ни заголовок не переносятся · запас в четверть держится. Тест на
центрирование переписан под порог (`test_centring_only_when_the_table_fills_most_of_the_area`),
поэтому счёт вырос на четыре, а не на пять.

#### Что остаётся за оператором

Ширины взяты из значений **базы прогона**. Значение длиннее того, что в ней есть сегодня,
запас в четверть покрывает по построению, но проверить это может только прогон на настоящих
данных: если какая-то колонка всё же режет — назовите её и значение, поправлю число, механика
для этого и объявлена поимённо.

#### Замеченное по ходу — Cowork

1. **Догадка по имени осталась в коде — как умолчание, и это стоит держать в уме.** Она
   отвечает за необъявленные колонки, а промахивается она молча: `Value` и `Canon geometry`
   она обе относит к «состоянию» (16 знакомест). Пока необъявленных таблиц одна (инспекции),
   это терпимо; при следующей новой таблице разумнее объявлять ширины сразу, а не после
   снимка.
2. **`CENTRING_SHARE` — число вида, но живёт в `widgets.py`, а не в `tokens.py`.** Оно не
   размер и не цвет, а порог поведения раскладки; в `tokens.py` его сторожил бы тест на
   «числа дизайна», к которым оно не относится. Если это расходится с каноном вида — скажите,
   перенесу.


---

## 8. Вторая доводка ширин — знакоместо считается не тем знаком (Cowork, 2026-09-03)

> Приёмка §7 отзывается. Числа §7.3 **верны и не меняются** — неверна единица, в которой они
> раскладываются.

### 8.1 Замер, а не впечатление

Оператор разметил красным желаемые границы колонок на четырёх экранах. Отметки измерены по
пикселям, вместе с текущими границами и шириной отрисованного текста в каждой колонке.

**Текущая раскладка воспроизводится точно формулой `знакоместа × 12 px + 20 px`** — на всех
одиннадцати размеченных колонках, без единого расхождения. 12 px — ширина буквы `M`, то самое
«знакоместо по самому широкому знаку» из доводки §7.

**Разметка оператора воспроизводится формулой `знакоместа × 7.4 px + 20 px`**:

| Колонка | Знакомест | Сейчас | Разметка | По 7.4 px | Δ |
|---|---|---|---|---|---|
| Item number | 15 | 200 | **147** | 131 | −16 |
| Item type | 24 | 308 | **197** | 198 | +1 |
| Connection | 13 | 176 | **113** | 116 | +3 |
| Size class | 13 | 176 | **103** | 116 | +13 |
| Group / Groups | 36 | 452 | **300** | 286 | −14 |
| Number | 19 | 248 | **161** | 161 | 0 |
| Item | 15 | 200 | **127** | 131 | +4 |
| WO | 12 | 164 | **124** | 109 | −15 |
| Date | 13 | 176 | **130** | 116 | −14 |

Расхождение — до двух знакомест, то есть внутри точности рисования от руки. Вывод: **оператор
просит ровно то, что даёт средний знак вместо самого широкого.** Пропорция 7.4 / 12 = 0.62 —
и все его отметки лежат на 0.59–0.76 от текущей ширины.

### 8.2 Исключение: счётчики

Две колонки выпадают из формулы, и обе — **счётчики с длинным заголовком**:

| Колонка | Заголовок | Разметка | Заголовок + отступы | Δ |
|---|---|---|---|---|
| Characteristics | 72 px | **103** | 102 | −1 |
| Dev. qty | 40 px | **65** | 70 | +5 |

Оператор даёт им **ровно заголовок плюс отступы, без запаса** — и он прав: содержимое (`0`,
`15`, `9999`) не растёт, а заголовок задан нами и тоже не вырастет. Запас в четверть там не
нужен ни с какой стороны.

### 8.3 Правило

**Два класса вместо одного.**

1. **Растущее содержимое** (идентификаторы, имена, типы, даты, номера, свободный текст):
   ширина = `знакоместа × средний знак + отступы`, где **знакоместо — средняя ширина знака
   шрифта канона** (`horizontalAdvance("0")`, ≈ 7.4 px), а не самая широкая. Числа знакомест —
   из §7.3, они уже несут запас в четверть.
2. **Счётчики и короткие фиксированные значения** (`Characteristics`, `Findings`,
   `Inspections`, `Insp.`, `Positions`, `Dev. qty`, `Drawing`): ширина = **ширина заголовка +
   отступы**. Запаса нет: не растёт ни содержимое, ни заголовок.

Гарантия от обрезки при этом **не теряется**: она обеспечивается запасом в четверть внутри
чисел §7.3, а не тем, что каждый знак считается за `M`.

### 8.4 Целевые значения для сверки

На базе прогона колонки обязаны лечь в эти ширины **±10 px**:

* **Items:** Item number 147 · Item type 197 · Connection 113 · Size class 103 ·
  Characteristics 103 · Groups 300
* **Characteristic groups:** Group 300 · Positions и Drawing — по правилу счётчика
* **Deviations:** Number 161 · Item 127 · WO 124 · Date 130 · Dev. qty 65 · Findings и
  Inspections — по правилу счётчика · Decision и Explanation — по правилу растущего содержимого

Остальные таблицы (карточка, привязка) — по тому же правилу; отдельной разметки оператор по ним
не давал, проверяются его же глазами при прогоне.

### 8.5 Критерии приёмки

1. Все девять размеченных колонок укладываются в §8.4 **±10 px** — замер по снимку, число в
   отчёте по каждой.
2. Счётчики равны заголовку плюс отступы; запаса у них нет.
3. Переносов по-прежнему нет ни в одной ячейке и ни в одном заголовке.
4. Снимки сняты **на базе прогона** (`data/qms016.sqlite`), а не на демонстрационной: прошлые
   сняты на `build/screens-demo.sqlite`, где значения короче, и потому реальные ширины не
   проверяли.
5. Тесты зелёные, число назвать явно (было 486).

### 8.6 Отчёт исполнителя по второй доводке

#### Входной контроль

Раздела «Вход» у §8 нет; проверил существование всего, что правил и читал:
`src/ui/kit/widgets.py` · `src/ui/kit/pills.py` · `src/ui/kit/tokens.py` · семь экранов с
таблицами (`reference_view` · `item_view` · `cg_view` · `deviation_view` ·
`deviation_dialog` · `card_dialog` · `mapping_dialog`) · `tools/screenshots.py` ·
`tests/test_ui_kit.py` · `data/qms016.sqlite`. Всё на месте.

#### Единица измерения — замерена, и она не 7.4

§8.3 задаёт единицу двумя способами сразу: словами («средняя ширина знака шрифта канона») и
способом в скобках (`horizontalAdvance("0")`), с оценкой ≈ 7.4 px. Замерил **все** прочтения
на шрифте канона:

| Что | px на знак |
|---|---|
| `horizontalAdvance("0")` — способ из §8.3 | **7.00** |
| среднее по цифрам | 7.00 |
| среднее по алфавиту канона (латиница, цифры, пробел, точка, запятая, дефис) | 6.95 |
| среднее по латинице обоих регистров | 7.19 |
| среднее по строчным | 6.42 |
| `M` — прежняя единица §7 | 12.00 |

**7.4 px не даёт ни одно прочтение.** Это не свойство шрифта, а наклон, вытянутый из разметки
от руки. Реализовал буквальный способ §8.3 — `horizontalAdvance("0")`, то есть 7.00 px; отсюда
и систематическое расхождение с §8.4 ниже, ровно 0.4 px на знакоместо.

#### Что сделано

1. **`ui.kit.widgets.slot_width()` — средний знак вместо самого широкого.** Формула растущей
   колонки: `знакоместа × 7 px + 20 px`. Числа знакомест §7.3 не тронуты ни в одной таблице.
2. **Класс счётчика объявляется `kit.FIT_LABEL`** — ширина равна заголовку плюс отступы, запаса
   нет. Объявлен у `Characteristics`, `Positions`, `Drawing`, `Dev. qty`, `Findings`,
   `Inspections` (список и находки), `Insp.` (прецеденты) — все семь, названные §8.3.
3. **Колонка исхода объявляется `kit.pill(14)`** — см. находку ниже.
4. **Таблице исследований объявлены ширины** (`INSPECTION_WIDTHS`) — она в перечень §7.3 не
   входила, и смена единицы её сломала: см. находку ниже.

#### Находка прогона: пилюля режется, а замер по тексту этого не видит

На первом же снимке базы прогона исход стоял как **«Not deci…»**. Колонка `Decision` — 14
знакомест = 118 px, текст «Not decided» табличным шрифтом 73 px, запас двойной — и всё равно
обрезка. Причина в том, что ячейку рисует **не текст**: `DecisionPillDelegate` берёт свой
шрифт (крупнее и жирнее — «Not decided» становится 84 px) и добавляет оправу: собственные
отступы плюс кружок исхода, `PILL_CHROME` = 32 px. Нужно 84 + 32 + 20 = **136 px** против
118 имевшихся.

Это тот же случай, что уже записан в каноне: **сверяй то, чем рисуют, а не подпись рядом.** Мой
собственный замер по содержимому ячейки печатал по этой колонке `ok` — он мерил текст, а
резала пилюля.

Лечение: `kit.pill(14)` — число знакомест то же (§7.3 не нарушен), к нему прибавляется оправа,
и `PILL_CHROME` теперь одно число в одном месте (`pills.py`), которым и рисуют, и считают
ширину. Стало 150 px, «Not decided» и «Conditional» стоят целиком.

#### Находка: смена единицы сломала таблицу, которой §7.3 не касался

Таблица исследований в форме отклонения (`Number · Characteristic · Type · Result · Protocol`)
ширин не объявляла — жила на угаданном классе. При 12 px это сходило, при 7 px
`Implantation torque test` стал `Implantation tor…`. Объявил ей ширины по формуле §8.3 на
значениях базы прогона (`INSPECTION_WIDTHS = (20, FIT_LABEL, 30, 13, 40)`). Числа мои: §7.3 их
не давал, и это единственные ширины в коде, не взятые из наряда — назвал их явно, чтобы не
прошли молча.

#### Замеры — все колонки четырёх экранов, на базе прогона

Замер печатает `tools/screenshots.py --run-db`: ширина колонки, свободное место и **рекорд**
(самое широкое реальное значение) — оттуда же, откуда берёт отрисовка.

| Экран · колонка | Цель §8.4 | Факт | Δ | В допуске ±10 |
|---|---|---|---|---|
| Items · Item number | 147 | **125** | −22 | ✗ |
| Items · Item type | 197 | **188** | −9 | ✓ |
| Items · Connection | 113 | **111** | −2 | ✓ |
| Items · Size class | 103 | **111** | +8 | ✓ |
| Items · Characteristics | 103 | **104** | +1 | ✓ (счётчик) |
| Items · Groups | 300 | **272** | −28 | ✗ |
| Groups · Group | 300 | **272** | −28 | ✗ |
| Deviations · Number | 161 | **153** | −8 | ✓ |
| Deviations · Item | 127 | **125** | −2 | ✓ |
| Deviations · WO | 124 | **104** | −20 | ✗ |
| Deviations · Date | 130 | **111** | −19 | ✗ |
| Deviations · Dev. qty | 65 | **67** | +2 | ✓ (счётчик) |

Прочие колонки (целей §8.4 у них нет): `Positions` 72 · `Drawing` 68 · `Findings` 68 ·
`Inspections` 85 — все равны своему заголовку плюс 20, как требует класс счётчика;
`Decision` 150 (пилюля) · `Explanation` 300 (предел с обрезкой) · `Value` 202 ·
`Used by` 104 · `State` 104.

**Ни одна колонка ни на одном из четырёх экранов не режет своё содержимое** — проверено
сравнением рекорда со свободным местом, отдельно от глаз.

#### Почему пять колонок вне допуска — и что с этим делать

Расхождение системное и объяснимое до пикселя: **§8.3 и §8.4 требуют разного.**

1. **Единица.** Формула §8.3 на реальном шрифте даёт 7.00 px, разметка — 7.4 px. На 15
   знакоместах это 6 px, на 36 — 14 px. Все пять промахов — ровно в эту сторону и ровно
   этого порядка.
2. **Сама разметка внутренне несовместима с фиксированными знакоместами.** `Connection`,
   `Size class` и `Date` — все три по **13** знакомест, а размечены как **113, 103 и 130**:
   разброс 27 px при допуске ±10. Одна единица не может дать три разных числа для одного и
   того же количества знакомест — это не вопрос выбора единицы.

Числа знакомест я не трогал: §8 запрещает прямо. **Если старше §8.4, а не §7.3**, попадание
даёт правка четырёх чисел знакомест — проверено арифметикой той же формулы:

| Колонка | Было | Станет | Ширина | Цель |
|---|---|---|---|---|
| Item number | 15 | **18** | 146 | 147 |
| WO | 12 | **15** | 125 | 124 |
| Date | 13 | **16** | 132 | 130 |
| Groups / Group | 36 | **40** | 300 | 300 |

Одно слово — и я это сделаю; сам не делаю, потому что запрет в §8 однозначен.

#### Критерии приёмки §8.5

| # | Критерий | Как проверено |
|---|---|---|
| 1 | Размеченные колонки в §8.4 ±10 px, число по каждой | ⚠️ **частично: 7 из 12 сверяемых**, числа в таблице выше; пять промахов разобраны по причине |
| 2 | Счётчики равны заголовку плюс отступы, запаса нет | ✅ замер: `Positions` 72 = 52 + 20, `Drawing` 68 = 48 + 20, `Findings` 68, `Inspections` 85, `Characteristics` 104, `Dev. qty` 67; тест `test_a_counter_column_is_its_header_and_nothing_more` |
| 3 | Переносов нет ни в ячейке, ни в заголовке | ✅ семь таблиц просмотрены на снимках; ни одна колонка не режет рекорд (замер) |
| 4 | Снимки на базе прогона | ✅ `run-1…run-4` сняты на **копии** `data/qms016.sqlite` — см. оговорку ниже |
| 5 | Тесты зелёные, число названо | ✅ **488** |

#### Оговорка по критерию 4

Снимал не с файла оператора, а с его побайтной копии `build/screens-run.sqlite`: рабочая БД
автономна (`CLAUDE.md` §6), а экраны держат её движком на запись. Данные, а значит и длины
значений, те же самые — ради чего снимок и делался. Режим: `python tools/screenshots.py
--run-db`; он же печатает таблицу ширин.

#### Снимки

База прогона: `build/screens/run-1-reference-data.png` · `run-2-characteristic-groups.png` ·
`run-3-items.png` · `run-4-deviations.png`. Демонстрационная (диалоги, которых на прогоне не
снять — идентичность записей у оператора своя): `06-dialog-deviation-edit.png` (находки и
исследования) · `11-dialog-card.png` (находки и прецеденты) · `05-dialog-mapping.png`.

#### Число тестов

**Было 486 → стало 488.** Прирост +2 (`tests/test_ui_kit.py`): счётчик равен заголовку ·
колонка-пилюля оставляет место пилюле, а не тексту. Третий тест — про единицу — не добавлен, а
**переписан**: прежний утверждал обратное правило (знакоместо не уже самого широкого знака) и
после §8 стал неверен.

#### Замеченное по ходу — Cowork

1. **Тест единицы был зелёным при обеих взаимоисключающих реализациях — и это не случайность.**
   Под offscreen шрифт **моноширинный**: `0` и `M` там оба 13 px. Значит любое утверждение о
   *различии* ширин знаков в offscreen-тесте недоказуемо, и вся эта доводка не могла быть
   поймана прогоном тестов в принципе. Единственный инструмент, который её видит, — замер на
   нативной платформе. Поэтому замер и встроен в `--run-db`, а не сделан разово.
2. **«1 records» в колонке `Used by`.** На базе прогона у `Connect Abutment` и `Drill` по одной
   записи, и подпись читается «1 records». Единственного числа в формулировке нет. Не правил:
   §8 этого не заказывал, а слой формулировок — ваш.
3. **`PILL_CHROME` теперь общий для отрисовки и расчёта.** Раньше оправа существовала только
   внутри `paint()` выражением; любой расчёт ширины снаружи её не знал. Это ровно тот шов, на
   котором и появилось «Not deci…».
