# Наряд 0004 — Отклонение + Находки + Исследование (S4)

- **Задача / цель:** `QMS-014` (S4 роадмапа). Дать ручной ввод **полной записи отклонения**:
  шапка (WO / NCR / количество / дата / машина / вложение), находки `1..N` по размерам с
  ранней привязкой к канону (R2), решение `decisionDev` отдельным действием, исследования
  `0..N` на находке. После S4 база наполняется руками от начала до конца — без карточки и
  поиска (это S5) и без импорта Excel (S6).

- **Вход (читать перед работой):**
  - `docs/model/Deviation.md` — атрибуты, 4 исхода, WO, NCR, три уровня количества,
    граница области базы.
  - `docs/model/Finding.md` — поля находки, привязка через характеристику, зона и тип.
  - `docs/model/Inspection.md` — когда заводится строка, привязка к находке **и** к паре
    (Item, размер), независимость `decisionInsp`, «наука в протоколе».
  - `docs/model/reference/reference-data.md` — зона и тип отклонения заполняются
    оператором при вводе; большинство справочных данных заводится по ходу, а не заранее.
  - `docs/model/_overview.md` §6 и шаги 3–8 процесса; `docs/model/Characteristic.md`.
  - `docs/architecture.md` §5 (**схема rev 0.2**), `docs/decisions.md` (ратификации S1, R2,
    решения S3).
  - `docs/specs/cg-editor-and-mapping.md` — публичный вход `MappingDialog.run(...)`, на
    который вешаются «ранние кнопки».
  - Готовый код: `src/domain/findings.py` (`make_finding`), `src/domain/mappings.py`,
    `src/domain/characteristics.py`, `src/db/ids.py`, `src/ui/*`.

---

## Решения Cowork, принятые к этому наряду

1. **Решение по отклонению вносится отдельным действием**, не в форме регистрации. Форма
   регистрирует отклонение с пустым решением (`decision_dev` nullable — ратификация S1 №2);
   решение вносится кнопкой «Решение…» из списка отклонений. Это порядок канона:
   регистрация — шаг 3, решение — шаг 8, после изучения прецедентов. В S5 то же действие
   переедет в карточку отклонения без переделки — вынести его в отдельный диалог.
2. **Исследование заводится действием на выбранной находке** («Исследование…» в таблице
   находок), список исследований отклонения виден в нижней части окна. Отдельного раздела
   «Исследования» не делаем: цель исследования — конкретная находка, и выбор её руками из
   общего списка добавляет только шанс промахнуться.
3. **Отклонение без находок не сохраняется** — минимум одна (канон: `1..N`). Отклонение без
   размера невидимо для поиска прецедентов, то есть бесполезно.
4. **Вложение** (`deviation.attachment`) — многострочное поле, одна ссылка на строку, плюс
   кнопка «Выбрать файл…», вставляющая путь строкой. Файлы **не копируются** в базу:
   исключение про блобы касается только чертежей CG (`architecture.md` §4).
5. **Деталь выбирается в форме** из списка, рядом — кнопка «Создать деталь…» (существующая
   `ItemDialog`). Это тот же ранний путь, что и привязка к канону: `_overview.md` §6 прямо
   допускает заведение детали в момент обработки отклонения.

---

## Что сделать

### 1. Домен (чистый Python, без импортов PySide6)

`src/domain/deviations.py` — новый модуль:

- `register(session, *, item, wo, quantity, date, machine=None, ncr=None, attachment=None)`
  — создать отклонение с бизнес-номером (существующий `db.ids`), **без решения**.
  Валидации: WO непустой; `quantity` целое ≥ 1; дата не в будущем; деталь обязательна.
- `set_decision(session, deviation, *, decision, explanation, ncr=None, decision_date=None)`
  — внести/сменить решение. Проверки:
  - `decision` ∈ 4 значениях словаря (`approved` / `rejected` / `sorting` / `repair`);
  - **`explanation` непусто при `approved`** — доменная валидация, отложенная с S1 (№3):
    на одобрении обоснование уходит в `אישור חריגה`, пустым быть не может;
  - `decision_date` по умолчанию — системное время (уже в модели).
- `list_deviations(session, *, item=None)` — список для экрана, с числом находок.
- `delete_deviation(session, deviation)` — удаление вместе с находками и исследованиями
  (каскад уже в схеме); отдельного подтверждения в домене не изобретать, спрашивает UI.

`src/domain/findings.py` — дополнить:

- `update_finding(session, finding, *, direction, value, dimension_point, comment, zone,
  deviation_type)` — **все поля обязательны** в подписи (правило S3: функция заменяет
  запись целиком, пропущенный аргумент не должен выглядеть как «не трогаем»).
- `remove_finding(session, finding)` — **заблокировать удаление последней находки**
  отклонения (инвариант `1..N`) и находки, на которой висит исследование (сообщение с
  числом ссылок, образец S2/S3).
- `make_finding` — не переписывать; это единственная точка создания находки.

`src/domain/inspections.py` — новый модуль:

- `create_inspection(session, finding, *, inspection_type, decision_insp, protocol)` —
  бизнес-номер `INSP-…` из `db.ids`; `decision_insp` ∈ (`approved`, `not_approved`);
  отклонение выводится из находки, отдельно не передаётся. Тип — из справочника
  `ref_inspection_type`.
- `update_inspection` / `remove_inspection` — по тому же правилу «целиком».
- `inspections_for(session, item, characteristic)` — зеркальный поиск «все исследования по
  паре (Item, размер)». Пара **выводится** через находку (`finding → characteristic →
  item`), отдельных полей в схему не добавлять — это и есть «явная привязка к паре» канона
  на текущей схеме; проверить запросом, а не новым столбцом.

### 2. UI

- **Раздел «Отклонения»** (`src/ui/deviation_view.py`): список — номер, деталь, WO, дата,
  количество, решение (или «не принято»), число находок, число исследований. Кнопки:
  «Добавить отклонение…», «Открыть…», «Решение…», «Удалить».
- **Форма отклонения** (`src/ui/deviation_dialog.py`) — регистрация и правка:
  - шапка: деталь (выбор + «Создать деталь…»), WO, машина, количество, дата, NCR,
    вложение (многострочное + «Выбрать файл…»);
  - **таблица находок** с кнопками «Добавить находку…», «Править…», «Убрать»,
    «Исследование…»; колонки: номер размера, канон (`g5` / «нет у детали» / «не привязан»),
    направление, величина, точка, зона, тип отклонения, число исследований;
  - **ранние кнопки (R2)** рядом с находкой: «Привязать к канону…» — зовёт
    `MappingDialog.run(engine, item_id, cg_id, parent)`; активна, когда деталь выбрана.
    Если у детали нет группы — та же логика выбора, что в разделе «Детали»;
  - сохранение — **одной транзакцией** по «Сохранить»: отклонение и его находки; пустой
    список находок кнопку блокирует (решение 3), причина видна текстом.
- **Диалог находки** (`src/ui/finding_dialog.py`): номер размера (с автосозданием
  характеристики доменной функцией, если такого размера у детали ещё нет), направление
  `+`/`−` (обязательно), величина, точка, комментарий, зона, тип отклонения (оба — из
  справочников, пусто допустимо). Показывать состояние привязки размера к канону.
- **Диалог решения** (`src/ui/decision_dialog.py`): 4 исхода списком с человеческими
  подписями, обоснование (`explanation`), NCR, дата решения. Отдельный виджет — в S5 он
  переезжает в карточку как есть.
- **Диалог исследования** (`src/ui/inspection_dialog.py`): тип из справочника, вердикт
  (одобрено / не одобрено), протокол (ссылка на документ), показ находки, к которой
  привязано.
- Разделы «Карточка отклонения» и «Поиск» остаются неактивными пунктами навигации (S5).

### 3. Подписи и bidi

- Все величины со знаком (`+0.05`, `−0.05`), номера размеров, `g`-подписи и бизнес-номера
  печатать через изолят `ui.common.iso` — это прямой перенос из S2/S3 (в RTL-окне
  ведущий знак иначе уезжает в хвост).
- Подписи кнопок: где правки копятся до записи — «Сохранить»/«Отмена»; где пишется по
  действию — «Готово»/«Закрыть» (конвенция S3).

---

## Критерии приёмки

1. **Регистрация.** Отклонение создаётся с бизнес-номером, без решения; в списке видно
   «решение не принято». Обязательные поля отбиваются доменными сообщениями, не
   `IntegrityError`.
2. **Находки.** Добавляются, правятся, удаляются; **последняя не удаляется** (сообщение);
   размер, которого у детали нет, создаётся автоматически доменной функцией.
3. **Гард (перенос из S2).** Находки создаются **только** через `make_finding` — проверка
   разбором кода (AST): в `src/ui/**` нет прямого конструирования `Finding(...)`, плюс
   тест-на-тест, что проверка ловит настоящее нарушение. Инвариант «находка ∈ деталь
   отклонения» покрыт через UI-путь, а не только доменным тестом.
4. **Ранняя привязка (R2).** Из формы отклонения открывается тот же `MappingDialog`;
   после привязки колонка «канон» в таблице находок показывает g-позицию без
   переоткрытия формы.
5. **Решение.** Вносится отдельным действием; 4 исхода пишутся; **`approved` без
   обоснования отбивается**; смена решения перезаписывает дату решения.
6. **Исследование.** Заводится на выбранной находке, `0..N` на отклонение; бизнес-номер
   уникален; `decision_insp` не влияет на `decision_dev` (тест: одобренное исследование
   при отклонённом отклонении — валидная комбинация).
7. **Зеркальный поиск.** `inspections_for(item, characteristic)` возвращает исследования по
   паре (Item, размер) — тест на двух деталях с одинаковым номером размера: выдачи не
   смешиваются.
8. **Целостность.** Удаление отклонения уносит находки и исследования; удаление находки с
   исследованием заблокировано.
9. **Слоение.** `src/domain/**` и `src/db/**` без импортов PySide6 — прежняя AST-проверка
   зелёная.
10. **Схема.** rev 0.2 **не меняется**: ни новых таблиц, ни колонок, ни ревизий миграции.
    Если по ходу окажется, что чего-то не хватает, — **стоп и вопрос в отчёт**, не правка.
11. **Тесты.** Прежние 151 зелёные + новые на всё перечисленное.
12. **Глазами.** Прогон приложения + рендер формы отклонения, диалога находки и диалога
    решения в PNG: RTL верный, величины со знаком не переставляются, иврит в справочниках
    рисуется корректно.

---

## Чего не трогать

- `docs/model/`, `docs/architecture.md`, `docs/decisions.md`, `docs/specs/` — слой Cowork.
  Расхождения — в «Отчёт исполнителя».
- **Схема БД** (см. критерий 10), генерация бизнес-номеров, сид, синтетика.
- Карточка отклонения и поиск прецедентов (S5) — включая любые «пока сделаю простой
  поиск». Это главный deliverable проекта, он проектируется отдельно.
- Импорт Excel и парсер иврита (S6), упаковка (S7).
- CG-редактор и диалог привязки (S3) — использовать, не переписывать.

---

## Заметки исполнителю

- **А. Количество.** Значение `9999` означает «выборка `X מתוך Y`» и приходит из импорта
  (S6). В ручном вводе — обычное число; отдельного флага не заводить (это флаг на S6,
  `decisions.md`).
- **Б. Направление обязательно** (`direction` NOT NULL, из слова max/min источника).
  Качественные признаки (`GO`, пин) на этапе 1 живут в `comment` — поле направления при
  этом всё равно требуется, оператор выбирает знак осознанно.
- **В. `dimension_point`** в поиске не участвует — это индекс точки замера, хранится и
  показывается, не более.
- **Г. Зона и тип отклонения** заполняются оператором и чистятся администратором; в форме
  это обычные выпадающие списки справочников, пустое значение допустимо.
- **Д. cp1255** на рабочей машине: `*.ini` — ASCII-only, CLI-скрипты → UTF-8 stdout
  (`INFRASTRUCTURE` §8).
- **Е. GUI-тесты** — offscreen, как в S2/S3; плюс рендер в PNG для глазной проверки.
- **Ж. Область базы кончается решением.** Судьба деталей после решения (сортировка → QC,
  повторные циклы, комитет по отклонениям) в базе не моделируется — не достраивать.

---
## Отчёт исполнителя (заполняет Claude Code)

- **Сделано:** ручной ввод полной записи отклонения работает от начала до конца. Домен
  пополнен тремя модулями, UI — пятью экранами, раздел «Отклонения» стал активным пунктом
  навигации (карточка и поиск остались неактивными, как и предписано). **230 тестов
  зелёные** (было 151, добавлено 79). Схема rev 0.2 не менялась. Канон не правил.

  Раскладка добавленного:

  ```
  src/domain/deviations.py    register · update_registration · set_decision ·
                              list_deviations (агрегат со счётчиками) · delete_deviation
  src/domain/findings.py      += update_finding · remove_finding · inspection_count
  src/domain/inspections.py   create · update · remove · inspections_for
  src/ui/deviation_view.py    раздел «Отклонения»
  src/ui/deviation_dialog.py  форма отклонения: шапка · находки · ранние кнопки R2
  src/ui/finding_dialog.py    диалог находки (+ FindingRow, canon_state)
  src/ui/decision_dialog.py   диалог решения — самостоятельный, под переезд в S5
  src/ui/inspection_dialog.py диалог исследования
  src/ui/common.py            += подписи словарей · direction_label · number_label · ltr_field
  src/ui/pickers.py           += choose_cg_for_item (общий с разделом «Детали»)
  tests/                      test_domain_deviations · test_domain_inspections ·
                              test_ui_deviation · дополнения findings и ui_smoke
  ```

- **Коммиты:**
  - `0aeb9ba` docs: канон S3 + наряд 0004 (QMS-013/014) — housekeeping до кода
  - `71444e3` feat(domain): отклонение, правка находок, исследования — слой S4
  - `00646b2` feat(ui): раздел «Отклонения» — форма, находки, решение, исследование (S4)
  - последний — `docs(worklog): отчёт исполнителя по наряду 0004`

- **Проверки:** `pytest` — **230 passed**; прогон приложения на чистой БД (миграция
  `пусто → rev02`, сид 21 значения, окно поднялось, event loop живой, раздел «Отклонения»
  открылся); рендер пяти экранов в PNG и просмотр глазами.

  1. **Регистрация.** Отклонение создаётся с номером `DEV-YYMMDD-NNNN` и **без решения**; в
     списке колонка показывает «решение не принято», в статусе — счётчик таких. Пустой WO,
     количество < 1, дробное количество, дата в будущем и отсутствие детали отбиваются
     доменными сообщениями; проверено и в домене, и через форму — тест на пустом WO смотрит,
     что запись не создалась, то есть до `IntegrityError` дело не доходит.
  2. **Находки.** Добавляются, правятся, удаляются через форму; **последняя не удаляется** —
     блокировка срабатывает сразу при нажатии «Убрать», а не при сохранении. Размер, которого
     у детали нет, создаётся доменной функцией при записи (тест: у детали не было ни одного
     размера, после сохранения появился `77`). Дубль размера внутри отклонения отбивается —
     две находки по одному размеру это одна находка.
  3. **Гард.** AST-проверка `src/ui/**` на прямое конструирование `Finding(...)` — ловит и
     голое имя, и `models.Finding(...)`; тест-на-тест на обеих формах плюс третий тест, что
     импорт и аннотация нарушением не считаются. Инвариант «находка ∈ деталь отклонения»
     покрыт **через путь формы**: у двух деталей заведён размер №12, отклонение
     регистрируется на первой — находка берёт её размер, не чужой.
  4. **Ранняя привязка (R2).** Из формы открывается тот же `MappingDialog.run`; после
     привязки колонка «канон» показывает `g1` **без переоткрытия формы** — тест подменяет
     только открытие диалога, привязку делает настоящий домен. Кнопка неактивна, пока не
     выбрана деталь. Выбор группы — общая с разделом «Детали» функция.
  5. **Решение.** Вносится отдельным действием из списка; все 4 исхода пишутся; `approved`
     без обоснования отбивается (в сообщении — `אישור חריגה`), и `decision_dev` остаётся
     пустым; смена решения перезаписывает `decision_date`; невыбранный исход — тоже отказ.
  6. **Исследование.** Заводится на выбранной находке, `0..N` на отклонение, номера `INSP-…`
     уникальны (пять подряд в один день — пять разных). Одобренное исследование при
     отклонённом отклонении — валидная комбинация, тест держит это явно; пустой протокол и
     невыбранный вид отбиваются.
  7. **Зеркальный поиск.** `inspections_for(item, characteristic)` — тест на двух деталях с
     размером №12 у обеих: выдачи не смешиваются. Плюс два теста: выдача сквозная по
     нескольким отклонениям одной пары, и половинки пары от разных деталей дают пусто.
  8. **Целостность.** Удаление отклонения уносит находки и исследования (размеры детали
     переживают); удаление находки с исследованием заблокировано и в домене, и в форме —
     форма показывает число ссылок сразу при нажатии.
  9. **Слоение.** AST-проверка `src/domain/**` и `src/db/**` на PySide6 зелёная,
     тест-на-тест сохранён.
  10. **Схема.** `git diff` по `migrations/` и `src/db/` пуст, ревизий по-прежнему две
      (`baseline`, `rev02`). Ничего не добавлял — места, где схемы не хватило, ниже.
  11. **Тесты.** Прежние 151 зелёные + 79 новых.
  12. **Глазами.** Отрисовал форму отклонения, диалог находки, диалог решения, диалог
      исследования и список: RTL верный, `+`/`−` не переставляются, иврит в справочниках, в
      комментарии находки (`GO לא עובר`) и в обосновании решения рисуется корректно рядом с
      латиницей, бизнес-номера (`DEV-260817-0001`, `INSP-260817-001`) сохраняют порядок.

  **Что вскрылось прогоном и починено:**

  - **Даты в RTL показывались задом наперёд.** `QDateEdit` рисовал `17.08.2026` как
    `2026.08.17`: точки — нейтральные символы, и алгоритм bidi переставляет числовые группы.
    Класс тот же, что минус в S3, но изолят здесь **не помогает** — текст внутри редактора
    рисует Qt, обернуть его нечем. Развернули сам виджет (`ui.common.ltr_field`), содержимое
    у него гарантированно латинско-цифровое; то же сделано для поля количества. В таблицах,
    где строку формируем сами, достаточно `iso` — список дату показывал верно с самого
    начала, что и подтвердило диагноз. Оба случая закреплены тестами.
  - **Offscreen-платформа Qt не годится для глазной проверки.** Первый рендер вышел сплошным
    «тофу»: у платформы `offscreen` база шрифтов **пуста** (`QFontDatabase.families() == 0`),
    поэтому ни кириллица, ни иврит не рисуются — код форм тут ни при чём. Снимал на нативной
    платформе и **без** `show()`: окна на экране не всплывают, раскладку доводит
    `layout().activate()`. Для протокола: GUI-**тесты** offscreen остаются верными, там глифы
    не нужны; непригоден offscreen только для снимков.

- **Отклонения / вопросы Cowork:**

  1. **Добавил не описанную в наряде доменную функцию — `update_registration`.** Раздел «Что
     сделать» перечисляет `register / set_decision / list_deviations / delete_deviation`, но
     форма по тому же разделу — «регистрация **и правка**», а править шапку было нечем.
     Сделал по правилу S3 (заменяет поля целиком). **Деталь она не меняет намеренно:**
     размеры находок принадлежат прежней детали, перенос осиротил бы их — в форме выбор
     детали после регистрации заблокирован с подсказкой. Прошу ратифицировать или сказать,
     что правка шапки не нужна.
  2. **`ncr=None` в `set_decision` трактовал как «не трогать», а не «стереть».** В подписи из
     наряда у `ncr` и `decision_date` стоит один и тот же `None`, но смысл нужен разный: для
     даты «не передали» = «сейчас» (иначе не выполнить критерий 5 про перезапись даты), а для
     NCR «не передали» обязано означать «оставить» — номер вводится и на регистрации, и в
     решении, и полная замена стирала бы введённое раньше (ровно та ловушка, за которую S3
     получил замечание 3). Снять номер явно — передать пустую строку. Оба смысла в докстроке
     и в тестах; если трактовка неверна — поменяю.
  3. **Колонка «канон» не может показать «нет у детали».** Наряд описывает три значения
     (`g5` / «нет у детали» / «не привязан»), но код 99 — это отметка на **g-позиции, которой
     у детали нет**, а находка всегда про размер, который у детали **есть**: он и отклонился.
     Реализовал три достижимых состояния: `g5` · «не привязан» (размер заведён, привязки нет)
     · «размер ещё не заведён» (находка новая, характеристики пока нет). Если под «нет у
     детали» имелось в виду что-то другое — скажите.
  4. **Исследование нельзя завести на несохранённой находке.** Оно ссылается на строку
     `finding` в базе, а находки по решению 3 пишутся одной транзакцией по «Сохранить».
     Кнопка «Исследование…» для новой находки неактивна, причина написана в статусе; после
     сохранения кнопка оживает. Альтернатива — писать находки сразу по действию, но это
     ломает «одной транзакцией» из наряда. Оставил как есть.
  5. **`decision_date` заполняется уже при регистрации.** Колонка NOT NULL с дефолтом
     `datetime.now` (ратификация S1 №2 сделала nullable только `decision_dev`), поэтому у
     только что зарегистрированного отклонения дата решения не пуста, хотя решения нет. На
     поведение не влияет — «решение не принято» определяется по `decision_dev`, — но в
     выгрузке поле будет вводить в заблуждение. Схему не трогал (критерий 10); если менять —
     это миграция на S6/S7.
  6. **Смену детали блокирует UI, а не домен.** `update_registration` деталь просто не
     принимает, а в форме виджет выключен. Если в S5 карточка захочет «перенести отклонение
     на другую деталь» — это отдельное доменное действие с переносом или пересозданием
     находок; сейчас его нет намеренно.
  7. **Заводить зону и тип отклонения прямо из диалога находки не дал.** В отличие от детали
     и CG, где канон прямо разрешает создание по ходу (R3, §6), для зоны и типа
     `reference-data.md` говорит, что их **чистит администратор**, — быстрое добавление из
     формы плодило бы дубли-синонимы. В диалоге это обычные выпадающие списки с пустым
     значением (заметка Г). Если оператору это всё же нужно, скажите: точка расширения та же,
     что у «Создать деталь…».

## Доводка по ревью Cowork (после приёмки S4)

S4 принят с одной доводкой. Дефект воспроизвёлся ровно по описанию; починен, две мелочи
приняты как флаги без правки.

- **Замена всех находок за одну правку теперь проходит.** В `_write_findings` порядок
  перевёрнут: **сначала запись** (`update_finding` для существующих, `make_finding` для
  новых), **затем удаление** того, чего в форме не осталось. Диагноз ревью точен — при
  прежнем порядке на последней удаляемой срабатывал гард «должна остаться хотя бы одна»,
  потому что новых находок в базе ещё не было, и сообщение выходило ложным: замена в форме
  была.

  Ловушку с `keep` обошёл, как предписано: множество собирается **по ходу записи**, а не из
  `self._rows` заранее. У новой строки `finding_id` ещё `None`, и посчитанное до записи
  множество не содержало бы только что созданных находок — цикл удаления снёс бы их следом,
  то есть наивная перестановка молча ломала бы больше, чем чинит. Удаление и дальше идёт
  через `remove_finding`: гард по исследованиям и гард «последняя» остаются в силе, но
  теперь видят уже записанные новые находки. Временное сосуществование старой и новой
  находки по одному номеру размера транзакцию не ломает — уникального ограничения на пару
  (отклонение, характеристика) в схеме нет, а дубли в форме блокируются на добавлении.

- **Три теста** (`tests/test_ui_deviation.py`): замена всех находок целиком — сохранение
  проходит, в базе ровно одна находка по №15, опечаточный размер №12 у детали остаётся
  (размер существует независимо от находок, его чистит администратор); регресс на гард по
  исследованиям — перестановка его не отменила; регресс на `1..N` — при пустом списке
  «Сохранить» по-прежнему недоступна, и в базе прежняя находка цела.

  **Проверил, что тест настоящий:** вернул прежний порядок временной подменой — тест падает
  за секунды. Заодно вскрылось, что первая версия теста при провале **вешала прогон**, а не
  роняла его: форма показывает модальный `QMessageBox`, и под offscreen он ждёт ответа
  вечно. Тест теперь перехватывает `show_error` и утверждает, что диалогов не было, — это в
  нём не удобство, а условие того, чтобы регрессия падала, а не висела.

- **Проверки:** `pytest` — **233 passed** (было 230); ручной прогон сценария ревью на живой
  форме (зарегистрировать находку №12 → переоткрыть → добавить №15 → убрать №12 →
  «Сохранить»): сохранение прошло без единого диалога, в базе `['15']`, у детали размеры
  `['12', '15']`. Любой модальный вызов в этом прогоне был заведомо превращён в ошибку.

- **Коммит:** `1b8b2e8` fix(ui): замена всех находок отклонения — писать до удаления, не после

- **Две мелочи — принял как флаги, не правил:**
  1. **N+1 в `_refresh_findings`.** Согласен, и согласен, что чинить надо не здесь: `canon_state`
     открывает сессию на строку. На S5 списки прецедентов сделают это дорогим — там нужен
     пакетный запрос состояния канона сразу по набору номеров, а не точечный вызов в цикле.
     Флаг на S5, как и предложено.
  2. **«Править исследование…» / «Удалить исследование» включены по наличию строк,** а не по
     выбору, и потом отвечают «Сначала выберите». Непоследовательность признаю. Не правил,
     потому что доводка идёт по вердикту ревью, а это уже вне его; чиню в первом же наряде,
     который тронет этот раздел, — либо скажите, и вынесу отдельной строчкой сейчас.
