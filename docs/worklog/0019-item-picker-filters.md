---
part_of: MIS-QMS/docs/worklog
order: 19
task: QMS-016
branch: run/qms-016
status: rework
issued: 2026-09-03
author: Cowork
executor: Claude Code
---

# 0019 — выбор из длинного списка фильтруется по набранному

> Наряд цикла QMS-016. Ветка `run/qms-016`, в `main` не писать ничего.
> Основание — журнал `docs/worklog/0006-run-qms-016.md`, находка **№17**.
> **Находка блокирующая: прогон стоит на шаге 8** (регистрация отклонения №1).

## 1. Причина (установлена по коду, не гипотеза)

**Поле выбора детали — обычный выпадающий список.** `deviation_dialog.py:129`
`self.item = QComboBox()`, наполняется в `reload_items()`. Отсюда ровно то поведение, которое
описал оператор и которое у этого элемента штатное:

* первая набранная буква **перемещает отметку** на первую подходящую запись, но список не
  сужает — на экране по-прежнему все записи;
* вторая буква начинает поиск **заново**, а не уточняет первую;
* буква, которой нет ни в одной записи, не делает ничего — молча;
* при повторном открытии отметка остаётся там, где стояла, и сбросить её нечем: строки ввода,
  которую можно было бы стереть, у элемента нет.

**Инструмент для этого уже написан и не подключён.** `kit/picker.py` несёт `PickerDialog` —
строку отбора, сужение по вхождению подстроки, счётчик «N из M» и объяснение пустого результата
(«значения не исчезли, их скрыл отбор»). Сделан нарядом `0010` по решению **В-6**. Рядом,
в `pickers.py`, написана `pick_item` — «выбрать деталь». **Она не вызывается ниоткуда: мёртвый
код.** Пикер развели по выбору группы (`choose_cg_for_item`), а поле детали осталось прежним.

Докстрока `pickers.py` этот отказ **предсказала дословно**: выбирать `MF5-10375A-N` из трёхсот
прокруткой — «дефект эргономики, который прогон вскрыл бы на шаге 5». Написано заранее; шагом 5
и вскрылось. Инструмент сделали, провод не кинули.

**Тот же класс ждёт в форме детали.** `item_dialog.py` — поле группы характеристик тоже
обычный `QComboBox` (`self.group = _combo()`). Пока групп единицы, это незаметно; на
производственном наборе повторится один в один.

## 1a. Решение пользователя (2026-09-03)

Из трёх вариантов выбран **фильтрующий список прямо в форме**, а не модальный пикер:
набираешь — выпадающий список сужается, стёр букву — расширился. Отклонений за смену много,
и лишнее окно на каждом — плата, которую платить не за что. Модальный `PickerDialog` при этом
**остаётся в деле** там, где выбор происходит не внутри формы.

## 1b. Дополнение 2026-09-03 — две находки вида на форме находки

> Внесено по решению пользователя **до начала работ**: находки **неблокирующие** и по протоколу
> шли бы пакетом на шов, но живут на том же экране и в том же слое, что и §3.1–3.2. Если работа
> по наряду уже начата — исполнитель говорит об этом, и дополнение переносится.

**№18 · В · индикатор радиокнопки отрисован квадратом.** В блоке `Direction` выбранное
состояние — синий скруглённый **квадрат**, невыбранное — серый **круг**: два состояния одного
элемента нарисованы разными фигурами, и индикаторы смещены друг относительно друга по
горизонтали (выбранный левее и крупнее).

Класс известен и назван в реестре решений 2026-09-01: **Qt перехватывает отрисовку у
объявленного** — описание индикатора в листе стиля подменяет системную отрисовку целиком, и без
скругления в половину размера круг превращается в квадрат. Это **шестой** случай того же класса
(выравнивание, направление абзаца, цвет пилюли, индикатор радиокнопки, стрелка списка — и вот
опять индикатор).

**Сам элемент каноничен и не пересматривается:** решение В-9 — радиокнопки при ≤5
взаимоисключающих значениях без умолчания; направление отклонения ровно такое. Чинится вид.

**№19 · В · вертикальные пустоты в блоке «Canon mapping» и «Direction».** Значение `g13` стоит
заметно ниже своей подписи и смещено вправо; радиокнопки разнесены большими промежутками и
тоже уведены вправо. На экране это читается как разрыв формы: подпись в одном месте, значение
в другом, между ними пусто.

**Гипотеза, а не диагноз** (проверяет исполнитель): контейнеры этих двух строк получают
вертикальное растяжение и/или несут собственные отступы поверх отступов формы, поэтому строка
растёт, а содержимое центрируется в выросшей строке.

## 2. Вход (проверить существование первым действием)

| Путь | Что это |
|---|---|
| `docs/worklog/0006-run-qms-016.md` | журнал, находка №17 |
| `src/ui/deviation_dialog.py` | поле детали, строка 129 и `reload_items()` |
| `src/ui/item_dialog.py` | поле группы характеристик |
| `src/ui/kit/picker.py` | `PickerDialog` — образец отбора, счётчика и пустого состояния |
| `src/ui/pickers.py` | `pick_item` (мёртвый код), `pick_group`, `choose_cg_for_item` |
| `src/ui/kit/__init__.py` · `src/ui/kit/widgets.py` | куда встаёт новый компонент |
| `src/ui/kit/direction.py` | `bind_direction`, `directional` — направление поля и строк |
| `src/ui/finding_dialog.py` | форма находки — §3.7, компоновка `Canon mapping` и `Direction` |
| `src/ui/kit/theme.py` | лист стиля — §3.6, индикатор радиокнопки |
| `docs/design/design-system.md` | канон вида: описан ли индикатор радиокнопки (только чтение) |
| `tests/test_ui_kit.py` · `tests/test_ui_deviation.py` · `tests/test_ui_screens_0011.py` | покрытие |

## 3. Что сделать

### 3.1 Компонент набора: поле выбора с отбором по набранному

Новый компонент в `src/ui/kit/` (имя на усмотрение исполнителя, в одном стиле с остальными).
Требования — от поведения, не от реализации:

1. **Любое изменение набранного перестраивает список** — ввод символа, удаление символа,
   вставка, полная очистка. Отбор — по **вхождению подстроки**, регистронезависимо
   (`casefold`), как в `PickerDialog`; не «по первой букве».
2. **Значение поля — всегда ключ из списка**, никогда набранный текст. Наружу отдаётся
   идентификатор (как сейчас `currentData()`), и вызывающий код менять смысл не должен.
3. **Страховка от свободного текста.** Набранное, не совпавшее ни с одной записью, при уходе
   фокуса и при подтверждении формы **откатывается к прежнему выбору**. Пустое поле —
   законное состояние «не выбрано» там, где оно и раньше было законным.
4. **Отбор сбрасывается при закрытии.** Открыл заново — список полон, набранного нет. Сейчас
   отметка остаётся там, где стояла, и это половина жалобы оператора.
5. **Пустой результат обязан быть объяснён**: «ничего не подходит — снимите отбор», а не
   пустота. Пустой список без объяснения читается как «таких деталей нет» — ровно та ошибка,
   от которой `PickerDialog` защищается своим пустым состоянием.
6. **Направление текста.** Каталожные номера латинские, значения справочников бывают
   ивритскими: `bind_direction` на поле ввода, `directional` на список — как в `PickerDialog`.
7. **Счётчик «N из M» не обязателен.** Если он встаёт дёшево — пусть будет рядом с полем;
   если требует ломать компоновку формы — не надо, это не то, ради чего наряд.

### 3.2 Развести по длинным спискам

* **Поле детали** в форме отклонения (`deviation_dialog`) — то, на чём встал прогон.
* **Поле группы характеристик** в форме детали (`item_dialog`) — тот же класс, чинится тем же
  движением; отдельного наряда на него не будет.

**Аудит остальных выпадающих списков.** Пройти по `src/ui/**`, назвать в отчёте, какие поля
остаются простыми списками и почему. Ожидаемо простыми остаются справочники (тип детали, тип
соединения, класс размера, зона, тип отклонения) — они короткие; порог короткого списка в
наборе уже есть (`PICKER_FILTER_THRESHOLD`).

### 3.3 Судьба `PickerDialog` и `pick_item`

`PickerDialog` **остаётся** — он в деле там, где выбор идёт не внутри формы, а по кнопке
(`choose_cg_for_item`: какую группу открывать для привязки). Дублирования тут нет: одно —
поле формы, другое — вопрос перед действием.

`pick_item` — мёртвый код. Либо задействовать, либо удалить; решение принять и **назвать в
отчёте с основанием**. Молча оставить мёртвым нельзя: она уже один раз создала иллюзию, что
выбор детали сделан по-человечески.

### 3.4 Что уходит в канон вида — назвать, не писать

Новый компонент — часть языка экранов, и его место в `docs/design/design-system.md`. Этот файл
ведёт Cowork (INFRA-013). Исполнитель **называет в отчёте**, что должно попасть в канон:
поведение отбора, страховка от свободного текста, сброс при закрытии, порог короткого списка.
Записывает Cowork на шве.

### 3.6 Индикатор радиокнопки — круглый в обоих состояниях (№18)

Выбранное и невыбранное состояния — **одна фигура одного размера**, выровненные по общему
левому краю; отличаются заливкой, а не формой. Лечится в листе стиля `kit`, поштучно по
экранам не правится: элемент один, мест применения уже два (исследование, решение) и теперь
третье.

**Проверяется пикселями, а не свойством виджета** — правило того же решения 2026-09-01: у
кружка работает описание индикатора, у стрелки списка работало только его отсутствие. Что
именно сработает здесь, покажет снимок; заранее не угадывать.

Если канон вида (`docs/design/design-system.md`) индикатор радиокнопки не описывает — **назвать
это в отчёте** (§3.4), запишет Cowork.

### 3.7 Компоновка формы находки — убрать пустоты (№19)

Значение стоит **в одной строке со своей подписью**; вертикальные промежутки — общие для формы,
без специальных отступов у этих двух строк; блок направления не уводится вправо дальше прочих
полей. Ориентир — остальные строки той же формы (`Local number`, `Value`, `Zone`), которые
выглядят правильно: цель не «сделать красиво», а привести две выпадающие строки к тому же
ритму, что у соседей.

Правка делается **в компоновке формы**, а не подгонкой отступов числами в обход набора: гард
«ни одного числа оформления вне `kit`» остаётся в силе.

### 3.5 Покрытие

1. набранная подстрока сужает список; совпадение **не только по началу** строки;
2. удаление символа расширяет список обратно; полная очистка возвращает всё;
3. подстрока, которой нет ни в одной записи, даёт пустой список **с объяснением**;
4. повторное открытие поля начинается с полного списка и пустого отбора;
5. набранный текст, не совпавший с записью, откатывается к прежнему выбору;
6. выбор отдаёт **ключ**, а не подпись — форма отклонения получает `item_id`;
7. форма отклонения с выбранной через отбор деталью сохраняется, и отклонение ложится на ту
   деталь, которую выбрали.

## 4. Чего НЕ трогать

- Схему, миграции, `main`.
- `MappingDialog` и `PickerDialog` — оба в деле и работают.
- Подписи `run(...)`.
- Долг к шву (`RUN_PROTOCOL_QMS-016` §6) — он идёт своим нарядом, сюда не подмешивается.
- **Запрет §3а наряда `0010`** («механики эталонного экрана не тиражируются») здесь **не
  нарушается** и обсуждать его заново не нужно: запрет про **аппарат экрана** — поиск по
  выдаче, срезы, чипы фильтров, экспорт вида. Отбор внутри поля сужает **выбор одного
  значения**, а не меняет, чем список является. Ровно это основание записано в докстроке
  `kit/picker.py`, когда тем же способом вводился `PickerDialog`.

## 5. Критерии приёмки

1. В форме отклонения набор в поле детали **сужает список по вхождению подстроки**; удаление
   символа расширяет; чужая подстрока даёт объяснённый пустой результат.
2. Повторное открытие поля — полный список, отбор пуст.
3. Набранный текст, не совпавший ни с одной деталью, откатывается к прежнему выбору; в базу он
   попасть не может.
4. Отклонение сохраняется на **выбранную** деталь (проверяется по `item_id`, не по подписи).
5. Поле группы в форме детали ведёт себя так же.
6. Аудит остальных выпадающих списков проведён, остающиеся простыми названы с основанием.
7. Судьба `pick_item` решена и обоснована в отчёте.
8. **Индикатор радиокнопки — круг в обоих состояниях**, одного размера, по общему левому краю.
   Проверяется **снимком**, приложенным к отчёту (`build/screens/`), а не свойством виджета.
9. **Форма находки без пустот:** `Canon mapping` и `Direction` стоят в том же ритме, что
   соседние строки; значение — в строке своей подписи. Снимок до и после в отчёте.
10. Тесты зелёные, число назвать явно (было 446).

> [!note] Объём базы прогона мал (четыре детали), поэтому живьём проверяется **механика**, а
> не поведение на трёхстах записях. Это объявлено заранее и расхождением приёмки не считается.

## 6. Отчёт исполнителя

### Входной контроль

Работа по прежней редакции наряда **не начиналась** — наряд прочитан сразу с §1b, §3.6 и §3.7,
дополнение исполнено вместе с остальным, переносить нечего.

Все одиннадцать строк раздела «Вход» проверены **до начала работы**; за ними четырнадцать
файлов, и все существуют: `docs/worklog/0006-run-qms-016.md` · `src/ui/deviation_dialog.py` ·
`src/ui/item_dialog.py` · `src/ui/kit/picker.py` · `src/ui/pickers.py` ·
`src/ui/kit/__init__.py` · `src/ui/kit/widgets.py` · `src/ui/kit/direction.py` ·
`src/ui/finding_dialog.py` · `src/ui/kit/theme.py` · `docs/design/design-system.md` ·
`tests/test_ui_kit.py` · `tests/test_ui_deviation.py` · `tests/test_ui_screens_0011.py`.

### Сделано

**3.1 · Компонент `kit.FilterCombo`** (`src/ui/kit/combo.py`). Поле выбора, сужаемое
набранным. Все семь требований — от поведения:

1. **любое** изменение набранного пересобирает список (`filter_to`), отбор по вхождению
   подстроки, `casefold`;
2. значение — **всегда ключ**: `currentData()` переопределён и отвечает выбранным ключом
   независимо от состояния отбора. Обычный вернул бы данные текущей строки **суженного**
   списка, то есть значение, которого оператор не выбирал;
3. страховка от свободного текста: `settle()` при уходе фокуса, при закрытии списка и по
   вызову формы восстанавливает прежний выбор;
4. отбор не переживает закрытие: `hidePopup()` зовёт `settle()`;
5. пустой результат объяснён строкой «Nothing matches … — all N values are still there, the
   filter hides them»; строка **выключена**, значением стать не может;
6. `bind_direction` на строке ввода, `directional` на списке — как в `PickerDialog`;
7. счётчик «N из M» не делал: §3.1.7 разрешает, а объяснение пустого результата несёт ту же
   службу без ломки компоновки формы.

Своё **пустое состояние**: стёртая строка — законное «не выбрано» (деталь ещё не названа,
деталь без группы). Живёт это в жесте оператора (`_on_typed`), а не в `settle`: смешать их
значило бы снимать программный выбор по ключу — строка в этот момент ещё пуста. Найдено
собственной проверкой контракта, до тестов.

**3.2 · Разведено по двум длинным спискам.** Поле детали в форме отклонения и поле группы в
форме детали. Заодно у формы детали исчез запрос группы по имени при сохранении: наружу
теперь идёт `created_group_id`, взятый ключом поля.

**3.6 · Индикатор радиокнопки.** Причина — не «Qt перехватывает отрисовку», как ожидал наряд,
а `width`/`height` в QSS: они задают **content-box**, и рамка в четыре пикселя у `:checked`
растила весь индикатор. Замерено: отмеченный **19×17** против невыбранного **17×17**, углы
закрашены на **15 из 16** против **0 из 16** у круга. Оба состояния приведены к одной рамке в
один пиксель и одному радиусу; отличаются **заливкой**. Снимки «до» и «после» приложены.

**3.7 · Компоновка формы находки.** Гипотеза наряда (собственные отступы контейнеров)
подтвердилась **наполовину**, и главное было не в ней. Замер до правки:

| строка | высота поля | смещение подписи |
|---|---|---|
| `Local number`, `Value`, `Zone`, … | 28 px | 0 |
| **`Canon mapping`** | **138 px** | 0 |
| **`Direction`** | **138 px** | 0 |

Подписи стояли ровно; расходилось **содержимое**, центрированное в выросшей строке. Росли
ровно те два поля, чьи виджеты умеют расти, — переносимая подпись канона и контейнер
направления, — потому что свободную вертикаль диалога распределяла форма. Лечится одной
строкой: `layout.addStretch(1)` перед кнопками отправляет её вниз. Отдельно подтвердилась и
вторая половина гипотезы: у голого `QVBoxLayout` в контейнере направления оказались отступы
**9 px со всех сторон** — это и уводило радиокнопки вправо; заменено на `kit.column`. После
правки: `Canon mapping` 17 px, `Direction` 42 px (две кнопки плюс зазор канона), ритм строк
общий. Ни одного числа оформления мимо набора не заведено.

### Аудит выпадающих списков

Считано по базе прогона, а не по умолчанию сида.

| Поле | Что показывает | Записей сейчас | Решение |
|---|---|---|---|
| `deviation_dialog.item` | детали | растёт до сотен | **`FilterCombo`** — то, на чём встал прогон |
| `item_dialog.group` | группы характеристик | растёт до десятков | **`FilterCombo`** — тот же класс |
| `item_dialog.item_type` | тип детали | 7 | простой список |
| `item_dialog.connection_type` | тип соединения | 5 | простой список |
| `item_dialog.size` | класс размера | 4 | простой список |
| `finding_dialog.zone` | зона | 6 | простой список |
| `finding_dialog.deviation_type` | тип отклонения | 7 | простой список |
| `inspection_dialog.kind` | тип исследования | 3 | простой список |

**Основание оставить простыми:** все шесть — справочники, и все ниже порога
`PICKER_FILTER_THRESHOLD` (12), которым набор уже пользуется в `PickerDialog`. Порог назван не
на глаз: он ратифицирован решением В-6. Ближе всех к нему `ref_item_type` и
`ref_deviation_type` (по 7); если справочник перевалит порог, поле меняется одной строкой —
компонент уже есть.

### Судьба `pick_item` — оставлена, и вот почему

**`pick_item` не мёртвый код.** §1 и §3.3 наряда утверждают обратное («Она не вызывается
ниоткуда»), но вызов есть: `src/ui/cg_view.py:166`, кнопка «Mapping…» раздела «Группы
характеристик» — «какую деталь привязать к этой группе». Проверено поиском по всему `src/`.

Оставляю без изменений, и это ровно тот случай, который §3.3 сам описывает: выбор идёт **не
внутри формы, а вопросом перед действием**. Удалить её значило бы сломать живую кнопку.

### Критерии приёмки

| # | Критерий | Как проверено |
|---|---|---|
| 1 | Набор сужает список по вхождению; удаление расширяет; чужая подстрока объяснена | ✅ `test_the_item_field_narrows_as_you_type` — по видимым строкам поля, на двух похожих номерах (`C1-08375A` и `MF5-10375A-N`); плюс три теста компонента |
| 2 | Повторное открытие — полный список, отбор пуст | ✅ `test_the_item_field_forgets_the_filter_when_it_closes`, `test_closing_the_list_drops_the_filter` |
| 3 | Несовпавший текст откатывается, в базу не попадает | ✅ `test_typed_nonsense_cannot_reach_the_database`, `test_free_text_never_becomes_the_value` |
| 4 | Отклонение ложится на выбранную деталь — по `item_id` | ✅ `test_the_deviation_lands_on_the_item_chosen_through_the_filter`: деталь выбрана отбором по середине номера, сверка по ключу, не по подписи |
| 5 | Поле группы ведёт себя так же | ✅ `test_the_group_field_narrows_the_same_way` — включая то, что наружу идёт `cg_id`, а не имя |
| 6 | Аудит проведён, остающиеся простыми названы с основанием | ✅ таблица выше: восемь полей, шесть остаются простыми, порог назван |
| 7 | Судьба `pick_item` решена и обоснована | ✅ оставлена; посылка наряда о мёртвом коде опровергнута вызовом `cg_view.py:166` |
| 8 | Индикатор — круг в обоих состояниях, одного размера, по общему левому краю; **снимком** | ✅ `build/screens/17-radio-indicator.png` против `build/screens/0019-before/17-radio-indicator.png`: было — синий скруглённый квадрат крупнее серого круга, стало — два круга одного размера, отличие в заливке. Тест `test_the_radio_indicator_is_the_same_circle_in_both_states` меряет рамку, размер, левый край и углы |
| 9 | Форма находки без пустот; снимок до и после | ✅ `build/screens/08-dialog-finding.png` против `build/screens/0019-before/08-dialog-finding.png`; числа замера — в разделе «Сделано» |
| 10 | Тесты зелёные, число названо явно | ✅ **461 тест, все зелёные** (было 446) |

### Число тестов

**Было 446 → стало 461**, прирост +15:

* `tests/test_ui_kit.py`: 17 → **27**. Восемь на компонент (вхождение подстроки, расширение
  при стирании, объяснение пустого, сброс отбора при закрытии, откат свободного текста,
  снятие выбора стиранием, ключ вместо подписи, ивритское значение) и два на индикатор —
  пиксельный и обратный к нему («одинаковость не достигается исчезновением отметки»).
* `tests/test_ui_deviation.py`: 37 → **41**. Четыре на поле детали в форме, включая сверку
  отклонения по `item_id`.
* `tests/test_ui_smoke.py`: 29 → **30**. Поле группы в форме детали.

**Проверка на непустоту.** Вернул прежнее правило индикатора — тест падает и называет причину
(«углы индикатора закрашены — это квадрат, а не круг: 15 из 16»); вернул правку — зелено.

### Что должно попасть в канон вида

Пишет Cowork (INFRA-013); здесь — список.

1. **Компонент «поле выбора с отбором»** — новый элемент языка экранов: отбор по вхождению
   подстроки, значение всегда ключ, свободный текст откатывается, отбор снимается при закрытии
   списка, пустой результат объяснён.
2. **Порог длинного списка** — `PICKER_FILTER_THRESHOLD` (12) становится общим для двух
   компонентов: до него простой список, выше — с отбором. Сейчас порог назван только в
   `PickerDialog`.
3. **Индикатор радиокнопки в каноне не описан вовсе** — проверено поиском по
   `docs/design/design-system.md` (§3.6 просил назвать, если так). Записать стоит не картинку,
   а правило: **два состояния — одна фигура одного размера, отличие только в заливке**; в QSS
   это значит одинаковую толщину рамки, потому что `width`/`height` задают content-box.
4. **Пустое состояние поля** — «ничего не выбрано» показывается подсказкой строки ввода, а не
   строкой-заглушкой в списке (`— pick an item —` перестал быть значением списка).

### Коммиты

* `feat(ui):` наряд 0019 — поле выбора с отбором, круглый индикатор, компоновка формы находки.
* `docs(worklog):` этот отчёт.

### Замеченное по ходу — Cowork

1. **Пиксельные тесты этого файла зависели от порядка запуска.** Лист стиля в тестах ставит
   **соседний** тест (`test_the_theme_does_not_inherit_the_system_palette`, приложение живёт
   сеанс), поэтому запущенный в одиночку пиксельный тест меряет **родной стиль Windows**, а не
   наш. То есть проверка отрисовки могла быть зелёной, ничего нашего не измерив. Свои два теста
   я сделал самодостаточными (`kit.apply_theme` внутри); прежний
   `test_the_choice_marks_the_selected_option` эту зависимость сохраняет — на шов.
2. **Прежний пиксельный тест выбора пропускал именно этот дефект.** Он считал «есть ли синие
   пиксели» — а квадрат синий не меньше круга. Это тот же класс, что «сверяй то, чем рисуют»,
   но на шаг тоньше: считать пиксели мало, надо считать **ту величину, которая отличает
   верное от неверного**. Кандидат в §9 одной строкой.
3. **Посылка наряда про мёртвый код была неверна** (см. «Судьба `pick_item`»). Проверка стоила
   одного `grep`, и без неё правка сломала бы кнопку «Mapping…» раздела групп. Отмечаю не в
   упрёк, а как повод: посылки вида «не вызывается ниоткуда» стоит проверять исполнителю
   первым же действием — они дешевле всего проверяются и дороже всего обходятся.
4. **Гипотеза §3.7 подтвердилась наполовину, и это стоило замера.** Отступы контейнера были
   (9 px), но пустоту делало не они, а растяжение двух строк до 138 px. Если бы правил по
   гипотезе — убрал бы отступы и оставил вертикальные пустоты на месте.
5. **`—  pick an item —` перестала быть строкой списка.** Тест, сверявший `currentText()` с
   этой подписью, теперь смотрит на подсказку и на `currentData() is None`. Поведение для
   оператора то же, но если канон вида где-то описывает «первая строка списка — приглашение»,
   его надо поправить (в `design-system.md` такого описания я не нашёл).


---

## 7. Доводка по ревью — приёмка §3.1–3.2 ОТКЛОНЕНА (Cowork, 2026-09-03)

> Проверка на реальной базе пользователем. Поле выбора детали **не работает**; состояние хуже
> исходного (`QComboBox` хотя бы не терял набранное). §3.6 и §3.7 доводки не требуют.

### 7.1 Что наблюдается (ФАКТ, слова оператора)

1. **Набранного не видно в строке** — вводишь букву, в поле её нет.
2. **Показывается не то, что отобрано:** на букву `м` список сужается до одной строки, и в ней
   стоит `маккад` — запись, в которой буквы `м` нет вовсе.
3. **Список ростом в одну строку прокручивается**, и в нём видны **все** детали — то есть
   содержимое списка и его высота относятся к разным моментам.
4. **Повторного отбора по каждой правке нет** — то, ради чего наряд и писался.

### 7.2 Механизм (ВЫВОД по коду; на машине не воспроизводился — у Cowork нет shell)

Компонент **одновременно владеет собственной моделью списка и восстанавливает её из
обработчиков жизненного цикла виджета**. Эти две обязанности дерутся на каждом нажатии:

* `_on_typed` → `filter_to` → `_show` → `self.clear()`. У редактируемого `QComboBox` модель —
  **тот же источник, из которого берётся текст строки ввода**: `clear()` стирает строку, а
  последующее заполнение ставит в неё подпись текущего элемента.
* `hidePopup()` **переопределён и зовёт `settle()`**, а `settle()` возвращает **полный** список
  (`_show(self._rows, "")`) и переписывает строку — либо подписью выбранного, либо пустотой.
  Qt закрывает выпадающий список не только по воле оператора: сброс модели во время открытого
  списка — одна из таких причин. Тогда порядок на одно нажатие получается такой: отобрали →
  Qt закрыл список → `settle` вернул всё обратно → `showPopup()` открыл список, высота которого
  посчитана по прежнему, суженному содержимому.

Это объясняет все четыре наблюдения разом, включая самое странное — «одна строка высотой, а
внутри все записи» (содержимое от `settle`, высота от отбора) и «`маккад` на букву `м`»
(в строке оказалась подпись текущего элемента полного списка, а не результат отбора).

**Корень — не отделочная ошибка, а устройство компонента.** Любая правка внутри этой схемы
будет заново разбираться с тем, что модель списка и текст поля — одно и то же.

### 7.3 Что сделать (требование, не пожелание)

**Строка ввода и список — разные вещи, и модель списка на набор текста не перестраивается.**

**Предпочтительный путь: собрать поле самим** — `QLineEdit` плюс всплывающий `QListView`
(`Qt.Popup`), без `QComboBox` вовсе. Ровно тот состав, что уже работает в `PickerDialog`
(строка отбора + список + объяснение пустого), только не модальным окном, а всплытием под
полем. Тогда:

* текст в строке принадлежит **только** оператору — ничто в него не пишет во время набора;
* список — отдельный виджет над **полным** набором, отбор меняет его содержимое и **высоту**
  вместе, потому что перестраивается он один;
* «выбор» — по-прежнему ключ (`current_key`), контракт наружу не меняется.

**Допустимая альтернатива**, если исполнитель покажет, что она держит все четыре проверки:
оставить `QComboBox`, но **не трогать его модель при наборе** — отбор отдать `QCompleter`
(`MatchContains`, всплывающий режим) или прокси-модели над полной. `hidePopup()` при этом
**не переопределять**: восстановление состояния на закрытии списка и есть то, что стирает
набранное посреди работы; сброс отбора — по уходу фокуса.

**Запрещено в любом варианте:** писать в строку ввода из кода, пока строка в фокусе и оператор
набирает.

### 7.4 Проверка — руками, до тестов

Наряд `0019` был закрыт зелёными тестами и не работал у оператора с первого нажатия. Значит
проверка была не той. **Первым делом — прогон руками** на базе прогона, по четырём симптомам
§7.1; только после этого тесты.

Тесты обязаны воспроизводить каждый из четырёх:

1. после каждого нажатия строка содержит **ровно набранное**;
2. содержимое списка после каждого нажатия равно отобранному набору — сверять **список
   подписей**, а не «сузился ли»;
3. число строк в списке и его высота относятся к одному состоянию (проверяется числом строк
   модели против числа видимых);
4. подстрока **в середине** подписи отбирает, подстрока-отсутствующая даёт объяснение, стирание
   символа расширяет.

### 7.5 Критерии приёмки доводки

1. Четыре симптома §7.1 не воспроизводятся **на живой сборке**, проверено руками; в отчёте —
   что именно набиралось и что показывалось.
2. Модель списка при наборе не перестраивается — либо списка `QComboBox` нет вовсе (свой
   виджет), либо отбор идёт мимо неё.
3. `hidePopup()` не переопределён восстановлением состояния (или `QComboBox` не используется).
4. Контракт наружу не изменился: `current_key()` / `currentData()` отдают ключ, форма
   отклонения и форма детали работают как прежде.
5. Тесты зелёные, число назвать явно (было 461); четыре теста §7.4 — новые.
6. Снимок поля с набранной подстрокой и суженным списком — в `build/screens/`.

### 7.6 Отчёт исполнителя по доводке

### Входной контроль

Пути §2 проверены заново, все четырнадцать файлов на месте; правились
`src/ui/kit/combo.py`, `src/ui/kit/tokens.py`, `tools/screenshots.py` и четыре файла тестов.
`src/ui/deviation_dialog.py` и `src/ui/item_dialog.py` **не менялись вовсе** — контракт наружу
тот же, и это часть проверки, а не совпадение.

### Что сделано

**Компонент собран заново — свой виджет, `QComboBox` нет** (предпочтительный путь §7.3).
Строка ввода `QLineEdit` и всплывающий `QListWidget` (`Qt.Popup`) — **разные виджеты со своими
моделями**. Отбор перестраивает только список; в строку код пишет ровно в двух случаях, и оба
— когда жест окончен: выбрана строка или ушёл фокус. `hidePopup()` не переопределён — его
здесь нет вовсе, вместе с `QComboBox`.

Устройство, которое это держит:

* `_on_typed` трогает **только** список — ни одного `setText` во время набора;
* высота всплытия считается **после** заполнения, поэтому содержимое и рост относятся к одному
  состоянию;
* уход фокуса различается **по причине**: `PopupFocusReason` (всплытие само забрало фокус) —
  не трогаем ничего; любая другая — жест окончен, откатываем набранное к выбранному;
* объяснение пустого результата — строка с `NoItemFlags`: выбрать её нельзя;
* `POPUP_ROWS` (8) заведён токеном — числа оформления мимо набора не пошли.

**Контракт наружу не тронут:** `current_key()` / `currentData()` отдают ключ,
`set_rows` / `select_key` / `setCurrentText` / `keyChanged` / `lineEdit()` на месте. Формы не
правились ни строкой.

### Прогон руками по §7.1 — до и после

Прогон настоящими событиями клавиш (`QKeyEvent` с текстом) по **показанной** форме отклонения,
на копии `data/qms016.sqlite`, дополненной похожими номерами и записью `маккад` — без похожих
записей симптом №2 не виден. `QTest.keyClick` не годится: кириллицу он калечит, доходит `Ð`.

**Первое, что выяснилось: без `show()` дефект не воспроизводится вовсе.** Всплывающий список у
скрытого виджета не открывается, и весь механизм отказа не запускается. Это и есть ответ на
вопрос §7.4, почему 461 зелёный тест ничего не поймал: тесты звали внутренние методы у
непоказанного виджета — то есть проверяли путь, которого у оператора нет.

| Что делаю | Было (первая редакция) | Стало |
|---|---|---|
| нажал `м` | строка `''`, список полон (9) | строка `'м'`, список `['маккад']` |
| нажал `а` | строка `'а'`, а в списке `маккад` | строка `'ма'`, список `['маккад']` |
| нажал `к` | строка `'ак'` — отстаёт на нажатие | строка `'мак'`, список `['маккад']` |
| набрал `10375` | работало, но первая буква съедалась | строка `'10375'`, список из двух |
| стёр символ | — | строка `'1037'`, список из трёх |
| высота против содержимого | 3 строки в модели, помещается **1.2** | 3 строки, помещается **3.2** |

Все четыре симптома §7.1 сняты; каждый проверен числом, а не впечатлением.

### Критерии приёмки доводки

| # | Критерий | Как проверено |
|---|---|---|
| 1 | Четыре симптома не воспроизводятся на живой сборке, руками | ✅ таблица выше: что набиралось и что показывалось на каждом нажатии |
| 2 | Модель списка при наборе не перестраивается | ✅ списка `QComboBox` нет вовсе: `grep QComboBox src/ui/kit/combo.py` даёт только упоминания в пояснениях. Отбор правит отдельный `QListWidget` |
| 3 | `hidePopup()` не переопределён | ✅ метода нет; сброс отбора — по уходу фокуса, и только когда причина не `PopupFocusReason` |
| 4 | Контракт наружу не изменился | ✅ `deviation_dialog.py` и `item_dialog.py` не правились; тесты обеих форм зелёные, включая сверку отклонения по `item_id` |
| 5 | Тесты зелёные, число названо; четыре теста §7.4 — новые | ✅ **463 теста** (было 461). Четыре теста §7.4 добавлены под своими номерами симптомов |
| 6 | Снимок поля с набранной подстрокой и суженным списком | ✅ `build/screens/18-filter-field.png`: в строке `1037`, в списке `MF5-10375A-N` и `MT-SD1037A` — вхождение в середине у обоих |

### Число тестов

**Было 461 → стало 463.** Прирост маленький, потому что почти все тесты компонента
**переписаны**, а не добавлены: восемь прежних звали внутренние методы и проверяли то, чего
оператор не делает.

* `tests/test_ui_kit.py`: 27 → **29**. Десять тестов компонента, все — настоящими нажатиями по
  показанному полю: четыре по симптомам §7.1 (строка после каждого нажатия; список ровно
  отобранного; высота против содержимого; вхождение в середине плюс стирание), плюс объяснение
  пустого, откат свободного текста, ключ вместо подписи, выбор строки, снятие выбора стиранием
  и ивритское значение.
* `tests/test_ui_deviation.py`: 41 → **41**. Два теста поля переписаны на нажатия по показанной
  форме; остальные не тронуты.
* `tests/test_ui_smoke.py`: 30 → **30**. Поле группы — так же.
* `tests/conftest.py`: помощники `press_key` / `type_keys` / `backspace` / `clear_line` /
  `leave_field` / `shown_field` — один набор на все файлы.

**Тесты нашли два настоящих дефекта, которых не было в списке §7.1** — оба в новом компоненте,
оба до сдачи:

1. **Уход фокуса с открытым всплытием не откатывал набранное.** Первая версия смотрела на
   видимость списка вместо причины ухода: ушёл по Tab, пока список открыт, — в строке остался
   несовпавший текст. Лечится различением `PopupFocusReason`.
2. **Показанные окна переживали свой тест.** Всплытие — окно с захватом ввода, и оставшееся от
   соседнего теста ломало доставку событий следующему. Заведена автоматическая уборка окон
   после каждого теста.

### Коммиты

* `fix(ui):` доводка наряда 0019 — поле выбора собрано своим виджетом.
* `docs(worklog):` этот отчёт.

### Замеченное по ходу — Cowork

1. **Правило, которое я нарушил и которое стоит записать: тест поведения виджета обязан
   показывать виджет.** Не «модальное окно вешает прогон» (это уже есть), а обратное: часть
   механики Qt — всплытия, захват ввода, фокус — у скрытого виджета **не запускается вовсе**,
   и тест молча проверяет пустоту. Мои десять тестов теперь показывают поле; уборка окон после
   теста — плата за это и тоже часть правила.
2. **Под offscreen окно теста не становится активным, и `setFocus()` соседнего виджета не
   порождает фокусных событий вовсе** (замерено: `hasFocus()` у строки всё время `False`).
   Поэтому уход из поля в тестах шлётся настоящим `QFocusEvent` с причиной. Это не обход
   проверки, а единственный способ доставить событие, которого система в этом режиме не
   присылает; вне pytest, на активном окне, тот же сценарий проходит через настоящий `setFocus`
   — проверено отдельно.
3. **Диагноз Cowork по коду оказался верным целиком** — включая самое неочевидное, «одна строка
   высотой, а внутри все записи». Замер это подтвердил числом: 3 строки в модели, 1.2 строки по
   высоте. Отмечаю, потому что диагноз ставился **без запуска**, по одному чтению кода.
4. **Пункт «отбор сбрасывается при закрытии списка» из §3.1.4 первой редакции снят самой
   доводкой.** Теперь отбор живёт до ухода фокуса: закрытие всплытия кликом мимо — не конец
   работы с полем, и стирать набранное на нём было бы тем же дефектом. В канон вида (§3.4)
   надо записать именно эту формулировку, а не прежнюю.
