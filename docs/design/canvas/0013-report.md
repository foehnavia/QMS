# Наряд 0013 — отчёт исполнителя (слой дизайна)

Дата: 01.09.2026. Канон сверялся по `design-system.md` **revision 1.3** (после доводки).
Файлы отданы в `docs/design/canvas/`: три канвы, `support.js`, `README.md`, этот отчёт.

## Входной контроль

Доступны и прочитаны: `docs/design/design-system.md` (revision 1.3), `docs/model/`
(Deviation, CharacteristicGroup, Search, DeviationCard, Import-Workflow,
reference/reference-data), `docs/specs/` (deviation-entry, cg-editor-and-mapping),
`docs/design/canvas/`. Наряда `docs/worklog/0013-*.md` в ветке нет — работа шла по файлу,
переданному в чат.

## Сделано по пунктам 1–8

1. **М-2 · 1b и 1c.** Убраны из эталонной канвы вместе с вёрсткой; на их месте рамка
   «отменены 01.09.2026» с причиной (шасси, снятое С-4) и ссылкой на действующее 1a.
   Ссылок `#1b` / `#1c` в файле не осталось; две фразы, называвшие 1b рекомендованным,
   переписаны.
2. **М-3 · поля.** Сняты `Hebrew anchor`, `Code`, `Last change` в S2 (обе ширины: шапка,
   строки, подпись сетки, пустое состояние) и `Hebrew anchor`, `Code` в SK1. Из списка
   справочников убраны `Decision outcomes` и `Machines`, добавлен `Size classes` (модель его
   несёт), он же играет пустой список. Дорисованы `Machine`, `NCR`, `Attachments` в S10 и
   `Decision date` в S13 — по `Deviation.md` и spec `deviation-entry` §3, §5: Machine и NCR
   строки, Attachments многострочное поле ссылок (файлы в базу не копируются), дата решения
   перезаписывается при смене решения.
   **Следствие доведено до конца** (решение Cowork от 01.09): ивритская половина ячейки зоны
   убрана в S10, S11, S14 и в эталоне. Ширины колонок не двигались — сетка фиксирована, ушло
   только содержимое ячейки.
3. **М-4 · четыре спора.**
   - `Drawing · required` → `Drawing` в S5 и `Drawing with marked g-positions` в SK3, с
     формулировкой канона «группа без чертежа работает, баллоны падают на сетку». Заодно
     лимит файла `20 МБ` → `5 МБ` (architecture §4–§5).
   - S8: канва баллонов с тремя состояниями, ввод местного номера **на баллоне**, строка
     «запись идёт по действию», кнопки **Done / Close** вместо «Save mapping / Cancel»,
     Done заблокирован пока хоть один баллон без состояния (spec cg-editor-and-mapping §4).
   - Шаг 10 снят: в S7 номера пустые и вводятся оператором (одна строка показана в наборе),
     в S6 и S8 мок-данные несут неравномерные номера с чертежа.
   - S14: уровень L1 двумя секциями — **L1a** та же деталь и тот же размер (3), **L1b**
     другие детали на той же g-позиции (1); вкладки подписаны L1 / L2, статусная строка
     называет оба числа.
4. **М-5 · язык.** Внутри нарисованных окон русского нет: значения ячеек, подписи и
   плейсхолдеры контролов, статусные строки, легенды, боксы-уведомления, пустые состояния и
   три модальных сообщения переведены. Русскими оставлены подписи к артбордам, панели
   пояснений и подсказки под полями — по прецеденту §11.
5. **§5 · иконки.** Все наборы в трёх канвах — сетка проектирования 24, штрих 1.7,
   `stroke-linecap` / `linejoin: round`. Размеры отрисовки приведены к §3 канона: 16 в ленте
   и панели, 13 в строке и в пилюле, 34 в пустом состоянии. Иконка `collapse-menu` удалена
   как остаток снятого С-4 шасси.
6. **§6 · клавиши.** Сняты все подсказки: `Enter`, `F2`, `Ctrl+D`, `Ctrl+M`, `Ctrl+F`,
   `Ctrl+колесо`, `Ins`, `Del`, `Esc`, `↑↓` — S1, S2, S3, S4, S6, S14, S16 и эталон.
7. **§7 · rev.** Ни одна канва номера ревизии не несёт; все три и `README.md` датированы
   01.09.2026. Утверждения канв о ревизии канона («канон rev. 1.4») сняты.
8. **§8 · сверка с каноном revision 1.3 — расхождение осталось одно.** См. ниже.

## Расхождения канвы с каноном после правок

**Сошлось.** Шесть уплотнённых высот (48 · 26 · 28 · 26 · 34 · 30) внесены в §3 канона
доводкой и совпадают с канвой; лента 44 и строка 40 не двигались. Иконки: сетка 24, штрих
1.7, скруглённые концы, размеры 16 / 13 / 34. Шрифтовой стек: из двух канв убраны
`Segoe UI Historic` и `Arial Hebrew`. Минимум окна 1280×760. Сняты устаревшие утверждения
канв о каноне: С-1 и С-2 значились открытыми (канон закрыл их измерением), «запасной
механизм tnum в §9» (в канон не вошёл), «семь высот строк» (канон несёт «одно состояние,
одно число», строка 40).

**Осталось — палитра и радиусы:**

| Что | Канва | Канон revision 1.3 |
|---|---|---|
| Акцент | `#2A6FB8` | `blue-600 #2563D9` |
| Рамка контрола | `#C9D2DA` | `n-250 #DCE0E6` |
| Основной текст | `#16202A` | `n-900 #1B2027` |
| Радиусы | 3–4 | кнопка/поле 5 · карточка 7 · действие строки 4 · пилюля 11 |

Это два набора значений, а не дефект витрины: канва рисует свою палитру с 31.08, канон
записан в другой. Перекраска канвы задела бы принятый эталон, поэтому сторону выбирает
Cowork. Числа выписаны здесь целиком — таблицы хватает, чтобы решить, не открывая канву.

## Мелкие расхождения внутри самого канона (замечены при сверке)

- §3 задаёт вкладки 34 и статус-бар 26, а §10 в описании компонентов всё ещё говорит
  `slice_tab` «a tab strip 44 px» и `status_bar` «the 36 px footer».
- §3 после доводки перечисляет высоты 48 · 26 · 28 · 26 · 34 · 30, но абзац про ленту ниже
  по-прежнему считает вертикальный бюджет по старым числам:
  «44 + 64 + 52 + 44 + 34 + 36 = 274 ... If vertical space ever runs short, the 64 px section
  header is what gives».
- §3 не называет размер иконки в модальном сообщении (знает 16 · 13 · 34); S15 рисует 22.

## Вопросы Cowork

1. Палитра и радиусы (таблица выше) — какая сторона правится.
2. Три расхождения внутри канона из раздела выше — правятся в каноне.
3. **Адрес `Drawing · required`.** §11 наряда утверждал, что в S5 такого требования нет.
   Фактически было — и в поле, и в правиле «Create отключён, пока имя и чертёж не заданы».
   Исправлено в обоих местах.
