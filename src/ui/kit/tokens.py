"""Токены дизайн-системы — единственное место, где живут числа оформления.

Соответствие один в один с `docs/design/design-system.md` §1–§3 (ревизия 1.1):
имя константы = имя токена канона, значение = значение канона. Ничего
производного здесь не считается — производные собирает `theme`.

Гард `tests/test_ui_kit.py` следит, чтобы шестнадцатеричный цвет, кегль,
радиус и высота не появились нигде в `src/ui/`, кроме этого пакета.
"""

from __future__ import annotations

# --- §1 Colour: синий — единственный акцент --------------------------------------

BLUE_50 = "#EFF5FE"
BLUE_100 = "#E4EDFC"
BLUE_500 = "#3B7BE8"
BLUE_600 = "#2563D9"
BLUE_700 = "#1B4FBF"
BLUE_HALO = "#DCE7FB"

# --- §1 Colour: нейтральные, холодные --------------------------------------------

WHITE = "#FFFFFF"
N_50 = "#F7F9FB"
N_100 = "#EFF2F5"
N_200 = "#E3E7EC"
N_250 = "#DCE0E6"
N_300 = "#C7CDD5"
N_350 = "#A9B1BC"
N_400 = "#8B94A1"
N_450 = "#B4BCC7"
N_500 = "#6C7683"
N_600 = "#4A525D"
N_700 = "#3C444E"
N_900 = "#1B2027"

# --- §1 Colour: лента навигации — единственная тёмная плоскость ------------------

RIBBON = "#16324F"
RIBBON_TEXT = "#FFFFFF"
RIBBON_MUTED = "#9FB3CC"
RIBBON_ACTIVE = "#1E4470"
RIBBON_BORDER = "#0F2438"

# --- §1 Colour: решение — четыре исхода плюс открытое состояние ------------------

#: `code -> (фон, текст, точка)`. `None` в фоне — пилюля рисуется контуром;
#: контурная ровно одна, и это «решения ещё нет» (канон §1).
DECISION_COLOURS: dict[str | None, tuple[str | None, str, str]] = {
    None: (None, N_500, N_350),
    "approved": ("#E4F4EA", "#1E6B3F", "#2E9155"),
    "rejected": ("#FCE8E8", "#99312F", "#C7433F"),
    "sorting": ("#FDF0DC", "#8A5A15", "#C58A2A"),
    "repair": ("#EFE7FA", "#5C3D96", "#8763C7"),
}

#: Рамка контурной пилюли и опасной кнопки — цвета, которых нет в шкале выше.
PILL_DASH = N_300
DANGER_BORDER = "#F0CFCE"
DANGER_TEXT = "#99312F"

# --- §2 Type ---------------------------------------------------------------------

#: Подтверждено `QFontDatabase` на целевой машине (наряд 0011 §1): все три
#: установлены и несут иврит. Цифры равной ширины по умолчанию — `tnum` не нужен.
FONT_STACK = ('"Segoe UI"', '"Arial"', '"Tahoma"')
FONT_FAMILY = ", ".join(FONT_STACK)

#: Кегли — точками, как в каноне; Qt берёт дробный размер через `setPointSizeF`.
SIZE_TITLE = 18.0
SIZE_SUBTITLE = 11.5
SIZE_BODY = 13.0
SIZE_HEADER = 11.0
SIZE_PILL = 11.5
SIZE_STATUS = 11.5
SIZE_CAPTION = 10.5

WEIGHT_TITLE = 650
WEIGHT_IDENTIFIER = 600
WEIGHT_HEADER = 700
WEIGHT_PILL = 600
WEIGHT_BODY = 400

# --- §3 Metrics: высоты зон ------------------------------------------------------
#
# Шесть высот уплотнены ревизией канона 1.3 (ревью наряда 0012, М-1): заголовок
# 64→48, подвал 36→26, кнопка 32→28, поле 32→26, вкладки 44→34, шапка таблицы
# 34→30. Основание — вертикальный бюджет при 1280×760: около 40 px экономии, то
# есть **одна дополнительная строка данных на каждом экране**. Лента (44) и
# строка таблицы (40) не тронуты.

RIBBON_HEIGHT = 44
SECTION_HEADER_HEIGHT = 48
TAB_STRIP_HEIGHT = 34
TOOLBAR_HEIGHT = 52
TABLE_HEADER_HEIGHT = 30
TABLE_ROW_HEIGHT = 40
FOOTER_HEIGHT = 26

#: Панель внутри экрана — список списков справочников (макет S2: 280, зазор 8).
#: Это **не** боковое меню: навигация — лента, а панель принадлежит экрану.
PANEL_WIDTH = 280

#: Знак приложения на ленте — квадрат с буквой (макет S1).
RIBBON_MARK_SIZE = 22

#: Кружок радиокнопки. Своё число, а не «иконка в строке»: индикатор выбора
#: живёт рядом с текстом 13 px и обязан читаться на его высоте.
INDICATOR_SIZE = 14

#: Иконки: 16 в навигации и на панели действий, 13 в строке и в пилюле,
#: 34 в пустом состоянии (канон §3).
ICON_NAV = 16
ICON_ROW = 13
ICON_EMPTY = 34


# --- §3 Metrics: контролы --------------------------------------------------------

BUTTON_HEIGHT = 28

#: Поле ввода ниже кнопки: у него нет рамки-нажатия, и лишние два пикселя
#: воздуха внутри поля читаются как пустота, а не как форма.
INPUT_HEIGHT = 26

TOOLBAR_BUTTON_HEIGHT = 30
NAV_ITEM_HEIGHT = 32
PILL_HEIGHT = 22

# --- Список отклонений: единственная плавающая строка (QMS-018, наряд 0028) -------
#
# `design-system.md` §3, revision 1.7: «The one exception to the 40 px row is the
# deviations list, and it is the exception the rule was written around». Строка там
# несёт сами пилюли находок и растёт вместе с ними. Ни один другой экран этих
# чисел не наследует — механики эталонного экрана на прочие не тиражируются
# (наряд 0010 §3а, решение пользователя 2026-09-01).

#: Правило ширины таблицы (`design-system.md` §3, revision 1.8): «at most 90 % of
#: the window, **and never narrower than 1216**».
#:
#: Пол добавлен QMS-018 и пересматривает **пустое место**, а не смысл правила 90 %.
#: При 1920 правило отдаёт 192 px фона — это воздух вокруг рабочей поверхности. При
#: минимальном окне те же проценты отдают 128 px, пока колонки голодают, а пришёл
#: оператор именно за колонками. Пол оставляет по 32 px с каждой стороны и
#: возвращает сетке 64 px; шире 1352 px окна правит по-прежнему доля.
TABLE_SHARE = 0.9
TABLE_WIDTH_FLOOR = 1216

#: Пилюля находки — та же высота, что у пилюли исхода: одна форма на приложение.
CHIP_HEIGHT = 22

#: Зазор между пилюлями в ячейке.
GAP_CHIP = 4

#: Отступ ячейки с пилюлями сверху и снизу.
PAD_CHIP_ROW = 7

#: Строка `+N findings` под двумя показанными пилюлями.
MORE_LINE_HEIGHT = 16

#: Сторона треугольника раскрытия. Колонка 23 px, и стрелка должна
#: помещаться в неё целиком — рисуется путём, не текстом ячейки.
EXPANDER_ARROW = 9

#: Исход находки в пилюле — **форма несёт смысл, цвет только усиливает**
#: (`design-system.md` §1). Цвета взяты у исходов решения: зелёный «прошло»,
#: красный «не прошло»; серый нейтральный — «ещё не решали».
OUTCOME_PERMITTED = "#2E7D4F"
OUTCOME_REFUSED = "#99312F"

#: Толщина контура иконки исхода: 1 px теряется на 13 px рядом с текстом.
OUTCOME_STROKE = 2

#: Значок мензурки внутри пилюли — 13 px, размер значка в строке (канон §3).
CHIP_GLYPH_SIZE = 13

#: Высота строки отклонения при двух и при трёх-и-более находках.
#: 40 + (22 + 4) = 66 — вторая пилюля с зазором; 66 + 16 = 82 — строка остатка.
ROW_TWO_FINDINGS = 66
ROW_MANY_FINDINGS = 82

#: Панель раскрытия: своя шапка и своя строка находки.
#: 28 / 43 / 58 по числу показанных исследований — каждая следующая строка
#: списка исследований добавляет 15.
PANEL_HEADER_HEIGHT = 24

#: Шапка раскрытой панели **в диалоге** — подпись колонок, а не заголовок новой
#: таблицы (`design-system.md` §7 rev 1.13, «One level, one header»). Лоскутным
#: одеялом карточку делает повтор оформления, а не наличие подписей: сами имена
#: остаются — семь колонок чисел без имён это ребус, — но заливка и вес шапки
#: уходят. 12/500 против 11/700 обычной шапки.
#: Высота групповой строки таблицы прецедентов. Она называет группу и считает
#: её, то есть несёт больше, чем строка данных, но заголовком таблицы не
#: является — отсюда и высота между строкой и шапкой.
PRECEDENT_GROUP_HEIGHT = 32

#: Ниже этого последняя колонка панели раскрытия не сжимается: колонка, в которой
#: не помещается и одно слово, перестаёт быть колонкой.
PANEL_TAIL_MIN = 120

PANEL_CAPTION_SIZE = 12.0
PANEL_CAPTION_WEIGHT = 500
FINDING_ROW_HEIGHT = 28
FINDING_ROW_TWO = 43
FINDING_ROW_MANY = 58
ROW_ACTION_HEIGHT = 24

# --- §3 Metrics: радиусы, штрихи, воздух -----------------------------------------

RADIUS_CONTROL = 5
RADIUS_PANEL = 7
RADIUS_ROW_ACTION = 4
RADIUS_PILL = 11

BORDER_WIDTH = 1
SELECTION_BAR_WIDTH = 2
FOCUS_HALO_WIDTH = 3

PAD_SCREEN = 20
PAD_RIBBON_H = 16
#: Отступ внутри пункта ленты (макет S1: 14, при сжатии 10).
PAD_NAV_ITEM = 14
PAD_CELL = 10
GAP_CONTROL = 8
GAP_NAV_ICON = 9
GAP_PILL_ICON = 6

# --- §3 Metrics: окно ------------------------------------------------------------

WINDOW_MIN_WIDTH = 1280
WINDOW_MIN_HEIGHT = 760

#: Ширины диалогов — часть спеки компонента «диалог» (наряд 0010 §8.5).
DIALOG_NARROW = 560
DIALOG_MEDIUM = 720
DIALOG_WIDE = 980
DIALOG_FULL = 1180
DIALOG_HEIGHT_SHORT = 320
DIALOG_HEIGHT_MEDIUM = 620
DIALOG_HEIGHT_TALL = 820

#: Карточка отклонения — единственный экран из четырёх секций подряд (шапка,
#: находки, исследования, прецеденты). С появлением секции исследований
#: (QMS-018, наряд 0027) в `DIALOG_HEIGHT_TALL` она перестала помещаться:
#: вертикали не хватало, и Qt сдавливал шапку, а прецеденты — главный
#: deliverable экрана — схлопывались до полосы прокрутки. Значение снято
#: замером на нативной платформе (`tools/screenshots.py`), а не выбрано на глаз,
#: и держится ниже 1080 с запасом на панель задач.
DIALOG_HEIGHT_CARD = 960

# --- §3 Metrics: ширина колонки — свойство класса содержимого --------------------
#
# Решение пользователя 02.09 (находки №10 и №11): ширину задаёт **тип данных**,
# а не экран. Колонка объявляет класс, класс несёт предел в знаках; ни одна
# колонка не тянется, и таблица шириной равна сумме своих колонок.
#
# Числа — ориентир и подстраиваются по месту; важно, что их семь, а не
# шестнадцать экранов со своими.

#: Тринадцать, потому что столько в бизнес-номере `DEV-2026-0001`; на десяти
#: он обрезался первым же снимком: `DEV-260903-0001` — пятнадцать знаков, и
#: ещё один сверху на запас отрисовки. Величина расширена под `+0.05 / −0.05`
#: (одиннадцать с лишним), состояние — под пилюлю «Not decided» с её отступами:
#: числа подобраны замером строк в шрифте канона, а не на глаз.
WIDTH_IDENTIFIER = 16
WIDTH_MAGNITUDE = 12
WIDTH_DATE = 12
WIDTH_STATE = 16
WIDTH_COUNTER = 6
#: Свободному тексту предел тоже нужен — просто щедрый. Колонка-остаток
#: возвращает растягивание на всё окно, то есть ту самую болезнь.
WIDTH_TEXT = 60
WIDTH_LINK = 40

#: Утопленная поверхность: поле вокруг таблицы, которая уже своей области.
#: Роль канона, а не подкрашенный фон — приглушённый серо-синий, ощутимо
#: темнее белой карточки и бледнее акцента.
SURFACE_SUNKEN = "#E9EDF2"

#: Разделители колонок: минимальные, чтобы клик по ячейке был уверенным.
GRID_LINE = N_200

#: Порог, ниже которого строка отбора только мешает (решение В-6). Общий для
#: двух компонентов: `picker` прячет под ним строку отбора, поле `combo` ниже
#: этого числа не нужно вовсе — короткий список выбирают глазами.
PICKER_FILTER_THRESHOLD = 12

#: Сколько строк показывает всплывающий список поля с отбором. Больше — и он
#: закрывает форму под собой; меньше — приходится прокручивать там, где отбор
#: уже сделал всю работу.
POPUP_ROWS = 8

#: Высота вложенных таблиц в диалогах: находки в карточке, исследования в форме.
INLINE_TABLE_HEIGHT = 150
INLINE_TABLE_HEIGHT_SHORT = 130

#: Текстовая область на две-три строки: вложения, комментарий к находке.
#: Отдельно от высот таблиц: это поле ввода, а не список, и растить его до
#: таблицы значит отнимать вертикаль у того, ради чего форма открыта.
TEXT_AREA_HEIGHT = 70

# --- §3 Metrics: чертёж группы ---------------------------------------------------
#
# Баллонных токенов здесь больше нет (QMS-016, наряд 0014): чертёж приходит из
# конструкторского отдела уже размеченным, приложение своих баллонов не рисует.
# Осталось то, чем показывают саму картинку.

#: Доля ширины окна, которую занимает вписанный чертёж: «ширина минус ~15 %»
#: (решение 2026-09-02). Это главный элемент экрана, а не иллюстрация сбоку:
#: вся работа на нём — читать выноски и переносить с них числа.
DRAWING_FIT_RATIO = 0.85

#: Шаг зума кнопкой и колесом, и границы, за которые он не уходит: ниже нижней
#: чертёж не читается, выше верхней рассыпается в пиксели.
DRAWING_ZOOM_STEP = 1.25
DRAWING_ZOOM_MIN = 0.1
DRAWING_ZOOM_MAX = 8.0

#: Высота, ниже которой область чертежа не сжимается даже разделителем: под
#: ней остаётся не чертёж, а полоса прокрутки.
DRAWING_MIN_HEIGHT = 260

#: Пустое состояние (§8): иконка 34, заголовок 14/650, текст 12.
EMPTY_ICON_SIZE = 34
SIZE_EMPTY_TITLE = 14.0
SIZE_EMPTY_BODY = 12.0
WEIGHT_EMPTY_TITLE = 650
